{
  for (  Map.Entry<Path,SourcePath> entry : links.entrySet()) {
    builder.setReflectively("link(" + entry.getKey().toString() + ")",getResolver().getRelativePath(entry.getValue()).toString());
  }
  return builder;
}
