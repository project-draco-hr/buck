{
  List<Path> keyList=Lists.newArrayList(links.keySet());
  Collections.sort(keyList);
  for (  Path key : keyList) {
    builder.setReflectively("link(" + key.toString() + ")",getResolver().getPath(links.get(key)).toString());
  }
  return builder;
}
