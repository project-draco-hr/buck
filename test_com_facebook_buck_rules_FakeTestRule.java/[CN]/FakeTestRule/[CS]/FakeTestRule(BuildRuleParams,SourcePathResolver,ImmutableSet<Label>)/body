{
  this(buildRuleParams,resolver,labels,Optional.<Path>absent(),false,ImmutableList.<Step>of(),new Callable<TestResults>(){
    @Override public TestResults call(){
      throw new UnsupportedOperationException("interpretTestResults() not implemented");
    }
  }
);
}
