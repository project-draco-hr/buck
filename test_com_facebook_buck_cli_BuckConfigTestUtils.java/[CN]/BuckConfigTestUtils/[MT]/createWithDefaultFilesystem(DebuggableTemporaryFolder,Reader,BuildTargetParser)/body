{
  ProjectFilesystem projectFilesystem=new ProjectFilesystem(temporaryFolder.getRootPath());
  if (parser == null) {
    parser=new BuildTargetParser();
  }
  return createFromReader(reader,projectFilesystem,parser,Platform.detect(),ImmutableMap.copyOf(System.getenv()));
}
