{
  ProjectFilesystem projectFilesystem=new ProjectFilesystem(temporaryFolder.getRootPath());
  if (parser == null) {
    parser=new BuildTargetParser();
  }
  return BuckConfig.createFromReader(reader,projectFilesystem,parser,Platform.detect(),ImmutableMap.copyOf(System.getenv()));
}
