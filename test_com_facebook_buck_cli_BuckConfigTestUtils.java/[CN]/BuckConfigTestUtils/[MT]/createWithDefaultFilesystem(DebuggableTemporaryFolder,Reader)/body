{
  ProjectFilesystem projectFilesystem=new ProjectFilesystem(temporaryFolder.getRootPath());
  return createFromReader(reader,projectFilesystem,Platform.detect(),ImmutableMap.copyOf(System.getenv()));
}
