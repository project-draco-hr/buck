{
  Path path=Paths.get("hello/world.txt");
  PathSourcePath pathSourcePath1=new PathSourcePath(path);
  String actual1=new SourcePathResolver(new BuildRuleResolver()).getSourcePathName(BuildTargetFactory.newInstance("//:test"),pathSourcePath1);
  assertEquals(path.toString(),actual1);
}
