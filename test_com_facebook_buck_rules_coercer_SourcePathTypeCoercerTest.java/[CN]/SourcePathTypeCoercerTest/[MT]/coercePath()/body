{
  String path="hello.a";
  projectFilesystem.touch(Paths.get(path));
  SourcePath sourcePath=sourcePathTypeCoercer.coerce(buildTargetParser,projectFilesystem,pathRelativeToProjectRoot,path);
  assertEquals(new PathSourcePath(Paths.get(path)),sourcePath);
}
