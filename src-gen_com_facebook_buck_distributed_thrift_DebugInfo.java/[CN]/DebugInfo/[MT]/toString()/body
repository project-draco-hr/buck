{
  StringBuilder sb=new StringBuilder("DebugInfo(");
  boolean first=true;
  if (isSetLogBook()) {
    sb.append("logBook:");
    if (this.logBook == null) {
      sb.append("null");
    }
 else {
      sb.append(this.logBook);
    }
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
