{
  connection=createNiceMock(HttpURLConnection.class);
  projectFilesystem=createMock(ProjectFilesystem.class);
  cache=new FakeHttpArtifactCache(connection,projectFilesystem,BuckEventBusFactory.newInstance(),ImmutableMap.of("X-Header","test"));
}
