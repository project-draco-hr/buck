{
  return FluentIterable.from(delegate.asListWithoutComments(delegate.getValue("project","temp_files"))).transform(new Function<String,Pattern>(){
    @Nullable @Override public Pattern apply(    String input){
      return Pattern.compile(input);
    }
  }
).toSet();
}
