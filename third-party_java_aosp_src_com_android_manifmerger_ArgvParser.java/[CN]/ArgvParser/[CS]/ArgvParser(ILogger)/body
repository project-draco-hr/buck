{
  super(logger,ACTIONS);
  define(Mode.STRING,true,VERB_MERGE,NO_VERB_OBJECT,"o",KEY_OUT,"Output path (where to write the merged manifest). Use - for stdout.",null);
  define(Mode.STRING,true,VERB_MERGE,NO_VERB_OBJECT,"1",KEY_MAIN,"Path of the main manifest (what to merge *into*)",null);
  define(Mode.STRING_ARRAY,true,VERB_MERGE,NO_VERB_OBJECT,"2",KEY_LIBS,"Paths of library manifests to be merged into the main one.",null);
}
