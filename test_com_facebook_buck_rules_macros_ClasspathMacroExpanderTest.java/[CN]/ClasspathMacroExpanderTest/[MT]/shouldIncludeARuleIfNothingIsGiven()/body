{
  BuildRule rule=JavaLibraryBuilder.createBuilder(BuildTargetFactory.newInstance("//cheese:cake")).addSrc(Paths.get("Example.java")).build(new BuildRuleResolver());
  String classpath=expander.expand(new FakeProjectFilesystem(),rule);
  assertEquals(rule.getPathToOutputFile().toString(),classpath);
}
