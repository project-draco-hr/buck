{
  super(params,sourcePathResolver,srcs,ImmutableList.of("-lib"),compiler,BuildTargets.getGenPath(params.getBuildTarget(),"%s/lib" + params.getBuildTarget().getShortName() + ".a"));
  this.params=params;
  this.buildRuleResolver=buildRuleResolver;
}
