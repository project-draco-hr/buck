{
  return new Function<Tool,Linker>(){
    @Override public Linker apply(    Tool input){
      try {
        return ldClass.getConstructor(Tool.class).newInstance(input);
      }
 catch (      ReflectiveOperationException e) {
        throw new RuntimeException(e);
      }
    }
  }
;
}
