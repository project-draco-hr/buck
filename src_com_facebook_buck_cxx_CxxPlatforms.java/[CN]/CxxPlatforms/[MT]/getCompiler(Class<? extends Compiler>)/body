{
  return new Function<Tool,Compiler>(){
    @Override public Compiler apply(    Tool input){
      try {
        return ccClass.getConstructor(Tool.class).newInstance(input);
      }
 catch (      ReflectiveOperationException e) {
        throw new RuntimeException(e);
      }
    }
  }
;
}
