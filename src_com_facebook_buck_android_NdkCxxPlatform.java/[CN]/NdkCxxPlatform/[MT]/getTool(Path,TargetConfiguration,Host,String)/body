{
  return new PathSourcePath(ndkRoot.resolve("toolchains").resolve(targetConfiguration.toolchain.toString()).resolve("prebuilt").resolve(host.toString()).resolve(targetConfiguration.toolchainPrefix.toString()).resolve("bin").resolve(tool));
}
