{
  return new VersionedTool(getSourcePath(ndkRoot,targetConfiguration,host,tool),ImmutableList.of("-B" + ndkRoot.resolve("platforms").resolve(targetConfiguration.targetPlatform).resolve("arch-" + targetConfiguration.targetArch).resolve("usr").resolve("lib").toString()),tool,targetConfiguration.toolchain.toString() + " " + version);
}
