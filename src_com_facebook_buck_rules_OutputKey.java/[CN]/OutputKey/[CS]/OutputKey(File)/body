{
  HashCode hc;
  try {
    byte[] fileBytes=Files.toByteArray(outputFile);
    hc=Hashing.sha1().hashBytes(fileBytes);
  }
 catch (  IOException e) {
    hc=null;
  }
  hashCode=hc;
  idempotent=(hc != null);
}
