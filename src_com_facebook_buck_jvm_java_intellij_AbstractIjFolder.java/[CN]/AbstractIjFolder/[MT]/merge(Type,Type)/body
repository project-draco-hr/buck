{
  Preconditions.checkNotNull(left);
  Preconditions.checkNotNull(right);
  if (left.equals(right)) {
    return left;
  }
  Preconditions.checkArgument(!left.equals(EXCLUDE_FOLDER) && !right.equals(EXCLUDE_FOLDER),"Exclude folders cannot merge with other types.");
  return SOURCE_FOLDER;
}
