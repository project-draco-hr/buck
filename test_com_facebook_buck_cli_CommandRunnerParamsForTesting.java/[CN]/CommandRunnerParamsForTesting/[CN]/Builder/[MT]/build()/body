{
  Repository repository=new Repository("command runner params for testing",projectFilesystem,buildRuleTypes);
  return new CommandRunnerParamsForTesting(console,repository,androidDirectoryResolver,artifactCacheFactory,eventBus,pythonInterpreter,platform,environment);
}
