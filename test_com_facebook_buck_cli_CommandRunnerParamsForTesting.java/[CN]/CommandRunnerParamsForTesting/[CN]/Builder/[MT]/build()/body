{
  Repository repository=new Repository("command runner params for testing",projectFilesystem,buildRuleTypes,new FakeBuckConfig());
  return new CommandRunnerParamsForTesting(console,repository,androidDirectoryResolver,artifactCacheFactory,eventBus,pythonInterpreter,platform,environment,javaPackageFinder);
}
