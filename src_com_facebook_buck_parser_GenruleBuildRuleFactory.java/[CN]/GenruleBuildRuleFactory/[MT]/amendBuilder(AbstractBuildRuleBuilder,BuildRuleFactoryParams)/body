{
  Genrule.Builder builder=((Genrule.Builder)abstractBuilder);
  String cmd=params.getRequiredStringAttribute("cmd");
  builder.setCmd(cmd);
  String out=params.getRequiredStringAttribute("out");
  builder.setOut(out);
}
