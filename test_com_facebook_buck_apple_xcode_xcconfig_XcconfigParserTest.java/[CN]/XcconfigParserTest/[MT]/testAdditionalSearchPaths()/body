{
  ImmutableMap<String,String> files=ImmutableMap.of("some_other_search_path/file1.xcconfig",s2 + "\n" + s3,"folder/file2.xcconfig",s1 + "\n" + "#include \"file1.xcconfig\"");
  ProjectFilesystem filesystem=new FakeReadonlyProjectFilesystem(files);
  assertFileHasValue(filesystem,"folder/file2.xcconfig",ImmutableList.of(Paths.get("some_other_search_path")),values);
}
