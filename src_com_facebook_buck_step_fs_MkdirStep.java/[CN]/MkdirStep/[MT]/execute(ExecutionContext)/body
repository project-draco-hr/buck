{
  try {
    context.getProjectFilesystem().mkdirs(pathRelativeToProjectRoot);
  }
 catch (  IOException e) {
    context.getBuckEventBus().post(ThrowableLogEvent.create(e,"Cannot make directories: %s",pathRelativeToProjectRoot));
    return 1;
  }
  return 0;
}
