{
  return new Function<FrameworkPath,Path>(){
    @Override public Path apply(    FrameworkPath input){
      return getExpandFunction(sourcePathResolver,sourceTreePathResolver).apply(input).getParent();
    }
  }
;
}
