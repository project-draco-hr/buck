{
  return ImmutableList.<String>builder().addAll(ocamlDepTool.getCommandPrefix(resolver)).add("-one-line").add("-native").addAll(flags).addAll(FluentIterable.from(input).transform(resolver.getAbsolutePathFunction()).transform(Functions.toStringFunction())).build();
}
