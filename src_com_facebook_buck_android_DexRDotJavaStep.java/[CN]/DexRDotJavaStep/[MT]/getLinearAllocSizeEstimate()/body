{
  Preconditions.checkState(stepFinished,"Trying to access the linear alloc size estimate of dex output " + "before DexRDotJavaStep finished its execution successfully.");
  return linearAllocSizeEstimate.get();
}
