{
  assertEquals("41",Escaper.hex('A'));
  assertEquals('A',Integer.parseInt(Escaper.hex('A'),16));
  assertEquals("61",Escaper.hex('a'));
  assertEquals('a',Integer.parseInt(Escaper.hex('a'),16));
}
