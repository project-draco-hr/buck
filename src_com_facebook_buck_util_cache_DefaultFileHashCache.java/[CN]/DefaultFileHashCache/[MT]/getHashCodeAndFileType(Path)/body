{
  if (projectFilesystem.isDirectory(path)) {
    return HashCodeAndFileType.of(getDirHashCode(path),HashCodeAndFileType.Type.DIRECTORY);
  }
 else {
    return HashCodeAndFileType.of(getFileHashCode(path),HashCodeAndFileType.Type.FILE);
  }
}
