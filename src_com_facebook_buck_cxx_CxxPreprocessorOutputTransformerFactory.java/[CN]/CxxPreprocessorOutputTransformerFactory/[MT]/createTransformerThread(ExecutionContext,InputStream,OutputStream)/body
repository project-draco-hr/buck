{
  return new LineProcessorRunnable(context.getExecutorService(ExecutionContext.ExecutorPool.CPU),inputStream,outputStream){
    @Override public String process(    String line){
      return transformLine(line);
    }
  }
;
}
