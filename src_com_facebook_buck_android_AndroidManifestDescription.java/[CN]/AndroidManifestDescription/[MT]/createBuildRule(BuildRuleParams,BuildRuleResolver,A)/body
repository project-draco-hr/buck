{
  ImmutableSet<Path> manifestFiles=findManifestFiles(args);
  ImmutableSortedSet<BuildRule> newDeps=FluentIterable.from(args.deps.get()).filter(Predicates.not(Predicates.instanceOf(AndroidResource.class))).filter(Predicates.not(Predicates.instanceOf(AndroidLibrary.class))).toSortedSet(BuildTarget.BUILD_TARGET_COMPARATOR);
  return new AndroidManifest(params.copyWithDeps(newDeps,params.getExtraDeps()),args.skeleton,manifestFiles);
}
