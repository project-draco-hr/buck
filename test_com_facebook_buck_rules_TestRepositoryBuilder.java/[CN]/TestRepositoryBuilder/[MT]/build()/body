{
  if (parserFactory == null) {
    return new Repository(name,filesystem,NULL_WATCHMAN,buckConfig,buildRuleTypes,androidDirectoryResolver);
  }
  return new Repository(name,filesystem,NULL_WATCHMAN,buckConfig,buildRuleTypes,androidDirectoryResolver){
    @Override public ProjectBuildFileParserFactory createBuildFileParserFactory(    boolean useWatchmanGlob){
      return parserFactory;
    }
  }
;
}
