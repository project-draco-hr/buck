{
  if (parserFactory == null) {
    return new Repository(name,filesystem,buckConfig,buildRuleTypes,androidDirectoryResolver);
  }
  return new Repository(name,filesystem,buckConfig,buildRuleTypes,androidDirectoryResolver){
    @Override public ProjectBuildFileParserFactory createBuildFileParserFactory(    String pythonInterpreter,    boolean useWatchmanGlob,    Watchman watchman){
      return parserFactory;
    }
  }
;
}
