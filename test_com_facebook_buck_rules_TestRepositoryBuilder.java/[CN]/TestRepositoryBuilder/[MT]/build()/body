{
  RepositoryFactory fakeRepositoryFactory=new RepositoryFactory(ImmutableMap.copyOf(System.getenv()),Platform.detect(),new TestConsole(),rootPath.toAbsolutePath());
  return Repository.of(name,filesystem,buildRuleTypes,buckConfig,fakeRepositoryFactory,androidDirectoryResolver);
}
