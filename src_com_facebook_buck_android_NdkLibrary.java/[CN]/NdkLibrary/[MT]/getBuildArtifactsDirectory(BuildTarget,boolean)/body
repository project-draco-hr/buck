{
  Path base=isScratchDir ? getProjectFilesystem().getBuckPaths().getScratchDir() : getProjectFilesystem().getBuckPaths().getGenDir();
  return base.resolve(target.getBasePath()).resolve(lastPathComponent);
}
