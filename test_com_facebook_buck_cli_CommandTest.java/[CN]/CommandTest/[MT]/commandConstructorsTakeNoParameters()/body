{
  for (  Command command : Command.values()) {
    try {
      command.getCommandRunnerClass().getDeclaredConstructor().newInstance();
    }
 catch (    Exception e) {
      fail(String.format("%s: %s %s",command.getDeclaringClass(),e.getClass(),e.getMessage()));
    }
  }
}
