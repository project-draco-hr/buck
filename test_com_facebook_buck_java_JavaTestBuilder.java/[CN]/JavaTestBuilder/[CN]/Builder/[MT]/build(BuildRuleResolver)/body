{
  BuildRuleParams params=new FakeBuildRuleParamsBuilder(target).setDeps(deps.build()).build();
  DescribedRule rule=new DescribedRule(JavaTestDescription.TYPE,build(),params);
  return resolver.addToIndex(rule);
}
