{
  ProjectFilesystem projectFilesystem=new ProjectFilesystem(Paths.get("."));
  return new DefaultAndroidDirectoryResolver(projectFilesystem,Optional.<String>absent(),new DefaultPropertyFinder(projectFilesystem,ImmutableMap.copyOf(System.getenv())));
}
