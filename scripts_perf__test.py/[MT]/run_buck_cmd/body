def run_buck_cmd(args, cwd, cmd, extra_env={}, **kwargs):
    env = os.environ.copy()
    env.update(extra_env)
    env['LIBRARY_PATH'] = os.path.join(distutils.sysconfig.PREFIX, 'lib')
    subprocess.check_call(([args.path_to_buck] + cmd), cwd=cwd, env=env, **kwargs)
