def git_get_revisions(args):
    return list(reversed(subprocess.check_output(['git', 'log', '--pretty=format:%H', 'HEAD', '-n', str((args.revisions_to_go_back + 1))], stderr=DEVNULL, cwd=args.repo_under_test).splitlines()))
