{
  return FluentIterable.from(frameworkSearchPaths.or(ImmutableSet.<Path>of())).transform(Functions.toStringFunction()).transform(CxxFlags.getTranslateMacrosFn(cxxPlatform)).transform(MorePaths.TO_PATH).toSet();
}
