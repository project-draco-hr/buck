{
  return FluentIterable.from(frameworkSearchPaths.or(ImmutableList.<Path>of())).transform(Functions.toStringFunction()).transform(CxxFlags.getTranslateMacrosFn(cxxPlatform)).transform(MorePaths.TO_PATH).toList();
}
