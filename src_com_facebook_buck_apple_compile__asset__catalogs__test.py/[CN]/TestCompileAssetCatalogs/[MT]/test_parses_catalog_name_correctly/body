def test_parses_catalog_name_correctly(self):
    self.assertEqual(catalog_name_from_path('/AssetCatalog.xcassets'), 'AssetCatalog')
    self.assertEqual(catalog_name_from_path('/a/b/c/d.xcassets'), 'd')
