{
  if (!(event instanceof ArtifactCacheEvent)) {
    return false;
  }
  ArtifactCacheEvent that=(ArtifactCacheEvent)event;
  return Objects.equal(getOperation(),that.getOperation()) && Objects.equal(getRuleKeys(),that.getRuleKeys());
}
