{
  if (extension == null)   throw new IllegalArgumentException("null input: extension");
  final String canonical=canonicalizeExtension(extension);
  if (extension.length() <= 1)   throw new IllegalArgumentException("empty input: extension");
  m_extension=canonical;
}
