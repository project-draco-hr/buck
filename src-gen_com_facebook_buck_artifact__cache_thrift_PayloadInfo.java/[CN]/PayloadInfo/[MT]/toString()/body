{
  StringBuilder sb=new StringBuilder("PayloadInfo(");
  boolean first=true;
  if (isSetSizeBytes()) {
    sb.append("sizeBytes:");
    sb.append(this.sizeBytes);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
