{
  return new RustLibrary(params,new SourcePathResolver(resolver),ImmutableSet.copyOf(args.srcs),ImmutableSet.copyOf(args.features.get()),ImmutableList.copyOf(args.rustcFlags.get()),rustBuckConfig.getRustCompiler().get());
}
