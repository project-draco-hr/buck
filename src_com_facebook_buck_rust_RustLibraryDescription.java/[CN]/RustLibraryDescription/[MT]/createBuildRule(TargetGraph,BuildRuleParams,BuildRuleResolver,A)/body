{
  return new RustLibrary(params,new SourcePathResolver(resolver),ImmutableSortedSet.copyOf(args.srcs),ImmutableSortedSet.copyOf(args.features.get()),ImmutableList.copyOf(args.rustcFlags.get()),rustBuckConfig.getRustCompiler().get());
}
