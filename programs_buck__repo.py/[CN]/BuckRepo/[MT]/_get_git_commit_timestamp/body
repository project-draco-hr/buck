def _get_git_commit_timestamp(self):
    if (self._is_buck_repo_dirty_override or (not self._is_git)):
        return (-1)
    return check_output(['git', 'log', '--pretty=format:%ct', '-1', 'HEAD', '--'], cwd=self._buck_dir).strip()
