{
  return new DefaultParserTargetNodeFactory(eventBus,marshaller,typeCoercerFactory,Optional.<LoadingCache<Cell,BuildFileTree>>absent(),new TargetNodeListener(){
    @Override public void onCreate(    Path buildFile,    TargetNode<?> node) throws IOException {
    }
  }
);
}
