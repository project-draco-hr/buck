{
  ImmutableList.Builder<Step> steps=ImmutableList.builder();
  Path workingDirectory=outputFile.getParent();
  steps.add(new MakeCleanDirectoryStep(workingDirectory));
  Path tempJarFolder=workingDirectory.resolve("tmp");
  steps.add(new CopyResourcesStep(getBuildTarget(),filesForGwtModule,tempJarFolder,context.getJavaPackageFinder()));
  steps.add(new JarDirectoryStep(outputFile,ImmutableSet.of(tempJarFolder),null,null));
  buildableContext.recordArtifact(outputFile);
  return steps.build();
}
