{
  if (!(o instanceof AbstractBuckEvent)) {
    return false;
  }
  AbstractBuckEvent that=(AbstractBuckEvent)o;
  return isRelatedTo(that) && getThreadId() == that.getThreadId() && Objects.equals(getClass(),that.getClass());
}
