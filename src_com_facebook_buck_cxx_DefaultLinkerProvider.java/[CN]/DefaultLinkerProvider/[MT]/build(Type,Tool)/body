{
switch (type) {
case DARWIN:
    return new DarwinLinker(tool);
case GNU:
  return new GnuLinker(tool);
case WINDOWS:
return new GnuLinker(tool);
case UNKNOWN:
return new UnknownLinker(tool);
}
throw new IllegalStateException("unexpected type: " + type);
}
