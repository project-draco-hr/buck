{
  return extractFirstMatchingLine(logPath,ARG_PATTERN).transform(new Function<Matcher,String>(){
    @Override public String apply(    Matcher input){
      return input.group("args");
    }
  }
);
}
