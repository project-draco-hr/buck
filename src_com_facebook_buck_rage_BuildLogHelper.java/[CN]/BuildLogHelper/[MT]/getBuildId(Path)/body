{
  return extractFirstMatchingLine(logPath,LOG_LINE_PATTERN).transform(new Function<Matcher,BuildId>(){
    @Override public BuildId apply(    Matcher input){
      return new BuildId(input.group("buildid"));
    }
  }
);
}
