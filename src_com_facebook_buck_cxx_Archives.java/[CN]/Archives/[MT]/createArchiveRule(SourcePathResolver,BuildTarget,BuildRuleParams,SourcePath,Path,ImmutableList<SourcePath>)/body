{
  BuildRuleParams archiveParams=originalParams.copyWithChanges(ARCHIVE_TYPE,target,ImmutableSortedSet.<BuildRule>of(),ImmutableSortedSet.copyOf(resolver.filterBuildRuleInputs(inputs)));
  return new Archive(archiveParams,resolver,archiver,output,inputs);
}
