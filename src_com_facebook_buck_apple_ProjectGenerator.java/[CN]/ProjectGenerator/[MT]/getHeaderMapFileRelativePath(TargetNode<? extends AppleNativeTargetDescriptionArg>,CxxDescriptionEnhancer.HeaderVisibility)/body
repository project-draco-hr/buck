{
  Path filePath=CxxDescriptionEnhancer.getHeaderMapFilePath(BuildTarget.of(targetNode.getBuildTarget().getUnflavoredBuildTarget()),DefaultCxxPlatforms.FLAVOR,headerVisibility);
  return pathRelativizer.outputDirToRootRelative(filePath);
}
