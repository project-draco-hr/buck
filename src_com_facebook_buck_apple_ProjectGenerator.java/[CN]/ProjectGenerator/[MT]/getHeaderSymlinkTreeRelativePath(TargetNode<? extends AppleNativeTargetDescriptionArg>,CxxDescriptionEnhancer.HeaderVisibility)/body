{
  Path filePath=CxxDescriptionEnhancer.getHeaderSymlinkTreePath(BuildTarget.of(targetNode.getBuildTarget().getUnflavoredBuildTarget()),DefaultCxxPlatforms.FLAVOR,headerVisibility);
  return pathRelativizer.outputDirToRootRelative(filePath);
}
