{
  Optional<Path> treeRoot=AppleDescriptions.getPathToHeaderSymlinkTree(targetNode,headerVisibility);
  Preconditions.checkState(treeRoot.isPresent(),"%s does not have a header symlink tree.",targetNode);
  return pathRelativizer.outputDirToRootRelative(treeRoot.get()).toString();
}
