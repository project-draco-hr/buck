{
  BuckBuildManager.getInstance(project()).setBuilding(project,false);
  SystemNotifications.getInstance().notify("Buck",StringUtil.capitalize(command.name()) + " Finished",this.processExitSuccesfull() ? "Successful" : "Failed");
}
