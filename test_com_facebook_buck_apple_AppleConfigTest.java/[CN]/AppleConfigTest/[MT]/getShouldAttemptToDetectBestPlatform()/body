{
  BuckConfig buckConfig=FakeBuckConfig.builder().setSections(ImmutableMap.of("apple",ImmutableMap.of("attempt_to_detect_best_platform","true"))).build();
  AppleConfig config=new AppleConfig(buckConfig);
  assertThat(config.shouldAttemptToDetermineBestCxxPlatform(),equalTo(true));
}
