{
  DefaultVersionControlCmdLineInterfaceFactory vcFactory=new DefaultVersionControlCmdLineInterfaceFactory(repoRootDir,new TestProcessExecutorFactory(),new VersionControlBuckConfig(FakeBuckConfig.builder().build()));
  return vcFactory.createCmdLineInterface();
}
