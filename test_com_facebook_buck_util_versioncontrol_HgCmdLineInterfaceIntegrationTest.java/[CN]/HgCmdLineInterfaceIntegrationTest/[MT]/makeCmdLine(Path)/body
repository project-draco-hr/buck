{
  DefaultVersionControlCmdLineInterfaceFactory vcFactory=new DefaultVersionControlCmdLineInterfaceFactory(repoRootDir,new ProcessExecutor(new TestConsole()),new VersionControlBuckConfig(new FakeBuckConfig()));
  return vcFactory.createCmdLineInterface();
}
