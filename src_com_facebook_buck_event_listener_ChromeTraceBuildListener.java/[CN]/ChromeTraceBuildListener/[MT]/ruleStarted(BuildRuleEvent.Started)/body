{
  BuildRule buildRule=started.getBuildRule();
  String ruleKey;
  try {
    ruleKey=buildRule.getRuleKey().toString();
  }
 catch (  IOException e) {
    ruleKey="INACCESSIBLE: " + e.getMessage();
  }
  writeChromeTraceEvent("buck",buildRule.getFullyQualifiedName(),ChromeTraceEvent.Phase.BEGIN,ImmutableMap.<String,String>of("rule_key",ruleKey),started);
}
