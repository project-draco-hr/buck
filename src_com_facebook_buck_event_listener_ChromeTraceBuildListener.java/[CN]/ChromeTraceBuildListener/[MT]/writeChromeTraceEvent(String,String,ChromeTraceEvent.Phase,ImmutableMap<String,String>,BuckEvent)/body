{
  final ChromeTraceEvent chromeTraceEvent=new ChromeTraceEvent(category,name,phase,0,event.getThreadId(),TimeUnit.NANOSECONDS.toMicros(event.getNanoTime()),arguments);
  submitTraceEvent(chromeTraceEvent);
}
