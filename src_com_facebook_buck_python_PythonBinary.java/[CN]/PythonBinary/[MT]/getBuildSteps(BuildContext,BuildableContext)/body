{
  ImmutableList.Builder<Step> steps=ImmutableList.builder();
  Path binPath=getBinPath();
  steps.add(new MkdirStep(binPath.getParent()));
  steps.add(new PexStep(pathToPex,pythonEnvironment.getPythonPath(),binPath,PythonUtil.toModuleName(getBuildTarget(),main.toString()),PythonUtil.getPathMapFromSourcePaths(components.getModules()),PythonUtil.getPathMapFromSourcePaths(components.getResources()),PythonUtil.getPathMapFromSourcePaths(components.getNativeLibraries())));
  buildableContext.recordArtifact(getBinPath());
  return steps.build();
}
