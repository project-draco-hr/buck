{
  for (  Path artifact : args.getAllOutputs()) {
    buildableContext.recordArtifact(artifact);
  }
  return ImmutableList.of(new MkdirStep(args.output.getParent()),new OCamlLinkStep(getProjectFilesystem().getRootPath(),args));
}
