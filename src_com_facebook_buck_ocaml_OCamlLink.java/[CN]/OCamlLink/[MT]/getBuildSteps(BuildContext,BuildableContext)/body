{
  for (  Path artifact : args.getAllOutputs()) {
    buildableContext.recordArtifact(artifact);
  }
  return ImmutableList.of(new MkdirStep(getProjectFilesystem(),args.output.getParent()),new OCamlLinkStep(getProjectFilesystem().getRootPath(),getResolver(),args));
}
