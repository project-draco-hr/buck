{
  if (isLibrary && !isBytecode) {
    return OCamlUtil.getExtensionVariants(outputRelativePath,OCamlCompilables.OCAML_A,OCamlCompilables.OCAML_CMXA);
  }
 else {
    return ImmutableSet.of(outputRelativePath);
  }
}
