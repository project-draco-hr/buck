{
  return FluentIterable.from(ruleResolvers.asMap().values()).transformAndConcat(new Function<BuildRuleResolver,Iterable<BuildRule>>(){
    @Override public Iterable<BuildRule> apply(    BuildRuleResolver input){
      return input.getBuildRules();
    }
  }
);
}
