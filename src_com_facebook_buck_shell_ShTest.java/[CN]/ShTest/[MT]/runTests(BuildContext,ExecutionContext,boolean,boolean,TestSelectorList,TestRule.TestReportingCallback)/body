{
  if (isDryRun) {
    return ImmutableList.of();
  }
  Step mkdirClean=new MakeCleanDirectoryStep(getPathToTestOutputDirectory());
  Step runTest=new RunShTestAndRecordResultStep(getProjectFilesystem().getRootPath(),getResolver().getPath(test),args,getPathToTestOutputResult());
  return ImmutableList.of(mkdirClean,runTest);
}
