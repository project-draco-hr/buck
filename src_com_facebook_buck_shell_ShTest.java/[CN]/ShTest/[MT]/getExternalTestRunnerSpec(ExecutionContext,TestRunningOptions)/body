{
  return ExternalTestRunnerTestSpec.builder().setTarget(getBuildTarget()).setType("custom").addCommand(getResolver().getResolvedPath(test).toString()).addAllCommand(args).addAllLabels(getLabels()).addAllContacts(getContacts()).build();
}
