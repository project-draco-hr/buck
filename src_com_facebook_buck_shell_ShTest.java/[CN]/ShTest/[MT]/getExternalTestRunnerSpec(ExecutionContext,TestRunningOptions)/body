{
  return ExternalTestRunnerTestSpec.builder().setTarget(getBuildTarget()).setType("custom").addCommand(getResolver().getAbsolutePath(test).toString()).addAllCommand(FluentIterable.from(args).transform(Arg.stringifyFunction()).toList()).addAllLabels(getLabels()).addAllContacts(getContacts()).build();
}
