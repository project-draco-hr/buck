{
  ImmutableMap.Builder<Path,Path> paths=ImmutableMap.builder();
  for (  ImmutableMap.Entry<Path,SourcePath> src : sourcePaths.entrySet()) {
    paths.put(src.getKey(),src.getValue().resolve());
  }
  return paths.build();
}
