{
  buildableContext.recordArtifact(dsymOutputPath);
  return ImmutableList.<Step>of(new DsymStep(getProjectFilesystem(),dsymutil.getEnvironment(getResolver()),dsymutil.getCommandPrefix(getResolver()),getResolver().getAbsolutePath(unstrippedBinarySourcePath),dsymOutputPath));
}
