{
  buildableContext.recordArtifact(dsymPath);
  DsymStep generateDsymStep=new DsymStep(getProjectFilesystem(),dsymutil.getEnvironment(getResolver()),dsymutil.getCommandPrefix(getResolver()),bundleBinaryPath,dsymPath);
  Step stripDebugSymbolsStep=getStripDebugSymbolsStep();
  return ImmutableList.of(generateDsymStep,stripDebugSymbolsStep);
}
