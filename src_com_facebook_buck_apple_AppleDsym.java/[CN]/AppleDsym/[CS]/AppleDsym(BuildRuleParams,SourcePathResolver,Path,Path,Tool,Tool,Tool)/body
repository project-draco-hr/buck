{
  super(buildRuleParams,resolver);
  this.dsymutil=dsymutil;
  this.lldb=lldb;
  this.strip=strip;
  this.bundleRoot=bundleRoot;
  this.bundleBinaryPath=bundleBinaryPath;
  this.dsymPath=bundleRoot.getParent().resolve(bundleRoot.getFileName().toString() + ".dSYM");
}
