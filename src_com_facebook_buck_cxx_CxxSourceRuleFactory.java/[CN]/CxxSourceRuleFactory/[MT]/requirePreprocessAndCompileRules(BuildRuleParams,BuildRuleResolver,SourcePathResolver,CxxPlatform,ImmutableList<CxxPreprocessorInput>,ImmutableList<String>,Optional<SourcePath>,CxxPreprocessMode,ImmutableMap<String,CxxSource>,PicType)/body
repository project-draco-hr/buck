{
  CxxSourceRuleFactory factory=new CxxSourceRuleFactory(params,resolver,pathResolver,cxxPlatform,cxxPreprocessorInput,compilerFlags,prefixHeader,pic);
  return factory.requirePreprocessAndCompileRules(resolver,strategy,sources);
}
