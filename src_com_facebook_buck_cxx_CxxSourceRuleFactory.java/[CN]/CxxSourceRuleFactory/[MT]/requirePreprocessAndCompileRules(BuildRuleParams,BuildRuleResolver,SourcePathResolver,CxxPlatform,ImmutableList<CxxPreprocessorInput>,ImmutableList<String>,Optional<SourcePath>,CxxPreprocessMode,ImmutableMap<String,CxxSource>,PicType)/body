{
  CxxSourceRuleFactory factory=new CxxSourceRuleFactory(params,resolver,pathResolver,cxxPlatform,cxxPreprocessorInput,compilerFlags,prefixHeader);
  return factory.requirePreprocessAndCompileRules(resolver,strategy,sources,pic);
}
