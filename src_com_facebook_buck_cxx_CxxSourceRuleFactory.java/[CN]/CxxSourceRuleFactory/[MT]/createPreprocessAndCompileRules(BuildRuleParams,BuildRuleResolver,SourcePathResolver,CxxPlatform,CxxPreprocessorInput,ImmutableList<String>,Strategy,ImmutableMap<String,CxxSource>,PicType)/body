{
  CxxSourceRuleFactory factory=new CxxSourceRuleFactory(params,resolver,pathResolver,cxxPlatform,cxxPreprocessorInput,compilerFlags);
  return factory.createPreprocessAndCompileRules(resolver,strategy,sources,pic);
}
