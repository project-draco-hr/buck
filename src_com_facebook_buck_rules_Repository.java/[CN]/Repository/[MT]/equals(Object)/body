{
  if (!(obj instanceof Repository)) {
    return false;
  }
  Repository that=(Repository)obj;
  return this.getFilesystem().equals(that.getFilesystem()) && this.getBuckConfig().equals(that.getBuckConfig());
}
