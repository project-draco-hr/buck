{
  if (!(obj instanceof Repository)) {
    return false;
  }
  Repository that=(Repository)obj;
  return Objects.equals(getFilesystem(),that.getFilesystem()) && Objects.equals(getBuckConfig(),that.getBuckConfig()) && Objects.equals(androidDirectoryResolver,that.androidDirectoryResolver);
}
