{
  assertEquals(Optional.<Path>absent(),MoreFiles.searchPathsForExecutable(Paths.get("bar"),ImmutableList.of(Paths.get("baz","foo")),Functions.<Path>forPredicate(Predicates.<Path>alwaysFalse())));
}
