{
switch (field) {
case BUILD_ID:
    if (value == null) {
      unsetBuildId();
    }
 else {
      setBuildId((BuildId)value);
    }
  break;
case FILES:
if (value == null) {
  unsetFiles();
}
 else {
  setFiles((List<FileInfo>)value);
}
break;
}
}
