{
  for (  Dex d : dexs) {
    transformAnnotationSets(d,indexMaps.get(d));
  }
  for (  Dex d : dexs) {
    transformAnnotationSetRefLists(d,indexMaps.get(d));
  }
  for (  Dex d : dexs) {
    transformAnnotationDirectories(d,indexMaps.get(d));
  }
  for (  Dex d : dexs) {
    transformStaticValues(d,indexMaps.get(d));
  }
}
