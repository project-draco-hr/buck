def start_coverage(self):
    if (not self.options.collect_coverage):
        return
    self.cov = coverage.coverage(include=self.options.coverage_include.split(','), omit=self.options.coverage_omit.split(','))
    self.cov.erase()
    self.cov.start()
