{
  super(new FakeBuildRuleParamsBuilder(target).setDeclaredDeps(ImmutableSortedSet.copyOf(deps)).setProjectFilesystem(filesystem).build(),new SourcePathResolver(ruleResolver));
  this.coords=coords;
  this.pomTemplate=pomTemplate;
}
