{
switch (field) {
case BUCK_VERSION:
    if (value == null) {
      unsetBuckVersion();
    }
 else {
      setBuckVersion((String)value);
    }
  break;
case REPOSITORY:
if (value == null) {
  unsetRepository();
}
 else {
  setRepository((String)value);
}
break;
}
}
