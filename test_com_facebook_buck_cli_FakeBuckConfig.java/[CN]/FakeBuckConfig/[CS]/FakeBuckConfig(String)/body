{
  super(new Config(Inis.read(new StringReader(Joiner.on("\n").join(Arrays.asList(iniFileLines))))),new FakeProjectFilesystem(),Architecture.detect(),Platform.detect(),ImmutableMap.copyOf(System.getenv()));
}
