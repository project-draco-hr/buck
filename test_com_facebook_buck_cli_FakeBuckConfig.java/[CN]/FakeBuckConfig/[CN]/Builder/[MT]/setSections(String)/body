{
  try {
    sections=RawConfig.of(Inis.read(new StringReader(Joiner.on("\n").join(Arrays.asList(iniFileLines)))));
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return this;
}
