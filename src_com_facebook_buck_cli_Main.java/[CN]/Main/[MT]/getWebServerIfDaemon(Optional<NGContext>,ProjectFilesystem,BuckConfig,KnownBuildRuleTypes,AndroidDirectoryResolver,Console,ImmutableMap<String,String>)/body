{
  if (context.isPresent()) {
    Daemon daemon=getDaemon(projectFilesystem,config,knownBuildRuleTypes,androidDirectoryResolver,console,environment);
    return daemon.getWebServer();
  }
  return Optional.absent();
}
