{
  LOG.debug("Using watchman to watch for file changes.");
  return new WatchmanWatcher(projectFilesystem,fileEventBus,clock,objectMapper,repository.getBuckConfig().getIgnorePaths(),DEFAULT_IGNORE_GLOBS);
}
