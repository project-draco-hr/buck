{
  LogRecord record=new LogRecord(Level.INFO,"Build started at {0}");
  record.setParameters(new Object[]{formatTimestamp(started.getTimestamp())});
  record.setLoggerName(getClass().getName());
  LOG.log(record);
}
