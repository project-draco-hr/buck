{
  super(EnvironmentFilter.filteredEnvironment(ImmutableMap.copyOf(System.getenv()),Platform.detect()),Platform.detect(),new TestConsole(),root,ImmutableMap.<String,ImmutableMap<String,String>>of());
}
