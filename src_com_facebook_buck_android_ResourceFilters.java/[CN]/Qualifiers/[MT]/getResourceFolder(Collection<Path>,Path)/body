{
  Optional<Path> match=FluentIterable.from(candidates).firstMatch(new Predicate<Path>(){
    @Override public boolean apply(    Path resourceFolder){
      return resourceFile.startsWith(resourceFolder);
    }
  }
);
  Preconditions.checkState(match.isPresent());
  return match.get();
}
