{
  BuildTarget buildTarget=BuildTargetFactory.newInstance("//fake:rule");
  return new FakeBuildRule(buildTarget,new SourcePathResolver(new BuildRuleResolver()),ImmutableSortedSet.<BuildRule>of());
}
