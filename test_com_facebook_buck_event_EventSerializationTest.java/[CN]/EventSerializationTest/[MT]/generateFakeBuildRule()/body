{
  BuildTarget buildTarget=BuildTargetFactory.newInstance("//fake:rule");
  return new FakeBuildRule(buildTarget,new SourcePathResolver(new BuildRuleResolver(TargetGraph.EMPTY,new BuildTargetNodeToBuildRuleTransformer())),ImmutableSortedSet.<BuildRule>of());
}
