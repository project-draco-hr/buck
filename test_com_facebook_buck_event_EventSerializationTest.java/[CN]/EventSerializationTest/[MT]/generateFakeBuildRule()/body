{
  BuildTarget buildTarget=BuildTargetFactory.newInstance("//fake:rule");
  FakeBuildRule result=new FakeBuildRule(JavaLibraryDescription.TYPE,buildTarget,new SourcePathResolver(new BuildRuleResolver()),ImmutableSortedSet.<BuildRule>of(),ImmutableSet.<BuildTargetPattern>of());
  result.setRuleKey(new RuleKey("aaaa"));
  return result;
}
