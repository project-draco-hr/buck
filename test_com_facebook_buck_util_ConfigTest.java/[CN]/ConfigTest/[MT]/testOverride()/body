{
  Reader readerA=new StringReader(Joiner.on('\n').join("[cache]","    mode = dir,http"));
  Reader readerB=new StringReader(Joiner.on('\n').join("[cache]","    mode ="));
  new Config(Inis.read(readerA),Inis.read(readerB));
}
