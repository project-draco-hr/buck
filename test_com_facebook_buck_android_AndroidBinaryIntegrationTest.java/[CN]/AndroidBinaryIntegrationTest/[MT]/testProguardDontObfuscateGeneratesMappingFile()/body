{
  workspace.runBuckCommand("build","//apps/sample:app_proguard_dontobfuscate").assertSuccess();
  Path mapping=workspace.resolve("buck-out/gen/apps/sample/__app_proguard_dontobfuscate#aapt_package__proguard__/" + ".proguard/mapping.txt");
  assertTrue(Files.exists(mapping));
}
