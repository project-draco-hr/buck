{
  Reader reader=new StringReader("{" + "'FOO': 'BAR'," + "'MOL': 42,"+ "'WORKING': true"+ "}".replace('\'','\"'));
  Map<String,Object> map=RawParser.parseFromReader(reader);
  assertEquals(ImmutableMap.<String,Object>of("FOO","BAR","MOL",42L,"WORKING",true),map);
}
