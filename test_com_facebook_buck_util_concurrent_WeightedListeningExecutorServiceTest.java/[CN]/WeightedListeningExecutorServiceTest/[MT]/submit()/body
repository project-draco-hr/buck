{
  WeightedListeningExecutorService service=new WeightedListeningExecutorService(new ListeningSemaphore(1),1,newDirectExecutorService());
  AtomicBoolean first=submitSetBool(service,1);
  assertTrue(first.get());
}
