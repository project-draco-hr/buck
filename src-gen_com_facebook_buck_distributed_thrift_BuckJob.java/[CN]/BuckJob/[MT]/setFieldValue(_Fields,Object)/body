{
switch (field) {
case ID:
    if (value == null) {
      unsetId();
    }
 else {
      setId((String)value);
    }
  break;
case TIMESTAMP_MS:
if (value == null) {
  unsetTimestamp_ms();
}
 else {
  setTimestamp_ms((Long)value);
}
break;
}
}
