{
  try (PrintStream stdout=new CapturingPrintStream();PrintStream stderr=new CapturingPrintStream()){
    ProcessExecutor executor=new ProcessExecutor(new Console(Verbosity.SILENT,stdout,stderr,Ansi.withoutTty()));
    return build(executor,rootPath,env,new ExecutableFinder(),console);
  }
 }
