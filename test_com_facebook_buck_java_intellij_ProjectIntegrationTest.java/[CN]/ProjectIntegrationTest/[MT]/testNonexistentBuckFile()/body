{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"project1",temporaryFolder);
  workspace.setUp();
  expectedException.expect(HumanReadableException.class);
  expectedException.expectMessage("Target '//nonexistent/path:target' does not exist.");
  workspace.runBuckCommand("project","//nonexistent/path:target");
}
