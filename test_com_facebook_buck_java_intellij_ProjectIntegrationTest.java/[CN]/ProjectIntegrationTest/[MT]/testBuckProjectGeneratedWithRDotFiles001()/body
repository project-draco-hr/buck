{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"project_r_001",temporaryFolder);
  workspace.setUp();
  ProcessResult result=workspace.runBuckCommand("project","--enable-android-auto-generate-sources","app");
  result.assertSuccess();
  workspace.verify();
}
