{
  setupPaths(10);
  assertThat(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.INCLUDE,Optional.<Integer>absent(),Optional.of(0L)),empty());
}
