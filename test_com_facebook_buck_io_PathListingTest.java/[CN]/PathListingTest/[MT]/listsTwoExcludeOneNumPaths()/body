{
  setupPaths(0);
  assertEquals(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.GET_PATH_MODIFIED_TIME,PathListing.FilterMode.EXCLUDE,Optional.of(1),Optional.<Long>absent()),ImmutableSet.of(middle,oldest));
}
