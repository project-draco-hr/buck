{
  setupPaths(0);
  assertEquals(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.EXCLUDE,Optional.of(1),Optional.<Long>absent()),ImmutableSet.of(middle,oldest));
}
