{
  setupPaths(10);
  assertThat(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.EXCLUDE,Optional.<Integer>absent(),Optional.of(Long.MAX_VALUE)),empty());
}
