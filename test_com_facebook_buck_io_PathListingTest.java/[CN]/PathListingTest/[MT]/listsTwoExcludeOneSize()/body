{
  setupPaths(10);
  assertEquals(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.EXCLUDE,Optional.<Integer>absent(),Optional.of(10L)),ImmutableSet.of(middle,oldest));
}
