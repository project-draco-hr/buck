{
  setupPaths(0);
  assertEquals(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.INCLUDE,Optional.of(Integer.MAX_VALUE),Optional.<Long>absent()),ImmutableSet.of(newest,middle,oldest));
}
