{
  setupPaths(0);
  assertThat(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.GET_PATH_MODIFIED_TIME,PathListing.FilterMode.EXCLUDE,Optional.of(Integer.MAX_VALUE),Optional.<Long>absent()),empty());
}
