{
  setupPaths(10);
  assertEquals(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.EXCLUDE,Optional.<Integer>absent(),Optional.of(0L)),ImmutableSet.of(newest,middle,oldest));
}
