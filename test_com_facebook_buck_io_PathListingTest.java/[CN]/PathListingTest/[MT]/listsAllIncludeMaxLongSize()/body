{
  setupPaths(10);
  assertEquals(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.INCLUDE,Optional.<Integer>absent(),Optional.of(Long.MAX_VALUE)),ImmutableSet.of(newest,middle,oldest));
}
