{
  setupPaths(0);
  assertThat(PathListing.listMatchingPathsWithFilters(tmpDir.getRoot().toPath(),"*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.INCLUDE,Optional.of(0),Optional.<Long>absent()),empty());
}
