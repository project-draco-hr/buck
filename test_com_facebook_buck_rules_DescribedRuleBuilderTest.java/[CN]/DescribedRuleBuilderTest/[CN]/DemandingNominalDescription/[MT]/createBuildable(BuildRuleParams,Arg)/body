{
  return new FakeBuildableWithHasDepsOverride(params.getBuildTarget()){
    @Override public ImmutableSortedSet<BuildRule> getEnhancedDeps(    BuildRuleResolver ruleResolver,    Iterable<BuildRule> declaredDeps,    Iterable<BuildRule> inferredDeps){
      return ImmutableSortedSet.<BuildRule>naturalOrder().add(ruleResolver.get(new BuildTarget("//my","unrelated"))).build();
    }
  }
;
}
