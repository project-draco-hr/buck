{
  boolean ruleIsCached=false;
  try {
    if (context.isPresent() && rule.isCached(context.get())) {
      ruleIsCached=true;
    }
  }
 catch (  IOException e) {
    throw new RuntimeException("Invalid State:  By the time we're checking if rules were " + "cached in the android_binary rule we should have already successfully checked " + "cache for all it's depenencies.");
  }
  return ruleIsCached;
}
