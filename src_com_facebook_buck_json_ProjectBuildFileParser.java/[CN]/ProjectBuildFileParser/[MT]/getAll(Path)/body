{
  ImmutableList<Map<String,Object>> result=getAllRulesAndMetaRules(options.getProjectRoot().toAbsolutePath(),buildFile);
  return Collections.unmodifiableList(result.subList(0,result.size() - 3));
}
