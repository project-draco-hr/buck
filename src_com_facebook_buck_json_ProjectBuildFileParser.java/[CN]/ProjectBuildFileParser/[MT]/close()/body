{
  if (isClosed) {
    return;
  }
  try {
    if (isInitialized) {
      executor.shutdown();
      if (isServerMode) {
        buckPyStdinWriter.close();
      }
    }
  }
  finally {
    isClosed=true;
  }
}
