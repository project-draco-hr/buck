{
  ImmutableList.Builder<Command> commands=ImmutableList.builder();
  AndroidTransitiveDependencies transitiveDependencies=transitiveDependencyGraph.findDependencies(getAndroidResourceDepsInternal(context.getDependencyGraph()),Optional.of(context));
  if (skeletonFile.isPresent() && manifestFile.isPresent()) {
    commands.add(new GenerateManifestCommand(skeletonFile.get(),manifestFile.get(),transitiveDependencies.manifestFiles));
  }
  return commands.build();
}
