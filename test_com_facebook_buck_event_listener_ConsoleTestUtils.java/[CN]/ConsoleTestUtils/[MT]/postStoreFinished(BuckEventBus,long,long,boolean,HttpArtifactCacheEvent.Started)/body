{
  HttpArtifactCacheEvent.Finished storeFinished=HttpArtifactCacheEvent.newFinishedEventBuilder(storeStartedOne).setWasUploadSuccessful(success).build();
  eventBus.postWithoutConfiguring(configureTestEventAtTime(storeFinished,time,TimeUnit.MILLISECONDS,threadId));
}
