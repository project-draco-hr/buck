{
  SourcePathResolver resolver=new SourcePathResolver(new BuildRuleResolver());
  return new RuleKeyBuilder(resolver,fileHashCache,this){
    @Override public RuleKeyBuilder setReflectively(    String key,    @Nullable Object val){
      return this;
    }
    @Override public RuleKey build(){
      return ruleKeys.get(buildRule.getBuildTarget());
    }
  }
;
}
