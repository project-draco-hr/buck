{
  return ExternalTestRunnerTestSpec.builder().setTarget(getBuildTarget().toString()).setType("pyunit").addAllCommand(binary.getExecutableCommand().getCommandPrefix(getResolver())).putAllEnv(env.get()).addAllLabels(getLabels()).addAllContacts(getContacts()).build();
}
