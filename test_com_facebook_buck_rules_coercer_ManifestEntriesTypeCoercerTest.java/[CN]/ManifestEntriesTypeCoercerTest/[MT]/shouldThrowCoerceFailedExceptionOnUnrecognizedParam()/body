{
  ImmutableMap<String,Object> inputMap=ImmutableMap.<String,Object>builder().put("bad_param_name",3).build();
  manifestEntriesTypeCoercer.coerce(createCellRoots(filesystem),filesystem,basePath,inputMap);
}
