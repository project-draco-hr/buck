def test_parser(self):
    parser = argparse.ArgumentParser(prog='PROG')
    string = ("ArgumentParser(prog='PROG', usage=None, description=None, version=None, formatter_class=%r, conflict_handler='error', add_help=True)" % argparse.HelpFormatter)
    self.assertStringEqual(parser, string)
