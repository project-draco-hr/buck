{
  Path parent=basePath.getParent();
  while (parent != null && basePath.getNameCount() > minimumPathDepth && !aggregationHaltPoints.contains(basePath)) {
    basePath=parent;
    parent=basePath.getParent();
  }
  return basePath;
}
