{
  BuckConfig buckConfig=FakeBuckConfig.builder().build();
  RageConfig config=RageBuckConfig.create(buckConfig);
  assertThat(config.getReportUploadUri(),Matchers.equalTo(Optional.<URI>absent()));
}
