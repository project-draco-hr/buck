{
  return ndkRoot.resolve("toolchains").resolve(String.format("%s-%s",targetConfiguration.getCompiler().getType() == NdkCxxPlatformCompiler.Type.CLANG ? "llvm" : targetConfiguration.getToolchain(),targetConfiguration.getCompiler().getVersion())).resolve("prebuilt").resolve(hostName);
}
