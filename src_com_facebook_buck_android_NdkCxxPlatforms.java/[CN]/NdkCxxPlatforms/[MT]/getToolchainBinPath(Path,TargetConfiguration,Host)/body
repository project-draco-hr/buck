{
  return getNdkToolRoot(ndkRoot,targetConfiguration,host.toString()).resolve(targetConfiguration.getCompiler().getType() == Compiler.Type.GCC ? targetConfiguration.getToolchainTarget().toString() : "").resolve("bin");
}
