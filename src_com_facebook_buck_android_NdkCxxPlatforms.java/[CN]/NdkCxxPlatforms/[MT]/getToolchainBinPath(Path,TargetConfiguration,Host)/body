{
  return getNdkToolRoot(ndkRoot,targetConfiguration,host).resolve(targetConfiguration.getCompiler().getType() == Compiler.Type.GCC ? targetConfiguration.getToolchainTarget().toString() : "").resolve("bin");
}
