{
  return ndkRoot.resolve("toolchains").resolve(String.format("%s-%s",targetConfiguration.getCompiler().getType() == Compiler.Type.CLANG ? "llvm" : targetConfiguration.getToolchain(),targetConfiguration.getCompiler().getVersion())).resolve("prebuilt").resolve(host.toString());
}
