{
  return ndkRoot.resolve("toolchains").resolve(String.format("%s-%s",targetConfiguration.getToolchain(),targetConfiguration.getCompiler().getGccVersion())).resolve("prebuilt").resolve(host.toString());
}
