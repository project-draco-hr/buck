{
  return getNdkToolRoot(ndkRoot,targetConfiguration,host).resolve("libexec").resolve("gcc").resolve(targetConfiguration.getToolchain().toString()).resolve(targetConfiguration.getCompiler().getVersion());
}
