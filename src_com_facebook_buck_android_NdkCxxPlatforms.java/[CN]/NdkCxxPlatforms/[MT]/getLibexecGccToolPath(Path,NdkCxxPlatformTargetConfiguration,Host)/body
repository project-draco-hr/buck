{
  return getNdkToolRoot(ndkRoot,targetConfiguration,host.toString()).resolve("libexec").resolve("gcc").resolve(targetConfiguration.getToolchain().toString()).resolve(targetConfiguration.getCompiler().getVersion());
}
