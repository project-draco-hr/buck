{
  Optional<Path> path=executableFinder.getOptionalExecutable(getNdkToolRoot(ndkRoot,targetConfiguration,host).resolve(targetConfiguration.toolchainPrefix.toString()).resolve("bin").resolve(tool),ImmutableMap.<String,String>of());
  Preconditions.checkState(path.isPresent());
  return path.get();
}
