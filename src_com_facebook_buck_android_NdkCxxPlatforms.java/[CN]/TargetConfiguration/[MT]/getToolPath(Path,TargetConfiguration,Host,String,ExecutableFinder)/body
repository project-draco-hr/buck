{
  Optional<Path> path=executableFinder.getOptionalExecutable(getToolchainBinPath(ndkRoot,targetConfiguration,host).resolve(tool),ImmutableMap.<String,String>of());
  Preconditions.checkState(path.isPresent());
  return path.get();
}
