{
  BuildableProperties buildableProperties=new BuildableProperties(LIBRARY,ANDROID);
  assertTrue(buildableProperties.is(LIBRARY));
  assertTrue(buildableProperties.is(ANDROID));
  assertFalse(buildableProperties.is(TEST));
}
