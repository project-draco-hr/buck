{
  BuildRule rule=new DescribedRule(AndroidResourceDescription.TYPE,resource,new FakeBuildRuleParamsBuilder(buildTarget).setFileHashCache(fileHashCache).build());
  return rule.getRuleKeyWithoutDeps();
}
