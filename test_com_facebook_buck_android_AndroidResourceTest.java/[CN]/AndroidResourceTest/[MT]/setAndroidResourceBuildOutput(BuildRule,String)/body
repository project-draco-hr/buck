{
  if (resourceRule.getBuildable() instanceof AndroidResource) {
    ((AndroidResource)resourceRule.getBuildable()).getBuildOutputInitializer().setBuildOutput(new BuildOutput(new Sha1HashCode(Strings.repeat(hashChar,40))));
  }
}
