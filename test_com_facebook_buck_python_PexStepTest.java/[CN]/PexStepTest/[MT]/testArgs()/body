{
  PexStep step=new PexStep(PEXPY_PATH,ImmutableList.of("--some","--args"),PYTHON_PATH,TEMP_PATH,DEST_PATH,ENTRY_POINT,MODULES,RESOURCES,NATIVE_LIBRARIES,PREBUILT_LIBRARIES,true);
  assertThat(step.getShellCommandInternal(TestExecutionContext.newInstance()),hasItems("--some","--args"));
}
