{
  ProjectFilesystem filesystem=new FakeProjectFilesystem();
  publisher=new Publisher(filesystem,Optional.<URL>absent(),true);
  ruleResolver=new BuildRuleResolver(TargetGraph.EMPTY,new BuildTargetNodeToBuildRuleTransformer());
}
