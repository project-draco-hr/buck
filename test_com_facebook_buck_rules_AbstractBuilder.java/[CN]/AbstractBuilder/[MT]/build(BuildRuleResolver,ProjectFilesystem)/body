{
  BuildRuleParams params=createBuildRuleParams(filesystem);
  DescribedRule rule=new DescribedRule(description.getBuildRuleType(),build(),params);
  resolver.addToIndex(target,rule);
  return rule;
}
