{
  BuildRuleParams params=createBuildRuleParams(filesystem);
  DescribedRule rule=new DescribedRule(description.getBuildRuleType(),build(filesystem),params);
  resolver.addToIndex(target,rule);
  return rule;
}
