{
  super(params,resolver);
  Preconditions.checkArgument(dllName.endsWith(".dll"));
  this.srcs=srcs;
  this.refs=refs;
  this.resources=resources;
  this.version=version;
  this.output=BuildTargets.getGenPath(getProjectFilesystem(),params.getBuildTarget(),String.format("%%s/%s",dllName));
}
