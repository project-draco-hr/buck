{
  BuildTarget target=createSymlinkTreeTarget(sourceParams.getBuildTarget());
  GoSymlinkTree tree=new GoSymlinkTree(sourceParams.copyWithBuildTarget(target),new SourcePathResolver(resolver));
  return tree;
}
