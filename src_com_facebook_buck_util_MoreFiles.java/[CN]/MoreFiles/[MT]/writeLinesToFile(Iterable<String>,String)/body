{
  BufferedWriter writer=null;
  try {
    writer=new BufferedWriter(new OutputStreamWriter(new FileOutputStream(pathToFile),Charsets.UTF_8));
    for (    String line : lines) {
      writer.write(line);
      writer.newLine();
    }
  }
  finally {
    Closeables.close(writer,false);
  }
}
