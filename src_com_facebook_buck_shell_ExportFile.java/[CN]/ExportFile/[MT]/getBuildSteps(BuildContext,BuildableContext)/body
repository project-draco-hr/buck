{
  ImmutableList.Builder<Step> builder=ImmutableList.<Step>builder().add(new MkdirStep(out.getParent())).add(CopyStep.forFile(src.resolve(context),out));
  buildableContext.recordArtifact(out);
  return builder.build();
}
