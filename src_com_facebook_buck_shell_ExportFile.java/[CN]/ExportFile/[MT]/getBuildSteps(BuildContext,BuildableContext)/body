{
  ImmutableList.Builder<Step> builder=ImmutableList.<Step>builder().add(new MkdirStep(getProjectFilesystem(),out.getParent())).add(CopyStep.forFile(getProjectFilesystem(),getResolver().getResolvedPath(src),out));
  buildableContext.recordArtifact(out);
  return builder.build();
}
