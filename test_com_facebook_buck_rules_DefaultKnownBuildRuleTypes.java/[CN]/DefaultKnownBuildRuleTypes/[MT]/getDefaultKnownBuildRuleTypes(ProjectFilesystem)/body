{
  BuckConfig config=FakeBuckConfig.builder().setFilesystem(filesystem).build();
  return KnownBuildRuleTypes.createInstance(config,new FakeProcessExecutor(ImmutableMap.<ProcessExecutorParams,FakeProcess>builder().put(XCODE_SELECT_PARAMS,XCODE_SELECT_PROCESS).putAll(getPythonProcessMap()).build()),new FakeAndroidDirectoryResolver(),Optional.<Path>absent());
}
