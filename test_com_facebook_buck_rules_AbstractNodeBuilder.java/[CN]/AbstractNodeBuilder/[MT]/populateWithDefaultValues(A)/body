{
  try {
    new ConstructorArgMarshaller(new DefaultTypeCoercerFactory()).populateDefaults(cellRoots,new FakeProjectFilesystem(),factoryParams,arg);
  }
 catch (  ConstructorArgMarshalException error) {
    throw Throwables.propagate(error);
  }
}
