{
  try {
    new ConstructorArgMarshaller(TYPE_COERCER_FACTORY).populateDefaults(cellRoots,factoryParams.getProjectFilesystem(),factoryParams,arg);
  }
 catch (  ConstructorArgMarshalException error) {
    throw Throwables.propagate(error);
  }
}
