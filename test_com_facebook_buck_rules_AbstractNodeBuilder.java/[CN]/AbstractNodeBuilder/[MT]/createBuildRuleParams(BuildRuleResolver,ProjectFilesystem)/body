{
  TargetNode<?> node=build();
  return new FakeBuildRuleParamsBuilder(target).setProjectFilesystem(filesystem).setDeps(resolver.getAllRules(node.getDeps())).setExtraDeps(resolver.getAllRules(node.getExtraDeps())).build();
}
