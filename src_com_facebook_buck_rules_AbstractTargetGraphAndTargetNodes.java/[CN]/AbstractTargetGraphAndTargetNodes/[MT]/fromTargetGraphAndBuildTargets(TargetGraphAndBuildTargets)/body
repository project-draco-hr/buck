{
  TargetGraph targetGraph=targetGraphAndBuildTargets.getTargetGraph();
  return TargetGraphAndTargetNodes.builder().setTargetGraph(targetGraph).setTargetNodes(targetGraph.getAll(targetGraphAndBuildTargets.getBuildTargets())).build();
}
