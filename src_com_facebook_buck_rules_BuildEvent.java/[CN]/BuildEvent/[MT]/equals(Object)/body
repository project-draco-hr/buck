{
  if (!(o instanceof BuildEvent)) {
    return false;
  }
  BuildEvent that=(BuildEvent)o;
  return Objects.equal(getClass(),o.getClass()) && Objects.equal(getBuildTargets(),that.getBuildTargets());
}
