{
  if (!(event instanceof BuildEvent)) {
    return false;
  }
  BuildEvent that=(BuildEvent)event;
  return Objects.equal(getClass(),event.getClass()) && Objects.equal(getBuildTargets(),that.getBuildTargets());
}
