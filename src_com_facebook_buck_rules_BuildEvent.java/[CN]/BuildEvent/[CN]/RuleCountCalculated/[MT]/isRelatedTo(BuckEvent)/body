{
  if (!(event instanceof RuleCountCalculated)) {
    return false;
  }
  RuleCountCalculated that=(RuleCountCalculated)event;
  return Objects.equal(buildRules,that.buildRules) && Objects.equal(numRules,that.numRules);
}
