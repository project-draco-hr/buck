{
  return new ProjectConfig(params,args.srcTarget.transform(resolver.getRuleFunction()).orNull(),args.srcRoots.orNull(),args.testTarget.transform(resolver.getRuleFunction()).orNull(),args.testRoots.orNull(),args.isIntellijPlugin.or(false));
}
