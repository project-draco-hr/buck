{
  buildableContext.recordArtifact(report);
  return ImmutableList.<Step>builder().add(new MkdirStep(getProjectFilesystem(),resultsDir)).add(new InferMergeReportsStep(getProjectFilesystem(),reportsToMerge,report)).build();
}
