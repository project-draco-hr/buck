{
  assertEquals(first.length,second.size());
  int i=0;
  for (  String s : second) {
    assertEquals(String.format("%s failed: Elements at %d are not equal",testName,i),first[i],s);
    i++;
  }
}
