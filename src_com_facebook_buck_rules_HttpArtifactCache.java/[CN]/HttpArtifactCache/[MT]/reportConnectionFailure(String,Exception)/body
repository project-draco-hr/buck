{
  if (numConnectionExceptionReports.getAndIncrement() < MAX_CONNECTION_FAILURE_REPORTS) {
    buckEventBus.post(ThrowableConsoleEvent.create(exception,"%s: Connection failed: %s",context,exception.getMessage()));
  }
}
