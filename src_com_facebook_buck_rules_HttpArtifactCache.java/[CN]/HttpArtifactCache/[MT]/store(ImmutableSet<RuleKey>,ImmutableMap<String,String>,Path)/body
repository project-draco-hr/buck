{
  if (!isStoreSupported()) {
    return;
  }
  try {
    storeImpl(ruleKeys,metadata,output);
  }
 catch (  IOException e) {
    reportFailure(e,"store(%s, %s): %s: %s",url,ruleKeys,e.getClass().getName(),e.getMessage());
  }
}
