{
  if (!isStoreSupported()) {
    return;
  }
  try {
    storeImpl(ruleKey,output);
  }
 catch (  IOException e) {
    reportFailure(e,"store(%s, %s): %s: %s",url,ruleKey,e.getClass().getName(),e.getMessage());
  }
}
