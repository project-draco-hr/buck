{
  try {
    return fetchImpl(ruleKey,output);
  }
 catch (  IOException e) {
    reportFailure(e,"fetch(%s, %s): %s: %s",url,ruleKey,e.getClass().getName(),e.getMessage());
    return CacheResult.MISS;
  }
}
