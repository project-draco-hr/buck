{
  if (numConnectionExceptionReports.incrementAndGet() < MAX_CONNECTION_FAILURE_REPORTS) {
    buckEventBus.post(ThrowableLogEvent.create(exception,"%s Connecting to cassandra failed: %s.",context,exception.getMessage()));
  }
}
