{
  return MoreObjects.toStringHelper(this).add("rules",rules).add("preprocessorFlags",preprocessorFlags).add("includes",includes).add("includeRoots",includeRoots).add("systemIncludeRoots",systemIncludeRoots).add("frameworkRoots",frameworkRoots).toString();
}
