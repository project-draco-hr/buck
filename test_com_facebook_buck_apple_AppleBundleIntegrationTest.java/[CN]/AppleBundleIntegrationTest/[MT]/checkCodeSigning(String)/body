{
  Path absoluteBundlePath=tmp.getRootPath().resolve(BuckConstant.GEN_DIR).resolve(Paths.get(relativeBundlePath));
  return CodeSigning.hasValidSignature(new ProcessExecutor(new TestConsole()),absoluteBundlePath);
}
