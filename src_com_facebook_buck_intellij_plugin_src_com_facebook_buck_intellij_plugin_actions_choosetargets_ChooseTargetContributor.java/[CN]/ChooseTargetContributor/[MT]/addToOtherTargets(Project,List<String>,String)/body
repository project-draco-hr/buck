{
  otherTargets.put(target,targets);
  ApplicationManager.getApplication().invokeLater(new Runnable(){
    public void run(){
      project.getUserData(ChooseByNamePopup.CHOOSE_BY_NAME_POPUP_IN_PROJECT_KEY).rebuildList(true);
    }
  }
);
}
