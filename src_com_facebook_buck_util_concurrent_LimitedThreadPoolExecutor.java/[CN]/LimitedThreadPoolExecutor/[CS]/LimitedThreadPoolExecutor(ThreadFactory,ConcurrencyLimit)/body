{
  super(concurrencyLimit.threadLimit,concurrencyLimit.threadLimit,0L,TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>(),threadFactory,new ThreadPoolExecutor.DiscardPolicy());
  loadLimit=concurrencyLimit.loadLimit;
}
