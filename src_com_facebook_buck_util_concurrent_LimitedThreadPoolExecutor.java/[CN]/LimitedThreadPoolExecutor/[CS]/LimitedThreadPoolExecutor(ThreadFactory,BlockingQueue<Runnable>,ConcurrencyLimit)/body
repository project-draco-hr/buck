{
  super(concurrencyLimit.threadLimit,concurrencyLimit.threadLimit,0L,TimeUnit.MILLISECONDS,workQueue,threadFactory,new ThreadPoolExecutor.DiscardPolicy());
  loadLimit=concurrencyLimit.loadLimit;
}
