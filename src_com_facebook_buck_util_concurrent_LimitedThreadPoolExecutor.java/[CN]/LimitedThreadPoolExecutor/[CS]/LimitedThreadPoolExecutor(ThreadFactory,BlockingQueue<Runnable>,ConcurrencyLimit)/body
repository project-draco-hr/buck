{
  super(concurrencyLimit.managedThreadCount,concurrencyLimit.managedThreadCount,0L,TimeUnit.MILLISECONDS,workQueue,threadFactory,new ThreadPoolExecutor.DiscardPolicy());
  loadLimit=concurrencyLimit.loadLimit;
}
