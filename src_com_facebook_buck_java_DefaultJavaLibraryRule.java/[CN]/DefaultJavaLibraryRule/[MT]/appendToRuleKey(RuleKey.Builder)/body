{
  super.appendToRuleKey(builder).set("exportedDeps",exportedDeps).set("srcs",srcs).setSourcePaths("resources",resources).set("proguard",proguardConfig);
  javacOptions.appendToRuleKey(builder);
  return builder;
}
