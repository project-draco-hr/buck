{
  AbstractBuildable.AnonymousBuildRule rule=new AbstractBuildable.AnonymousBuildRule(new BuildRuleType("example"),new FakeBuildable().setPathToOutputFile(Paths.get("cheese")),new FakeBuildRuleParams(target));
  BuildRuleSourcePath path=new BuildRuleSourcePath(rule);
  Path resolved=path.resolve();
  assertEquals(Paths.get("cheese"),resolved);
}
