{
  String[] parts=id.split(":");
  BuildRule rule=graph.getActionGraph().findBuildRuleByTarget(new BuildTarget(parts[0],parts[1]));
  Preconditions.checkNotNull(rule,"No rule for %s",id);
  return rule;
}
