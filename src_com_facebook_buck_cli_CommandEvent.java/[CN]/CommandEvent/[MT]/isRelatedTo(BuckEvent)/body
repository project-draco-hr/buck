{
  if (!(event instanceof CommandEvent)) {
    return false;
  }
  CommandEvent that=(CommandEvent)event;
  return Objects.equal(getCommandName(),that.getCommandName()) && Objects.equal(getArgs(),that.getArgs()) && Objects.equal(isDaemon(),that.isDaemon());
}
