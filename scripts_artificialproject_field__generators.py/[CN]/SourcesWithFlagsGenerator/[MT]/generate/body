def generate(self):
    source_paths = self._source_path_set_generator.generate()
    output = [self._generate_source_with_flags(sp) for sp in source_paths.value]
    return GeneratedField(output, source_paths.deps)
