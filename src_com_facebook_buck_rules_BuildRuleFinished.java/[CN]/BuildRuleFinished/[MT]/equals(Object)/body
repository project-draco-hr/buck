{
  if (!(o instanceof BuildRuleFinished)) {
    return false;
  }
  BuildRuleFinished finished=(BuildRuleFinished)o;
  return Objects.equal(getBuildRule(),finished.getBuildRule()) && Objects.equal(getStatus(),finished.getStatus()) && Objects.equal(getCacheResult(),finished.getCacheResult());
}
