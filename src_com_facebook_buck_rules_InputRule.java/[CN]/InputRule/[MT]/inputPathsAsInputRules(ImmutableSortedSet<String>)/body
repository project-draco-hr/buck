{
  Comparator<InputRule> comparator=new Comparator<InputRule>(){
    @Override public int compare(    InputRule o1,    InputRule o2){
      return o1.compareTo(o2);
    }
  }
;
  ImmutableSortedSet.Builder<InputRule> builder=new ImmutableSortedSet.Builder<InputRule>(comparator);
  for (  String path : paths) {
    builder.add(new InputRule(path));
  }
  return builder.build();
}
