{
  inputFile=Preconditions.checkNotNull(input);
  buildTarget=new BuildTarget(input);
  BuildRuleSuccess buildRuleSuccess=new BuildRuleSuccess(this,false);
  this.buildOutput=Futures.immediateFuture(buildRuleSuccess);
}
