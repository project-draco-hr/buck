{
  super(cachingBuildRuleParams);
  this.srcs=ImmutableSortedSet.copyOf(srcs);
  this.outputDirectory=String.format("%s/%s/__%s__",BuckConstant.GEN_DIR,cachingBuildRuleParams.getBuildTarget().getBasePath(),cachingBuildRuleParams.getBuildTarget().getShortName());
}
