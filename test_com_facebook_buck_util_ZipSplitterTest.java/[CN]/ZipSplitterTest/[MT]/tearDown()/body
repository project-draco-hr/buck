{
  if (privateDir != null) {
    ProcessExecutor processExecutor=new ProcessExecutor(new Console(System.out,System.err,Ansi.withoutTty()));
    MoreFiles.rmdir(privateDir.getAbsolutePath(),processExecutor);
  }
}
