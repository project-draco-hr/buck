{
  Path dir1=tmp.newFolder("foo").toPath();
  Path dir2=tmp.newFolder("bar").toPath();
  Path dir3=tmp.newFolder("baz").toPath();
  assertEquals(Optional.<Path>absent(),MoreFiles.searchPathsForExecutable(Paths.get("blech"),ImmutableList.of(dir1,dir2,dir3)));
}
