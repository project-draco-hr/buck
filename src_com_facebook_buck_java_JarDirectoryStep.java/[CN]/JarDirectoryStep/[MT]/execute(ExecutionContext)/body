{
  try {
    return JarDirectoryStepHelper.createJarFile(filesystem,pathToOutputFile,entriesToJar,mainClass,manifestFile,mergeManifests,blacklist,context);
  }
 catch (  IOException e) {
    e.printStackTrace(context.getStdErr());
    return 1;
  }
}
