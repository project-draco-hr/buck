{
  try {
    JarDirectoryStepHelper.createJarFile(pathToOutputFile,entriesToJar,mainClass,manifestFile,mergeManifests,blacklist,context);
  }
 catch (  IOException e) {
    e.printStackTrace(context.getStdErr());
    return 1;
  }
  return 0;
}
