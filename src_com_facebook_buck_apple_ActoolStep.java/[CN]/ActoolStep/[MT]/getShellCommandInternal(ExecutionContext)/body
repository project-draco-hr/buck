{
  ImmutableList.Builder<String> commandBuilder=ImmutableList.builder();
  String target="7.0";
  if (bundlingMode == BundlingMode.SEPARATE_BUNDLES) {
    target="6.0";
  }
  commandBuilder.addAll(actoolCommand);
  commandBuilder.add("--output-format","human-readable-text","--notices","--warnings","--errors","--platform",applePlatformName,"--minimum-deployment-target",target,"--target-device","iphone","--compress-pngs","--compile",output.toString());
  commandBuilder.addAll(Iterables.transform(assetCatalogDirs,Functions.toStringFunction()));
  return commandBuilder.build();
}
