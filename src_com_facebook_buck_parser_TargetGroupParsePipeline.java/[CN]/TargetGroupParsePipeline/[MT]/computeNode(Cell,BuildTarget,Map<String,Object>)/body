{
  try (SimplePerfEvent.Scope scope=Parser.getTargetGroupEventScope(eventBus,buildTarget)){
    return delegate.createTargetNode(cell,cell.getAbsolutePathToBuildFile(buildTarget),buildTarget,rawNode);
  }
 }
