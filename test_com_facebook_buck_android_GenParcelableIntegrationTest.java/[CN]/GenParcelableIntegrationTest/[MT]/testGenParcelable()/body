{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"gen_parcelable_test",tmp);
  workspace.setUp();
  ProcessResult result=workspace.runBuckCommand("build","//:student");
  result.assertSuccess("Failed to build example gen_parcelable.");
}
