{
  BuckConfig config=createFromText("[cache]","dir = ~/cache_dir");
  assertEquals("User home cache directory must be expanded.",MorePaths.expandHomeDir(Paths.get("~/cache_dir")),config.getCacheDir());
}
