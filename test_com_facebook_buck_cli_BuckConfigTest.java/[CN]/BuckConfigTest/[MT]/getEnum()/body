{
  FakeBuckConfig config=new FakeBuckConfig(ImmutableMap.<String,Map<String,String>>of("section",ImmutableMap.of("field","A")));
  Optional<TestEnum> value=config.getEnum("section","field",TestEnum.class);
  assertEquals(Optional.of(TestEnum.A),value);
}
