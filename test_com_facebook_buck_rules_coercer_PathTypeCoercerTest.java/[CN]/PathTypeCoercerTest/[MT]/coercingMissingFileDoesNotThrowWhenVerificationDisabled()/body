{
  String missingPath="hello";
  new PathTypeCoercer(PathTypeCoercer.PathExistenceVerificationMode.DO_NOT_VERIFY).coerce(createCellRoots(filesystem),filesystem,pathRelativeToProjectRoot,missingPath);
}
