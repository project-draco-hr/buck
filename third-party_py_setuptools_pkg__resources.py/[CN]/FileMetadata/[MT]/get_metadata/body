def get_metadata(self, name):
    if (name == 'PKG-INFO'):
        f = open(self.path, 'rU')
        metadata = f.read()
        f.close()
        return metadata
    raise KeyError('No metadata except PKG-INFO is available')
