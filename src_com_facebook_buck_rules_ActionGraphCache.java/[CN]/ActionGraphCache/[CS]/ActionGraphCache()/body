{
  this(new ThreadPoolExecutor(0,1,0L,TimeUnit.MILLISECONDS,new LinkedBlockingQueue<Runnable>(),new MostExecutors.NamedAndPriorityThreadFactory("ActionGraphCache-RuleCheck",Thread.MIN_PRIORITY),new ThreadPoolExecutor.DiscardPolicy()));
}
