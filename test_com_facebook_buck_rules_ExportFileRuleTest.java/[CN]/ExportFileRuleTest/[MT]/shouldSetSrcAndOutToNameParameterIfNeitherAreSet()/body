{
  ExportFileRule rule=new ExportFileRule(params,Optional.<String>absent(),Optional.<String>absent());
  List<Command> commands=rule.buildInternal(context);
  MkdirAndSymlinkFileCommand expected=new MkdirAndSymlinkFileCommand(new File(root,"example.html"),new File(BuckConstant.GEN_DIR,"example.html"));
  assertEquals(ImmutableList.of(expected),commands);
}
