{
  ExportFileRule rule=new ExportFileRule(params,Optional.<String>absent(),Optional.<String>absent());
  List<Step> steps=rule.buildInternal(context);
  MkdirAndSymlinkFileStep expected=new MkdirAndSymlinkFileStep(new File(root,"example.html"),new File(BuckConstant.GEN_DIR,"example.html"));
  assertEquals(ImmutableList.of(expected),steps);
}
