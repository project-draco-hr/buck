{
  BuildTarget target=BuildTargetFactory.newInstance("//:example.html");
  params=new BuildRuleParams(target,ImmutableSortedSet.<BuildRule>of(),ImmutableSet.<BuildTargetPattern>of());
  root=new File(".");
  context=getBuildContext(root);
}
