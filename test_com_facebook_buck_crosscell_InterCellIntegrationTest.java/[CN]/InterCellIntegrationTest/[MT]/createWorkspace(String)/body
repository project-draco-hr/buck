{
  final Path tmpSubfolder=tmp.newFolder();
  TemporaryRoot temporaryRoot=new TemporaryRoot(){
    @Override public Path getRootPath(){
      return tmpSubfolder;
    }
  }
;
  ProjectWorkspace projectWorkspace=TestDataHelper.createProjectWorkspaceForScenario(this,scenarioName,temporaryRoot);
  projectWorkspace.setUp();
  return projectWorkspace;
}
