{
  ProjectWorkspace mainRepo=TestDataHelper.createProjectWorkspaceForScenario(this,"inter-cell/circular",tmp);
  mainRepo.setUp();
  Path primary=mainRepo.getPath("primary");
  ProjectWorkspace.ProcessResult result=mainRepo.runBuckCommandWithEnvironmentAndContext(primary,Optional.<NGContext>absent(),Optional.<ImmutableMap<String,String>>absent(),"build","//:bin");
  result.assertSuccess();
}
