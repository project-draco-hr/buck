{
  SymCopyStep symCopyStep=new SymCopyStep(ImmutableList.<Path>builder().add(Paths.get("here")).build(),Paths.get("there"));
  assertEquals("Symlink-Copy step",symCopyStep.getDescription(context));
}
