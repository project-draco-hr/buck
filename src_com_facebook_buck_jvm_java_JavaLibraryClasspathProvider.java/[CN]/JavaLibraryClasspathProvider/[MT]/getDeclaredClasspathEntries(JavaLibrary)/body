{
  final ImmutableSetMultimap.Builder<JavaLibrary,Path> classpathEntries=ImmutableSetMultimap.builder();
  Iterable<JavaLibrary> javaLibraryDeps=getJavaLibraryDeps(javaLibraryRule.getDepsForTransitiveClasspathEntries());
  for (  JavaLibrary rule : javaLibraryDeps) {
    for (    Path path : rule.getOutputClasspaths()) {
      classpathEntries.put(rule,rule.getProjectFilesystem().resolve(path));
    }
  }
  return classpathEntries.build();
}
