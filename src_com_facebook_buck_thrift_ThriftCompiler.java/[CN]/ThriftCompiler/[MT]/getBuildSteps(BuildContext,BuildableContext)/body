{
  buildableContext.recordArtifactsInDirectory(outputDir);
  return ImmutableList.of(new MakeCleanDirectoryStep(outputDir),new ThriftCompilerStep(getResolver().getPath(compiler),flags,outputDir,getResolver().getPath(input),language,options,includeRoots));
}
