{
  buildableContext.recordArtifactsInDirectory(outputDir);
  return ImmutableList.of(new MakeCleanDirectoryStep(outputDir),new ThriftCompilerStep(compiler.resolve(),flags,outputDir,input.resolve(),language,options,includeRoots));
}
