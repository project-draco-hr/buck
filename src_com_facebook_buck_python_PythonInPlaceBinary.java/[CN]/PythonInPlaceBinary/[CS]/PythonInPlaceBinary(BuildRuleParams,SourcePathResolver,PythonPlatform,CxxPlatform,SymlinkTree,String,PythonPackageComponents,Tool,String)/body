{
  super(params,resolver,pythonPlatform,mainModule,components,pexExtension);
  this.script=getScript(pythonPlatform,cxxPlatform,mainModule,components,getBinPath().getParent().relativize(linkTree.getRoot()));
  this.linkTree=linkTree;
  this.components=components;
  this.python=python;
}
