{
  String pythonPath=Joiner.on(':').join(Iterables.transform(getPythonPathEntries(),Functions.RELATIVE_TO_ABSOLUTE_PATH));
  return String.format("PYTHONPATH=%s python %s",pythonPath,Functions.RELATIVE_TO_ABSOLUTE_PATH.apply(main));
}
