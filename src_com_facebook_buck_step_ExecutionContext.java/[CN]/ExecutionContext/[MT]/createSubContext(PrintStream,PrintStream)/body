{
  Console console=new Console(this.getConsole().getVerbosity(),newStdout,newStderr,this.getConsole().getAnsi());
  return ImmutableExecutionContext.copyOf(this).withConsole(console).withProcessExecutor(new ProcessExecutor(console)).withClassLoaderCache(getClassLoaderCache().addRef());
}
