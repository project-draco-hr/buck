{
  AnnotationProcessingData empty=AnnotationProcessingData.EMPTY;
  assertTrue(empty.isEmpty());
  assertTrue(empty.getSearchPathElements().isEmpty());
  assertTrue(empty.getNames().isEmpty());
  assertTrue(empty.getParameters().isEmpty());
  assertEquals(null,empty.getGeneratedSourceFolderName());
  assertFalse(empty.getProcessOnly());
}
