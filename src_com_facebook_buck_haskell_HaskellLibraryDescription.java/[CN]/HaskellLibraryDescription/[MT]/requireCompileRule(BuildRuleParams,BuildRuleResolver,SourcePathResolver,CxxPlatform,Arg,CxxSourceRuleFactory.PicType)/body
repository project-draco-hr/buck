{
  return HaskellDescriptionUtils.requireCompileRule(params,resolver,pathResolver,cxxPlatform,haskellConfig,picType,Optional.<String>absent(),args.compilerFlags.or(ImmutableList.<String>of()),HaskellSources.from(params.getBuildTarget(),pathResolver,"srcs",args.srcs.or(SourceList.EMPTY)));
}
