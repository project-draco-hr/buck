{
  if (!isEmpty()) {
    String owner=(ownerTarget == null) ? null : ownerTarget.getFullyQualifiedName();
    builder.setReflectively(key + ".owner",owner).setReflectively(key + ".names",names).setReflectively(key + ".parameters",parameters).setReflectively(key + ".processOnly",processOnly).setReflectively(key + ".inputs",inputs);
  }
  return builder;
}
