{
  InstallCommandOptions options=getOptions(InstallCommandOptions.RUN_SHORT_ARG,"katana",InstallCommandOptions.VERBOSE_SHORT_ARG,"10");
  assertTrue(options.shouldStartActivity());
  assertNull(options.getActivityToStart());
}
