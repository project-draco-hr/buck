{
  ProjectFilesystem filesystem=new ProjectFilesystem(folder.getRoot());
  params=new BuildRuleFactoryParams(Maps.<String,Object>newHashMap(),filesystem,new FilesystemBackedBuildFileTree(filesystem),new BuildTargetParser(filesystem),BuildTargetFactory.newInstance("//:foo"),new FakeRuleKeyBuilderFactory(),true);
}
