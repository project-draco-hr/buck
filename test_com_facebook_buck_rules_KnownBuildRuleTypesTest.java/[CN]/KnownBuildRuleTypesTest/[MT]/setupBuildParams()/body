{
  ProjectFilesystem filesystem=new ProjectFilesystem(folder.getRoot());
  params=new BuildRuleFactoryParams(new HashMap<String,Object>(),filesystem,BuildFileTree.constructBuildFileTree(filesystem),new BuildTargetParser(filesystem),BuildTargetFactory.newInstance("//:foo"),new FakeRuleKeyBuilderFactory(),true);
}
