def _is_dirty(self):
    if self._is_buck_repo_dirty_override:
        return (self._is_buck_repo_dirty_override == '1')
    if (not self._is_git):
        return False
    output = check_output(['git', 'status', '--porcelain'], cwd=self._buck_dir)
    return bool(output.strip())
