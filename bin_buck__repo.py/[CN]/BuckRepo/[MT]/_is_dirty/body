def _is_dirty(self):
    buck_repo_dirty = os.environ.get('BUCK_REPOSITORY_DIRTY')
    if buck_repo_dirty:
        return (buck_repo_dirty == '1')
    output = subprocess.check_output(['git', 'status', '-s'], cwd=self._buck_dir)
    return bool(output.strip())
