def _get_git_commit_timestamp(self):
    if (not self._is_git):
        return (-1)
    return subprocess.check_output(['git', 'log', '--pretty=format:%ct', '-1', 'HEAD', '--'], cwd=self._buck_dir).strip()
