{
  DefaultAndroidDirectoryResolver resolver=createResolver(Optional.<String>absent(),Optional.<Path>absent(),Optional.<Path>absent(),Optional.<Path>absent());
  assertEquals(Optional.<Path>absent(),resolver.getSdkOrAbsent());
  assertEquals(Optional.<Path>absent(),resolver.getNdkOrAbsent());
}
