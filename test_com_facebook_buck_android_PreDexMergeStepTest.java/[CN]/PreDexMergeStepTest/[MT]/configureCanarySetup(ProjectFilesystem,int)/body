{
  for (int i=1; i <= num; i++) {
    Path classFile=scratchDirectory.resolve("canary_" + i + "/secondary/dex0"+ i+ "/Canary.class");
    projectFilesystem.createParentDirs(classFile);
    projectFilesystem.copyToPath(capture(new Capture<InputStream>()),eq(classFile));
  }
}
