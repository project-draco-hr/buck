{
  EasyMock.expect(projectFilesystem.resolve(EasyMock.isA(Path.class))).andAnswer(new IAnswer<Path>(){
    @Override public Path answer() throws Throwable {
      Path arg=(Path)EasyMock.getCurrentArguments()[0];
      return arg;
    }
  }
).anyTimes();
}
