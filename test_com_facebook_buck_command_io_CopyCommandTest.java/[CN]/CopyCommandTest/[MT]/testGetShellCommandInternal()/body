{
  String source="path/to/source.txt";
  String destination="path/to/destination.txt";
  CopyCommand copyCommand=new CopyCommand(source,destination);
  MoreAsserts.assertListEquals(ImmutableList.of("cp","path/to/source.txt","path/to/destination.txt"),copyCommand.getShellCommand(context));
}
