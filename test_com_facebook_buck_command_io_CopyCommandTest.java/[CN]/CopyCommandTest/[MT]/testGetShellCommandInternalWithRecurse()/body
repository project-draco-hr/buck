{
  String source="path/to/source";
  String destination="path/to/destination";
  CopyCommand copyCommand=new CopyCommand(source,destination,true);
  MoreAsserts.assertListEquals(ImmutableList.of("cp","-R","path/to/source","path/to/destination"),copyCommand.getShellCommand(context));
}
