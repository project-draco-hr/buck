{
  Process process=new ProcessBuilder("watchman","watch-del",path.toAbsolutePath().toString()).start();
  try {
    process.waitFor();
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
    Thread.currentThread().interrupt();
  }
}
