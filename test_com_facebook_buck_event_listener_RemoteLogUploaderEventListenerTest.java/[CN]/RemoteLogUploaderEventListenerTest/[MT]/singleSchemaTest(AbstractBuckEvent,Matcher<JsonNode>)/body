{
  FluentIterable<JsonNode> loggedNodes=getJsonNodes(event);
  Matcher<JsonNode> nodeMatcher=Matchers.allOf(hasJsonField("buildId",BuckEventBusFactory.BUILD_ID_FOR_TEST.toString()),hasJsonField("type"),eventMatcher);
  assertThat(loggedNodes,Matchers.hasItem(nodeMatcher));
}
