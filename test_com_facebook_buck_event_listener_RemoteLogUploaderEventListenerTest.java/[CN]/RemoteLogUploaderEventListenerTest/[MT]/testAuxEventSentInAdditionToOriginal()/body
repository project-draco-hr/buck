{
  FluentIterable<JsonNode> nodes=getJsonNodes(CommandEvent.started("cook",ImmutableList.of("--dinner"),true));
  assertThat(nodes,Matchers.containsInAnyOrder(hasJsonField("type","CommandStarted"),hasJsonField("type","CommandStartedAux")));
}
