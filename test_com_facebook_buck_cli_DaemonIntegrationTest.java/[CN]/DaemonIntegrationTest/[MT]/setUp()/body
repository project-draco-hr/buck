{
  executorService=Executors.newScheduledThreadPool(5);
  tmp.newFolder(".git");
  Watchman watchman=Watchman.build(ImmutableSet.of(tmp.getRoot()),getWatchmanEnv(),new TestConsole(),new FakeClock(0));
  assumeFalse(watchman == Watchman.NULL_WATCHMAN);
}
