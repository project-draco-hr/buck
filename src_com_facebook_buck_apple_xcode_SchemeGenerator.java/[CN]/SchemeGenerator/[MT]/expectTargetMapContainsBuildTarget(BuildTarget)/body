{
  if (!buildTargetToPbxTargetMap.containsKey(buildTarget)) {
    throw new HumanReadableException("Scheme generation failed: No project containing required target %s was found.",buildTarget.getFullyQualifiedName());
  }
}
