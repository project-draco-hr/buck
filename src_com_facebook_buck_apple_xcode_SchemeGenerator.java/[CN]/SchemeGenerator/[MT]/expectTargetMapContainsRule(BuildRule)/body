{
  if (!buildRuleToTargetMap.containsKey(rule)) {
    throw new HumanReadableException("Scheme generation failed: No project containing required target %s was found.",rule.getFullyQualifiedName());
  }
}
