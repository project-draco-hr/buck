{
  builder.set("sourceLevel",sourceLevel).set("targetLevel",targetLevel).set("debug",debug).set("javacVersion",javacEnv.getJavacVersion().transform(Functions.toStringFunction()).orNull());
  return annotationProcessingData.appendToRuleKey(builder);
}
