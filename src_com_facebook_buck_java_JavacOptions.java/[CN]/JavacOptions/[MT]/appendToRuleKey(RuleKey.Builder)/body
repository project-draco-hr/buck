{
  builder.set("sourceLevel",sourceLevel).set("targetLevel",targetLevel).set("extraArguments",Joiner.on(',').join(extraArguments)).set("debug",debug);
  return annotationProcessingParams.appendToRuleKey(builder);
}
