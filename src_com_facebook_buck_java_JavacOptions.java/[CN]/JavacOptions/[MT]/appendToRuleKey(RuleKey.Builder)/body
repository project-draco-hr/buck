{
  builder.set("sourceLevel",sourceLevel).set("javacPath",javacPath.transform(Functions.toStringFunction()).orNull()).set("targetLevel",targetLevel).set("extraArguments",Joiner.on(',').join(extraArguments)).set("debug",debug);
  return annotationProcessingParams.appendToRuleKey(builder);
}
