{
  builder.set("sourceLevel",getSourceLevel()).set("javacPath",getJavacPath().transform(Functions.toStringFunction()).orNull()).set("javacJarPath",getJavacJarPath().transform(Functions.toStringFunction()).orNull()).set("targetLevel",getTargetLevel()).set("extraArguments",Joiner.on(',').join(getExtraArguments())).set("debug",isDebug());
  return getAnnotationProcessingParams().appendToRuleKey(builder);
}
