{
  Dot<BuildRule> dot=new Dot<BuildRule>(dependencyGraph,"dependency_graph",new Function<BuildRule,String>(){
    @Override public String apply(    BuildRule buildRule){
      return "\"" + buildRule.getFullyQualifiedName() + "\"";
    }
  }
,stdOut);
  try {
    dot.writeOutput();
  }
 catch (  IOException e) {
    return 1;
  }
  return 0;
}
