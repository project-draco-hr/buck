{
  ImmutableList.Builder<TargetNodeSpec> specs=ImmutableList.builder();
  CommandLineTargetNodeSpecParser parser=new CommandLineTargetNodeSpecParser(config,new BuildTargetPatternTargetNodeParser(new BuildTargetParser(),ignorePaths));
  for (  String arg : targetsAsArgs) {
    specs.add(parser.parse(arg));
  }
  return specs.build();
}
