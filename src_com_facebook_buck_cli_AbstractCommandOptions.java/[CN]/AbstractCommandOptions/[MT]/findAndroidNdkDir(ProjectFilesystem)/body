{
  Optional<File> path=findDirectoryByPropertiesThenEnvironmentVariable(projectFilesystem,"ndk.dir","ANDROID_NDK");
  if (path.isPresent()) {
    validateNdkVersion(projectFilesystem,path.get());
  }
  return path;
}
