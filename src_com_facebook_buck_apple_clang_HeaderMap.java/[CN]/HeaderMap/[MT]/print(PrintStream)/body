{
  visit(new HeaderMapVisitor(){
    @Override public void apply(    String str,    String prefix,    String suffix){
      stream.print("\"");
      stream.print(str);
      stream.print("\" -> \"");
      stream.print(prefix);
      stream.print(suffix);
      stream.print("\"\n");
    }
  }
);
}
