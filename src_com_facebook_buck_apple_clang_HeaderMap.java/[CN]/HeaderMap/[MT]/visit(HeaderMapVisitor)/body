{
  for (int i=0; i < numBuckets; i++) {
    Bucket bucket=buckets[i];
    if (bucket != null) {
      visitor.apply(getString(bucket.key),getString(bucket.prefix),getString(bucket.suffix));
    }
  }
}
