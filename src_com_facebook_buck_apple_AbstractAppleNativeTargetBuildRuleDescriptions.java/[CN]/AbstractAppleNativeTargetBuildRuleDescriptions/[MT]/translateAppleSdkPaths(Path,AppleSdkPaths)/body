{
  if (path.startsWith("$SDKROOT")) {
    path=appleSdkPaths.getSdkPath().resolve(path.subpath(1,path.getNameCount()));
  }
 else   if (path.startsWith("$DEVELOPER_DIR")) {
    path=appleSdkPaths.getDeveloperPath().resolve(path.subpath(1,path.getNameCount()));
  }
  return path;
}
