{
  try (Writer writer=new BufferedWriter(new FileWriter(getFileForRelativePath(pathRelativeToProjectRoot)))){
    for (    String line : lines) {
      writer.write(line);
      writer.write('\n');
    }
  }
 }
