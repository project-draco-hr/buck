{
  if (androidBootclasspathSupplier == null) {
    setDefaultAndroidBootclasspathSupplier();
  }
  return new BuildContext(projectRoot,dependencyGraph,stepRunner,projectFilesystem,artifactCache,javaPackgeFinder,events,androidBootclasspathSupplier,buildDependencies,console);
}
