{
  return new TestThreadStateRenderer(ANSI,FORMAT_TIME_FUNCTION,timeMs,testSummaries,testStatusMessages,runningSteps,new AccumulatedTimeTracker(ImmutableMap.<Long,Optional<? extends BuildRuleEvent>>of(),testEvents,accumulatedTimes));
}
