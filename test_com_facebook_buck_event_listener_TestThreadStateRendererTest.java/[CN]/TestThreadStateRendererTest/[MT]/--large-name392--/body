{
  TestThreadStateRenderer renderer=new TestThreadStateRenderer(ANSI,FORMAT_TIME_FUNCTION,timeMs,testEvents,testSummaries,testStatusMessages,runningSteps,accumulatedTimes);
  ImmutableList.Builder<String> lines=ImmutableList.builder();
  StringBuilder lineBuilder=new StringBuilder();
  for (  long threadId : renderer.getSortedThreadIds()) {
    lines.add(renderer.renderStatusLine(threadId,lineBuilder));
  }
  return lines.build();
}
