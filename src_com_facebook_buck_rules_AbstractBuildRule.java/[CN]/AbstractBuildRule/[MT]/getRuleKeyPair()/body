{
  if (ruleKeyPair == null) {
synchronized (this) {
      if (ruleKeyPair == null) {
        RuleKey.Builder builder=RuleKey.builder(this);
        appendToRuleKey(builder);
        ruleKeyPair=builder.build();
      }
    }
  }
  return ruleKeyPair;
}
