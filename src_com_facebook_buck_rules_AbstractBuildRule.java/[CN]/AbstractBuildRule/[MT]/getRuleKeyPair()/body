{
  if (ruleKeyPair == null) {
synchronized (this) {
      if (ruleKeyPair == null) {
        RuleKey.Builder builder=ruleKeyBuilderFactory.newInstance(this);
        ruleKeyPair=builder.build();
      }
    }
  }
  return ruleKeyPair;
}
