{
  if (ruleKey == null) {
synchronized (this) {
      if (ruleKey == null) {
        RuleKey.Builder builder=RuleKey.builder(this);
        appendToRuleKey(builder);
        ruleKey=builder.build();
      }
    }
  }
  return ruleKey;
}
