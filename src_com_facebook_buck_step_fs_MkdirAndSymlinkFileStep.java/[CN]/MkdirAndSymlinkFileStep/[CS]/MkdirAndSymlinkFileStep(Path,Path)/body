{
  super(ImmutableList.of(new MkdirStep(target.getParent()),new SymlinkFileStep(source,target,true)));
  this.source=Preconditions.checkNotNull(source);
  this.target=Preconditions.checkNotNull(target);
}
