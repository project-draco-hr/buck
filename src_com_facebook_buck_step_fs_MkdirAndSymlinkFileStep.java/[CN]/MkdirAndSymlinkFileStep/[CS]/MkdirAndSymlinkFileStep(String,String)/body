{
  super(ImmutableList.of(new MkdirStep(Paths.get(target).getParent().toString()),new SymlinkFileStep(source,target,true)));
  this.source=Preconditions.checkNotNull(source);
  this.target=Preconditions.checkNotNull(target);
}
