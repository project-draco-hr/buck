{
  try {
    graph=params.getParser().buildTargetGraph(params.getBuckEventBus(),params.getCell(),enableProfiling,targets);
  }
 catch (  BuildFileParseException|IOException e) {
    throw new QueryException("Error in building depencency graph");
  }
}
