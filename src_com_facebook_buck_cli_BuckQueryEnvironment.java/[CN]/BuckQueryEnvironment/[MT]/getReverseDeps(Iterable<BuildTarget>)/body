{
  Set<BuildTarget> result=new LinkedHashSet<>();
  for (  BuildTarget target : targets) {
    TargetNode<?> node=getNode(target);
    if (node != null) {
      result.addAll(getTargetsFromNodes(ImmutableSortedSet.copyOf(graph.getIncomingNodesFor(node))));
    }
  }
  return result;
}
