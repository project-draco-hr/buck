{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,name,tmp);
  workspace.setUp();
  workspace.writeContentsToPath(String.format("[ndk]\n  compiler = %s\n  cxx_runtime = %s\n",compiler,cxxRuntime),".buckconfig");
  return workspace;
}
