{
  File inputFile=context.getProjectFilesystem().getFileForRelativePath(src);
  File outputFile=out.get();
  ImmutableList.Builder<Step> builder=ImmutableList.<Step>builder().add(new MkdirAndSymlinkFileStep(inputFile,outputFile));
  return builder.build();
}
