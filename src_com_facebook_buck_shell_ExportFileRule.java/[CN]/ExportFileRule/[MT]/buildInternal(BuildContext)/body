{
  File inputFile=context.getProjectFilesystem().getFileForRelativePath(src);
  String pathToOutputFile=out.get();
  ImmutableList.Builder<Step> builder=ImmutableList.<Step>builder().add(new MkdirAndSymlinkFileStep(inputFile,pathToOutputFile));
  return builder.build();
}
