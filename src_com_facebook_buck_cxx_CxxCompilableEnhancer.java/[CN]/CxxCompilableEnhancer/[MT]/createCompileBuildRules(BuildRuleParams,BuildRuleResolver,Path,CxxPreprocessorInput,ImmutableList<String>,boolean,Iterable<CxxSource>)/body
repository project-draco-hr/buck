{
  ImmutableSortedSet.Builder<BuildRule> rules=ImmutableSortedSet.naturalOrder();
  for (  CxxSource source : sources) {
    rules.add(createCompileBuildRule(params,resolver,compiler,preprocessorInput,compilerFlags,pic,source));
  }
  return rules.build();
}
