{
  BuildTarget buildTarget=BuildTargetFactory.newInstance("//java/com/example:library");
  AndroidLibraryGraphEnhancer graphEnhancer=new AndroidLibraryGraphEnhancer(buildTarget,new FakeBuildRuleParamsBuilder(buildTarget).build(),DEFAULT_JAVAC_OPTIONS,ResourceDependencyMode.FIRST_ORDER,Optional.<String>absent());
  Optional<DummyRDotJava> result=graphEnhancer.getBuildableForAndroidResources(new BuildRuleResolver(),false);
  assertFalse(result.isPresent());
}
