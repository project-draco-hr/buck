{
  BuildTarget buildTarget=new BuildTarget("//java/com/example","library");
  AndroidLibraryGraphEnhancer graphEnhancer=new AndroidLibraryGraphEnhancer(buildTarget,new FakeBuildRuleParamsBuilder(buildTarget).build(),JavacOptions.DEFAULTS);
  Optional<DummyRDotJava> result=graphEnhancer.createBuildableForAndroidResources(new BuildRuleResolver(),false);
  assertFalse(result.isPresent());
}
