{
  for (  Writer writer : Iterables.concat(commandIdToConsoleHandlerWriter.values(),commandIdToLogFileHandlerWriter.values())) {
    try {
      writer.close();
    }
 catch (    IOException e) {
      LOG.error(e,"Failed to cleanly close() and OutputStreamWriter.");
    }
  }
}
