{
  if (AndroidDebugBridge.getBridge() == null || !(AndroidDebugBridge.getBridge().hasInitialDeviceList() && AndroidDebugBridge.getBridge().isConnected())) {
    AndroidDebugBridge.initIfNeeded(true);
    AndroidDebugBridge.createBridge(ADB_PATH,false);
  }
}
