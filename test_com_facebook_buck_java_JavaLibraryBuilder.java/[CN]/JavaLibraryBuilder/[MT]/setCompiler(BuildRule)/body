{
  SourcePath right=new BuildTargetSourcePath(javac.getProjectFilesystem(),javac.getBuildTarget());
  Either<BuiltInJavac,SourcePath> value=Either.ofRight(right);
  arg.compiler=Optional.of(value);
  return this;
}
