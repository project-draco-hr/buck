{
class NoName implements ConstructorArg {
    public String random;
  }
  String definition=buckPyFunction.toPythonFunction(new BuildRuleType("bad"),new NoName());
  assertTrue(definition.contains("name"));
}
