def invoke_sign(self):
    out = self.get_output_for_command([self.codesign_tool_path, '-s', self.identity_sha, '-f', '--entitlements', self.entitlements_file.name, self.app_bundle_path])
    print ('Codesign complete: ' + out)
