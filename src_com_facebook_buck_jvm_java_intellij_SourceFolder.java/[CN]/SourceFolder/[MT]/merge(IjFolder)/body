{
  checkMergeConditions(otherFolder);
  if (otherFolder == this) {
    return this;
  }
  return new SourceFolder(otherFolder.getPath(),getWantsPackagePrefix() || otherFolder.getWantsPackagePrefix(),combineInputs(this,otherFolder));
}
