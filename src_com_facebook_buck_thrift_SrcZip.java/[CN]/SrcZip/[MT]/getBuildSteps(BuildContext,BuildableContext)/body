{
  buildableContext.recordArtifact(sourceZip);
  return ImmutableList.of(new RmStep(sourceZip,true),new ZipStep(sourceZip,ImmutableSet.<Path>of(),false,ZipStep.MIN_COMPRESSION_LEVEL,sourceDirectory));
}
