{
  if (!(event instanceof StartActivityEvent)) {
    return false;
  }
  StartActivityEvent that=(StartActivityEvent)event;
  return Objects.equal(getBuildTarget(),that.getBuildTarget()) && Objects.equal(getActivityName(),that.getActivityName());
}
