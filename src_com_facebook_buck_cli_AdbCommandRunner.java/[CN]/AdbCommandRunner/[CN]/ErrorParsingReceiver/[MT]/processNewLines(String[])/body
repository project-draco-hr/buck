{
  for (  String line : lines) {
    if (line.length() > 0) {
      if (matchForSuccess(line)) {
        mErrorMessage=null;
      }
 else {
        String err=matchForError(line);
        if (err != null) {
          mErrorMessage=err;
        }
      }
    }
  }
}
