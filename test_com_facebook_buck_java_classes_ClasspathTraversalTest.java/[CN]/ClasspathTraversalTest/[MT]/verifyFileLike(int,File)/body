{
  int fileLikeCount=0;
  for (  Map.Entry<FileLike,String> entry : traverse(Lists.newArrayList(paths)).entrySet()) {
    assertEquals("Relative file-like path mismatch",entry.getValue(),entry.getKey().getRelativePath());
    fileLikeCount++;
  }
  assertEquals(expectedFiles,fileLikeCount);
}
