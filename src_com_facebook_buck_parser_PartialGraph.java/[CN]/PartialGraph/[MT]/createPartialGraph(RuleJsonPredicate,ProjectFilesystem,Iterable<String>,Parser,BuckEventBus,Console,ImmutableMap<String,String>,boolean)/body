{
  ImmutableSet<BuildTarget> targets=parser.filterAllTargetsInProject(filesystem,includes,predicate,console,environment,eventBus,enableProfiling);
  return createPartialGraph(targets,includes,parser,eventBus,console,environment,Optional.<BuildRuleResolver>absent());
}
