{
  Preconditions.checkNotNull(parser);
  ActionGraph graph=parser.parseBuildFilesForTargets(targets,includes,eventBus,console,environment).buildActionGraph();
  return new PartialGraph(graph,targets);
}
