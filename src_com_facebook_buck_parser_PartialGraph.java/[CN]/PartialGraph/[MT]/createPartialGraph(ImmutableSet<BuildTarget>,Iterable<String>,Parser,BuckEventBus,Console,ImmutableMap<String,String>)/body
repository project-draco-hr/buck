{
  Preconditions.checkNotNull(parser);
  ActionGraph graph=parser.buildTargetGraph(targets,includes,eventBus,console,environment).getActionGraph(eventBus);
  return new PartialGraph(graph,targets);
}
