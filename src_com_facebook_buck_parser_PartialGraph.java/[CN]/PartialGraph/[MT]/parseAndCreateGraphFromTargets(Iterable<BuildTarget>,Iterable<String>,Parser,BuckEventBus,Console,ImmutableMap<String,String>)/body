{
  Preconditions.checkNotNull(parser);
  ActionGraph graph=parser.parseBuildFilesForTargets(targets,includes,eventBus,console,environment);
  return new PartialGraph(graph,ImmutableList.copyOf(targets));
}
