{
  List<BuildTarget> targets=parser.filterAllTargetsInProject(filesystem,includes,predicate,console,environment);
  return parseAndCreateGraphFromTargets(targets,includes,parser,eventBus,console,environment);
}
