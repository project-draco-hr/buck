{
  TargetGraph graph=parser.buildTargetGraphForTargetNodeSpecs(ImmutableList.of(new TargetNodePredicateSpec(Predicates.<TargetNode<?>>alwaysTrue(),projectFilesystem.getIgnorePaths())),includes,eventBus,console,environment,enableProfiling);
  return new PartialGraph(graph,FluentIterable.from(graph.getNodes()).transform(HasBuildTarget.TO_TARGET).toSet());
}
