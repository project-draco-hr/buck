{
  Preconditions.checkNotNull(parser);
  ActionGraph graph=parser.buildTargetGraph(targets,includes,eventBus,console,environment).buildActionGraph(ruleResolver,eventBus);
  return new PartialGraph(graph,targets);
}
