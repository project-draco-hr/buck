{
  if (this == other) {
    return true;
  }
  if (!(other instanceof TokenValue)) {
    return false;
  }
  TokenValue that=(TokenValue)other;
  return Objects.equals(this.type,that.type) && Objects.equals(this.literalValue,that.literalValue) && Objects.equals(this.interpolationValue,that.interpolationValue);
}
