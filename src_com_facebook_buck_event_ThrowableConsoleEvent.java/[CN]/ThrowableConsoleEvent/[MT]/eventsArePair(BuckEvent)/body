{
  if (!(event instanceof ThrowableConsoleEvent)) {
    return false;
  }
  ThrowableConsoleEvent that=(ThrowableConsoleEvent)event;
  return super.eventsArePair(that) && Objects.equal(getThrowable(),that.getThrowable());
}
