def main():
    args = parse_args()
    command = get_command(args.repo_root, args.buck_path, args.build_flags, args.escaped_build_target, args.buck_dwarf_flavor, args.buck_dsym_flavor)
    print command
