{
  try {
    return Class.forName(AbiWriterProtocol.ABI_ANNOTATION_PROCESSOR_CLASS_NAME).asSubclass(Processor.class);
  }
 catch (  ClassNotFoundException e) {
    LOG.info("Unable to load AbiWriter.");
    return null;
  }
}
