{
  String name=String.format("//:%s",suffix);
  return new FakeTestRule(JavaTestDescription.TYPE,ImmutableSet.<Label>of(),BuildTargetFactory.newInstance(name),ImmutableSortedSet.copyOf(dependencies),ImmutableSet.<BuildTargetPattern>of());
}
