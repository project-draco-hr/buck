{
  String name=String.format("//:%s",suffix);
  return new FakeTestRule(ImmutableSet.<Label>of(),BuildTargetFactory.newInstance(name),new SourcePathResolver(new BuildRuleResolver()),ImmutableSortedSet.copyOf(dependencies));
}
