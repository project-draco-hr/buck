{
  return Futures.allAsList(Iterables.transform(rules,new Function<BuildRule,ListenableFuture<BuildRuleSuccess>>(){
    @Override public ListenableFuture<BuildRuleSuccess> apply(    BuildRule rule){
      return rule.build(context);
    }
  }
));
}
