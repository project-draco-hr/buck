{
  if (!(o instanceof InstallEvent)) {
    return false;
  }
  InstallEvent that=(InstallEvent)o;
  return Objects.equal(getClass(),o.getClass()) && Objects.equal(getBuildTarget(),that.getBuildTarget());
}
