{
  String expectedJavaCode="package com.example.buck;\n" + "public class BuildConfig {\n" + "  private BuildConfig() {}\n"+ "  public static final boolean DEBUG = !Boolean.parseBoolean(null);\n"+ "  public static final boolean IS_EXOPACKAGE = Boolean.parseBoolean(null);\n"+ "}\n";
  String observedJavaCode=BuildConfigs.generateBuildConfigDotJava("com.example.buck");
  assertEquals("Because the 'temporary' BuildConfig.java might be used in a unit test, " + "DEBUG should default to true while IS_EXOPACKAGE should default to false.",expectedJavaCode,observedJavaCode);
}
