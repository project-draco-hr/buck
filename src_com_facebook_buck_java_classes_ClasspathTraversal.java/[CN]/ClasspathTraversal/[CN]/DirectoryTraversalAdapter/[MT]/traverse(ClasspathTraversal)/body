{
  DirectoryTraversal impl=new DirectoryTraversal(file){
    @Override public void visit(    Path file,    String relativePath) throws IOException {
      traversal.visit(new FileLikeInDirectory(file,relativePath));
    }
  }
;
  impl.traverse();
}
