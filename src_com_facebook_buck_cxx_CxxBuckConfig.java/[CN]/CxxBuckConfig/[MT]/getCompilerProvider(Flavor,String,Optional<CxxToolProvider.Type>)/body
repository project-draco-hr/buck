{
  Optional<CxxToolProviderParams> params=getCxxToolProviderParams(flavor.toString() + '_' + field,defaultType).or(getCxxToolProviderParams(field,defaultType));
  if (!params.isPresent()) {
    return Optional.absent();
  }
  return Optional.of(params.get().getCompilerProvider());
}
