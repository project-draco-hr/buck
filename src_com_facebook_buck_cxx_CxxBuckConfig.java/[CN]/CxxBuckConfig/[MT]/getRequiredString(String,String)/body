{
  Optional<String> value=delegate.getValue(section,name);
  if (!value.isPresent()) {
    throw new HumanReadableException(String.format("Required config setting %s:%s not set",section,name));
  }
  return value.get();
}
