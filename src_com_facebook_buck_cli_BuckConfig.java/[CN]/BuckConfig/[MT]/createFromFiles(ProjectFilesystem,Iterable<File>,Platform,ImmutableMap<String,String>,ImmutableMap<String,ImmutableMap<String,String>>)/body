{
  if (Iterables.isEmpty(files)) {
    return new BuckConfig(new Config(configOverrides),projectFilesystem,platform,environment);
  }
  ImmutableMap.Builder<Path,Reader> readers=ImmutableMap.builder();
  for (  File file : files) {
    readers.put(file.toPath(),Files.newReader(file,Charsets.UTF_8));
  }
  return createFromReaders(readers.build(),projectFilesystem,platform,environment,configOverrides);
}
