{
  ImmutableSortedSet<BuildTarget> sortedTargets=ImmutableSortedSet.copyOf(Iterables.transform(targets,HasBuildTarget.TO_TARGET));
  return ImmutableBuildTarget.of(Optional.<String>absent(),"//buck/synthesized/ios",Joiner.on('-').join(Iterables.transform(sortedTargets,new Function<BuildTarget,String>(){
    @Override public String apply(    BuildTarget input){
      return input.getFullyQualifiedName().replace('/','-').replace('.','-').replace('+','-').replace(' ','-').replace(':','-');
    }
  }
)),ImmutableSortedSet.<Flavor>of());
}
