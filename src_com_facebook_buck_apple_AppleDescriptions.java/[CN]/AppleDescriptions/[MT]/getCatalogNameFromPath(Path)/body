{
  String name=assetCatalogDir.getFileName().toString();
  if (name.endsWith(AppleDescriptions.XCASSETS_DIRECTORY_EXTENSION)) {
    name=name.substring(0,name.length() - AppleDescriptions.XCASSETS_DIRECTORY_EXTENSION.length());
  }
  return name;
}
