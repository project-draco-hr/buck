{
  this.offlineScribeLogger=new OfflineScribeLogger(new FakeFailingScribeLogger(),blacklistCategories);
  this.blacklistCategories=blacklistCategories;
  storedCategoriesWithLines=new AtomicInteger(0);
}
