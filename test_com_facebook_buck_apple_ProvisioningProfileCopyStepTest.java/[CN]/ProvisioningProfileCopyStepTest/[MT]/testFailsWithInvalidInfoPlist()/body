{
  thrown.expect(HumanReadableException.class);
  ProvisioningProfileCopyStep step=new ProvisioningProfileCopyStep(testdataDir.resolve("Invalid.plist"),Optional.<String>absent(),Optional.<Path>absent(),ProvisioningProfileCopyStep.findProfilesInPath(testdataDir),outputFile,xcentFile);
  step.execute(executionContext);
}
