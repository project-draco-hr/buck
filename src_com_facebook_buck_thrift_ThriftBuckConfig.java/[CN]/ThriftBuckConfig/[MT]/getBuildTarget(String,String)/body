{
  Optional<String> target=delegate.getValue(section,field);
  return target.isPresent() ? Optional.of(delegate.getBuildTargetForFullyQualifiedTarget(target.get())) : Optional.<BuildTarget>absent();
}
