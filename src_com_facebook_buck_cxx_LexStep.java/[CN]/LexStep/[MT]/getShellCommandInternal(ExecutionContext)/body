{
  return ImmutableList.<String>builder().addAll(lexPrefix).addAll(flags).add("--outfile=" + outputSource.toString()).add("--header-file=" + outputHeader.toString()).add(input.toString()).build();
}
