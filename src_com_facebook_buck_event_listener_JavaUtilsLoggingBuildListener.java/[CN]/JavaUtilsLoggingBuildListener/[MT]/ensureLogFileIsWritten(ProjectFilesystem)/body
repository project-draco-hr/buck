{
  try {
    filesystem.mkdirs(BuckConstant.BIN_PATH);
  }
 catch (  IOException e) {
    throw new HumanReadableException(e,"Unable to create output directory: %s",BuckConstant.BIN_DIR);
  }
  try {
    FileHandler handler=new FileHandler(filesystem.resolve(BuckConstant.BIN_PATH.resolve("build.log")).toString(),false);
    Formatter formatter=new BuildEventFormatter();
    handler.setFormatter(formatter);
    LOG.setUseParentHandlers(false);
    LOG.addHandler(handler);
    LOG.setLevel(LEVEL);
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
