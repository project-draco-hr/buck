{
switch (field) {
case BUILD_ID:
    if (value == null) {
      unsetBuildId();
    }
 else {
      setBuildId((BuildId)value);
    }
  break;
case BUCK_VERSION:
if (value == null) {
  unsetBuckVersion();
}
 else {
  setBuckVersion((BuckVersion)value);
}
break;
}
}
