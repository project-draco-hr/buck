{
  return target.getBasePath().resolve(getLibDir(target,cellNames,ruleResolver,cxxPlatform,libDir)).resolve(String.format("lib%s%s",getLibName(target,cellNames,ruleResolver,cxxPlatform,libName),suffix));
}
