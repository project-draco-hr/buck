{
  return new BuildThreadStateRenderer(ANSI,FORMAT_TIME_FUNCTION,timeMs,runningSteps,new AccumulatedTimeTracker(buildEvents,ImmutableMap.<Long,Optional<? extends TestRuleEvent>>of(),accumulatedTimes));
}
