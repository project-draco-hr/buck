{
  BuildTarget target=pendingBuildTargets.poll(WAIT_FOR_WORK_TIMEOUT_MILLIS,TimeUnit.MILLISECONDS);
  if (target == null) {
    throw new TimeoutException();
  }
  return new BuildTargetProcessingScope(target);
}
