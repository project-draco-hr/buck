{
  BuildTarget target=pendingBuildTargets.poll(WAIT_FOR_WORK_TIMEOUT_SECONDS,TimeUnit.SECONDS);
  if (target == null) {
    throw new TimeoutException();
  }
  return new BuildTargetProcessingScope(target);
}
