{
  Path buildFile=pendingBuildFiles.poll(WAIT_FOR_WORK_TIMEOUT_SECONDS,TimeUnit.SECONDS);
  if (buildFile == null) {
    throw new TimeoutException();
  }
  return new BuildFileProcessingScope(buildFile);
}
