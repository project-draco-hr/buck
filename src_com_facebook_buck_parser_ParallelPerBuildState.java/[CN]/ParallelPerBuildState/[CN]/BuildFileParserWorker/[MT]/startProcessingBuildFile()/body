{
  Path buildFile=pendingBuildFiles.poll(WAIT_FOR_WORK_TIMEOUT_MILLIS,TimeUnit.MILLISECONDS);
  if (buildFile == null) {
    throw new TimeoutException();
  }
  return new BuildFileProcessingScope(buildFile);
}
