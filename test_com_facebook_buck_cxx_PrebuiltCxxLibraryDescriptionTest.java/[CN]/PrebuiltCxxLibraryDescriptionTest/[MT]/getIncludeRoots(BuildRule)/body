{
  return Iterables.transform(getInputRules(buildRule),new Function<BuildTarget,Path>(){
    @Override public Path apply(    BuildTarget target){
      return CxxDescriptionEnhancer.getHeaderSymlinkTreePath(target,CXX_PLATFORM.getFlavor(),HeaderVisibility.PUBLIC);
    }
  }
);
}
