{
  ImmutableList.Builder<Step> steps=ImmutableList.builder();
  steps.add(new RmStep(getPathToOutputFile(),true));
  steps.add(new MkdirStep(pathToOutputFile.getParent()));
  steps.add(new AccumulateClassNamesStep(Paths.get(javaLibraryRule.getPathToOutputFile()),Paths.get(getPathToOutputFile())));
  return steps.build();
}
