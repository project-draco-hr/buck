{
  if (!(event instanceof AnnotationProcessingEvent)) {
    return false;
  }
  AnnotationProcessingEvent that=(AnnotationProcessingEvent)event;
  return Objects.equal(getBuildTarget(),that.getBuildTarget()) && Objects.equal(getAnnotationProcessorName(),that.getAnnotationProcessorName()) && Objects.equal(getOperation(),that.getOperation())&& getRound() == that.getRound();
}
