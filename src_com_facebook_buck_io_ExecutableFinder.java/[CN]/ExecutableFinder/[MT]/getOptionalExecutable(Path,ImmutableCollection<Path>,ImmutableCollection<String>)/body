{
  Optional<Path> executable=Optional.fromNullable(findExecutable(suggestedExecutable,path,fileSuffixes));
  LOG.debug("Executable '%s' mapped to '%s'",suggestedExecutable,executable);
  return executable;
}
