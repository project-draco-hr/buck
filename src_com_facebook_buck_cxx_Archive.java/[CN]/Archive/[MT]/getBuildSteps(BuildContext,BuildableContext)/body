{
  buildableContext.recordArtifact(output);
  return ImmutableList.of(new MkdirStep(output.getParent()),new RmStep(output,true),new ArchiveStep(getResolver().getPath(archiver),output,getResolver().getAllPaths(inputs)),new ArchiveScrubberStep(output));
}
