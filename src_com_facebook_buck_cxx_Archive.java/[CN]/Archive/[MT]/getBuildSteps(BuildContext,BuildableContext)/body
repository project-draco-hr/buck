{
  buildableContext.recordArtifact(output);
  return ImmutableList.of(new MkdirStep(output.getParent()),new RmStep(output,true),new ArchiveStep(archiver.resolve(),output,SourcePaths.toPaths(inputs)),new ArchiveScrubberStep(output));
}
