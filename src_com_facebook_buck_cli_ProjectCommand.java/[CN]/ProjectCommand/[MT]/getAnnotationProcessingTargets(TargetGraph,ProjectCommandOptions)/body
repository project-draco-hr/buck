{
  ImmutableSet<BuildTarget> buildTargets=getRootsFromOptionsWithPredicate(fullGraph,options,ANNOTATION_PREDICATE);
  return FluentIterable.from(buildTargets).transform(Functions.toStringFunction()).toList();
}
