{
  return ImmutableList.copyOf(Iterables.transform(createPartialGraph(annotationPredicate,options).getTargets(),new Function<BuildTarget,String>(){
    @Override public String apply(    BuildTarget target){
      return target.getFullyQualifiedName();
    }
  }
));
}
