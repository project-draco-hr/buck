{
  return ImmutableList.copyOf(Iterables.transform(createPartialGraph(ANNOTATION_PREDICATE,AssociatedRulePredicates.alwaysTrue(),options).getTargets(),new Function<BuildTarget,String>(){
    @Override public String apply(    BuildTarget target){
      return target.getFullyQualifiedName();
    }
  }
));
}
