{
  Parser parser=new Parser(repository,enforcePackageBoundary,tempFilePatterns,buildFileName,buildFileTreeSupplier,buildFileParserFactory);
  try {
    parser.parseRawRulesInternal(rules);
  }
 catch (  BuildTargetException|IOException e) {
    throw new RuntimeException(e);
  }
  return parser;
}
