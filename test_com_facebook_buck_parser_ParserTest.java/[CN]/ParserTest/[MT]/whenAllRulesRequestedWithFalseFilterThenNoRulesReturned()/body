{
  List<BuildTarget> targets=testParser.filterAllTargetsInProject(filesystem,Lists.<String>newArrayList(),alwaysFalse(),new TestConsole(),ImmutableMap.<String,String>of());
  Preconditions.checkNotNull(targets);
  assertEquals("Should have returned no rules.",0,targets.size());
}
