{
  List<BuildTarget> targets=testParser.filterAllTargetsInProject(filesystem,Lists.<String>newArrayList(),alwaysFalse(),new TestConsole());
  Preconditions.checkNotNull(targets);
  assertEquals("Should have returned no rules.",0,targets.size());
}
