{
  Parser parser=new Parser(repositoryFactory,new ParserConfig(new FakeBuckConfig(ImmutableMap.<String,Map<String,String>>of("project",ImmutableMap.of("temp_files",".*\\.swp$")))),buildFileTreeSupplier,buildTargetParser,buildFileParserFactory,new FakeRuleKeyBuilderFactory());
  try {
    parser.parseRawRulesInternal(rules);
  }
 catch (  BuildTargetException|IOException e) {
    throw new RuntimeException(e);
  }
  return parser;
}
