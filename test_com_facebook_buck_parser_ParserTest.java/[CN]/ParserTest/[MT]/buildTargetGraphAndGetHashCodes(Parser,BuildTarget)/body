{
  ImmutableList<BuildTarget> buildTargetsList=ImmutableList.copyOf(buildTargets);
  parser.buildTargetGraphForBuildTargets(buildTargetsList,ImmutableList.<String>of(),BuckEventBusFactory.newInstance(),new TestConsole(),ImmutableMap.<String,String>of(),false);
  return parser.getBuildTargetHashCodeCache().getAll(buildTargetsList);
}
