{
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(new TestConsole(),ImmutableMap.<String,String>of(),BuckEventBusFactory.newInstance())){
    parser.parseBuildFile(buildFile,new ParserConfig(new FakeBuckConfig()),projectBuildFileParser,ImmutableMap.<String,String>of());
  }
 }
