{
  BuckConfig config=new FakeBuckConfig();
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(new TestConsole(),config.getEnvironment(),BuckEventBusFactory.newInstance())){
    parser.parseBuildFile(buildFile,new ParserConfig(config),projectBuildFileParser,config.getEnvironment());
  }
 }
