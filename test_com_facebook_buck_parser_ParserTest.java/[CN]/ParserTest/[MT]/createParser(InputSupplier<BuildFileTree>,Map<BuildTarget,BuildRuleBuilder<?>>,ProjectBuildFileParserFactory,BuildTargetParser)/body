{
  return new Parser(filesystem,buildRuleTypes,new TestConsole(),ImmutableMap.copyOf(System.getenv()),buildFileTreeSupplier,buildTargetParser,knownBuildTargets,buildFileParserFactory,tempFilePatterns,new FakeRuleKeyBuilderFactory());
}
