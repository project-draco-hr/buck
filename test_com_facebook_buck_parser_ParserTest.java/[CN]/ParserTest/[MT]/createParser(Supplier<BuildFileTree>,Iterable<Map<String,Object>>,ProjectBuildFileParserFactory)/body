{
  BuckConfig buckConfig=FakeBuckConfig.builder().setFilesystem(filesystem).setSections("[project]","temp_files = .*\\.swp$").build();
  return createParser(buildFileTreeSupplier,rules,buildFileParserFactory,buckConfig);
}
