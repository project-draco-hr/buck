{
  ParserConfig parserConfig=new ParserConfig(new FakeBuckConfig(ImmutableMap.of("project",ImmutableMap.of("temp_files",".*\\.swp$"))));
  return createParser(buildFileTreeSupplier,rules,buildFileParserFactory,parserConfig);
}
