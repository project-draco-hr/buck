{
  ConsoleEventExternalInterface consoleEvent=objectMapper.readValue(rawMessage,ConsoleEventExternalInterface.class);
  buckEventsConsumerFactory.getConsoleEventConsumer().consumeConsoleEvent(consoleEvent.getMessage());
}
