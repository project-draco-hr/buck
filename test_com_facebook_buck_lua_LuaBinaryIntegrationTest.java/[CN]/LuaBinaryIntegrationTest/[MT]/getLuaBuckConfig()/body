{
  Config rawConfig=Configs.createConfig(ConfigConfig.of().withProjectRoot(tmp.getRootPath()));
  BuckConfig buckConfig=new BuckConfig(rawConfig,new ProjectFilesystem(tmp.getRootPath()),Architecture.detect(),Platform.detect(),ImmutableMap.<String,String>of());
  return new LuaBuckConfig(buckConfig,new FakeExecutableFinder(ImmutableList.<Path>of()));
}
