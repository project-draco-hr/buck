{
  RuleKey ruleKey;
  try {
    ruleKey=getBuildRule().getRuleKey();
  }
 catch (  IOException e) {
    throw new RuntimeException("RuleKey should already be computed if this is built.",e);
  }
  return String.format("BuildRuleFinished(%s): %s %s %s",getBuildRule(),getStatus(),getCacheResult(),ruleKey);
}
