{
  JavacOptions original=createStandardBuilder().setSourceLevel("5").setBootclasspathMap(ImmutableMap.of("5","some-magic.jar:also.jar")).build();
  JavacOptions copy=JavacOptions.builder(original).build();
  assertOptionsContains(copy,"-bootclasspath some-magic.jar:also.jar");
}
