{
  assumeTrue(Platform.detect() == Platform.MACOS || Platform.detect() == Platform.LINUX);
  workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"worker_tool_test",tmpFolder);
  workspace.setUp();
}
