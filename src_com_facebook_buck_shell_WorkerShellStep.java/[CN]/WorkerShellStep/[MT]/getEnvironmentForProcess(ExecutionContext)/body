{
  Map<String,String> envVars=this.getEnvironmentVariables(context);
  Map<String,String> contextEnv=Maps.filterKeys(context.getEnvironment(),Predicates.not(Predicates.in(envVars.keySet())));
  return ImmutableMap.<String,String>builder().putAll(envVars).putAll(contextEnv).build();
}
