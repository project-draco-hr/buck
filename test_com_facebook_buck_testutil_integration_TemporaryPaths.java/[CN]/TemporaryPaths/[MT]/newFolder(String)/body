{
  Path toCreate=root.resolve(name);
  if (Files.exists(toCreate)) {
    throw new IOException("a folder with the name \'" + name + "\' already exists in the test folder");
  }
  return Files.createDirectories(toCreate);
}
