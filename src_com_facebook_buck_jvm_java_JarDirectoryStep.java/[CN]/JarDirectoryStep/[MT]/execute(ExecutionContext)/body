{
  try {
    return JarDirectoryStepHelper.createJarFile(filesystem,pathToOutputFile,entriesToJar,Optional.fromNullable(mainClass),Optional.fromNullable(manifestFile),mergeManifests,blacklist,context);
  }
 catch (  IOException e) {
    e.printStackTrace(context.getStdErr());
    return 1;
  }
}
