{
  return newBuilder().setProjectFilesystem(new ProjectFilesystem(new File(".")){
    @Override public Path resolve(    Path path){
      return path;
    }
    @Override public Function<Path,Path> getAbsolutifier(){
      return IdentityPathRelativizer.getIdentityAbsolutifier();
    }
  }
).build();
}
