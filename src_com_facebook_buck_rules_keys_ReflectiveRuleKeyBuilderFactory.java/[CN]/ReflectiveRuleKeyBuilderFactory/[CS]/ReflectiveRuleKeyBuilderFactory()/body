{
  knownFields=CacheBuilder.newBuilder().build(new ReflectiveAlterKeyLoader());
  knownRules=CacheBuilder.newBuilder().weakKeys().build(new CacheLoader<BuildRule,U>(){
    @Override public U load(    BuildRule key) throws Exception {
      return newInstance(key).build();
    }
  }
);
}
