{
  return ExecutionContext.builder().setConsole(new Console(Verbosity.SILENT,System.out,System.err,Ansi.withoutTty())).setProjectFilesystem(new ProjectFilesystem(new File("."))).setEventBus(new EventBus()).build();
}
