{
  BuildTarget target=BuildTargetFactory.newInstance(String.format("//%s:genrule",contextBasePath));
  return GenruleBuilder.newGenruleBuilder(target).setBash(originalCmd).setOut("example-file").setDeps(deps).build(ruleResolver);
}
