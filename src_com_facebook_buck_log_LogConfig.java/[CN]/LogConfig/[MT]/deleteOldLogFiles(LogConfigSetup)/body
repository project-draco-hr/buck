{
  for (  Path path : PathListing.listMatchingPathsWithFilters(BuckConstant.getLogPath(),logConfigSetup.getLogFilePrefix() + "*.log*",PathListing.GET_PATH_MODIFIED_TIME,PathListing.FilterMode.EXCLUDE,Optional.<Integer>absent(),Optional.of(MAX_LOG_SIZE))) {
    Files.deleteIfExists(path);
  }
}
