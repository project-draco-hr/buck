{
  for (  Path path : PathListing.listMatchingPathsWithFilters(BuckConstant.LOG_PATH,"buck-*.log*",PathListing.MODIFIED_TIME_DESC,PathListing.FilterMode.EXCLUDE,Optional.<Integer>absent(),Optional.of(MAX_LOG_SIZE))) {
    Files.deleteIfExists(path);
  }
}
