{
  BuildRuleResolver ruleResolver=new BuildRuleResolver();
  return KeystoreRule.newKeystoreBuilder(new FakeAbstractBuildRuleBuilderParams()).setBuildTarget(BuildTargetFactory.newInstance("//keystores:debug")).setStore("keystores/debug.keystore").setProperties("keystores/debug.keystore.properties").build(ruleResolver);
}
