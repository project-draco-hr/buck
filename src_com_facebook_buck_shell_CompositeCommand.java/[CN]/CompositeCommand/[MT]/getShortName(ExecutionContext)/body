{
  return Joiner.on(" && ").join(Iterables.transform(commands,new Function<Command,String>(){
    @Override public String apply(    Command command){
      return command.getShortName(context);
    }
  }
));
}
