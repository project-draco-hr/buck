{
  String targetString=buildTarget.toString();
  int colonIndex=targetString.indexOf(":");
  return String.format("%s/%s (%s)",targetString.substring(2,colonIndex),BuckConstant.BUILD_RULES_FILE_NAME,targetString.substring(colonIndex));
}
