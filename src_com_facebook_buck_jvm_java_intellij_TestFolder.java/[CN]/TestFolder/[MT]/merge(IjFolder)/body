{
  checkMergeConditions(otherFolder);
  if (otherFolder == this) {
    return this;
  }
  IJFolderFactory factory=otherFolder.getFactory();
  return factory.create(otherFolder.getPath(),getWantsPackagePrefix() || otherFolder.getWantsPackagePrefix(),combineInputs(this,otherFolder));
}
