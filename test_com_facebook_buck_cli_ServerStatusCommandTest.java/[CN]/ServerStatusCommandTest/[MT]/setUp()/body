{
  console=new TestConsole();
  webServer=createStrictMock(WebServer.class);
  command=new ServerStatusCommand(CommandRunnerParamsForTesting.builder().setWebserver(Optional.of(webServer)).setConsole(console).build());
}
