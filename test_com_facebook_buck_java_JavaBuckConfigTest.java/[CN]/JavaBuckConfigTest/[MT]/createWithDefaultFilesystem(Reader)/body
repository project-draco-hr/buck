{
  ProjectFilesystem filesystem=new ProjectFilesystem(temporaryFolder.getRootPath());
  BuildTargetParser parser=new BuildTargetParser();
  BuckConfig raw=BuckConfigTestUtils.createFromReader(reader,filesystem,parser,Platform.detect(),ImmutableMap.copyOf(System.getenv()));
  return new JavaBuckConfig(raw);
}
