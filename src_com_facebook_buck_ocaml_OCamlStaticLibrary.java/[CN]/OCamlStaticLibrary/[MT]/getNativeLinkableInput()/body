{
  NativeLinkableInput.Builder inputBuilder=NativeLinkableInput.builder();
  inputBuilder.addAllArgs(StringArg.from(linkerFlags));
  inputBuilder.addArgs(new SourcePathArg(getResolver(),new BuildTargetSourcePath(ocamlLibraryBuild.getBuildTarget(),OCamlBuildContext.getOutputPath(staticLibraryTarget.getUnflavoredBuildTarget(),true))));
  for (  SourcePath objFile : objFiles) {
    inputBuilder.addArgs(new SourcePathArg(getResolver(),objFile));
  }
  return inputBuilder.build();
}
