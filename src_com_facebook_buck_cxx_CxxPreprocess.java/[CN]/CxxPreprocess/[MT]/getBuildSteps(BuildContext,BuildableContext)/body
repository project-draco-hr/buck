{
  buildableContext.recordArtifact(output);
  return ImmutableList.of(new MkdirStep(output.getParent()),new CxxPreprocessStep(getResolver().getPath(preprocessor),flags,output,getResolver().getPath(input),includeRoots,systemIncludeRoots));
}
