{
  try {
    filesystem.writeLinesToPath(FluentIterable.from(classpathEntries).transform(Functions.toStringFunction()),getClassPathFile());
  }
 catch (  IOException e) {
    e.printStackTrace(context.getStdErr());
    return 1;
  }
  return super.execute(context);
}
