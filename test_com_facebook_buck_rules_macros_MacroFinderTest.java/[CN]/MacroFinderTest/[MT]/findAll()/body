{
  ImmutableMap<String,String> expectedResults=ImmutableMap.of("macro1","","macro2","arg");
  ImmutableList<Pair<String,String>> actualResults=FINDER.findAll(expectedResults.keySet(),"hello world $(macro1) and $(macro2 arg)");
  assertEquals(expectedResults,pairListToMap(actualResults));
}
