def _block_unsafe_function(self, module, name):

    def func(*args, **kwargs):
        raise AttributeError(((('Using function %s is forbidden in the safe version of ' % name) + ('module %s. If you really need to use this function read about ' % module)) + 'allow_unsafe_import()'))
    return func
