{
  if (mConnected) {
    scheduledFuture.cancel(true);
    try {
      mWSClient.stop();
      mConnected=false;
    }
 catch (    Throwable t) {
      Logger.getInstance(this.getClass()).error("Could not disconnect from buck. " + t.getMessage());
    }
  }
}
