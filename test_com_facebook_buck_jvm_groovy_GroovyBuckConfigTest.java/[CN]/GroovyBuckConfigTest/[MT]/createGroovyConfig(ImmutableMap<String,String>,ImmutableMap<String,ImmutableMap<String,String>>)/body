{
  ProjectFilesystem projectFilesystem=new ProjectFilesystem(temporaryFolder.getRootPath());
  BuckConfig config=new BuckConfig(Configs.createConfig(RawConfig.of(rawConfig)),projectFilesystem,Architecture.detect(),Platform.detect(),environment);
  return new GroovyBuckConfig(config);
}
