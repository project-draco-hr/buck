{
  return new DTest(params,new SourcePathResolver(resolver),args.srcs,args.contacts.get(),args.labels.get(),resolver.getAllRules(args.sourceUnderTest.or(ImmutableSortedSet.<BuildTarget>of())),dBuckConfig.getDCompiler());
}
