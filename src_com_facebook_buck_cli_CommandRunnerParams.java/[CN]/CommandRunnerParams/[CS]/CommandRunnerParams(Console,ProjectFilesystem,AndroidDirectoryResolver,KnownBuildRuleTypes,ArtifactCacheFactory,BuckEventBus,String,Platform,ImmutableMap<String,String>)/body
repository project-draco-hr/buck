{
  this(console,projectFilesystem,androidDirectoryResolver,buildRuleTypes,new CachingBuildEngine(),artifactCacheFactory,eventBus,new Parser(projectFilesystem,buildRuleTypes,console,environment,pythonInterpreter,ImmutableSet.<Pattern>of(),new RuleKeyBuilderFactory(){
    @Override public Builder newInstance(    BuildRule buildRule){
      return RuleKey.builder(buildRule,new NullFileHashCache());
    }
  }
),platform,environment);
}
