{
  String[] parts=id.split(":");
  BuildRule rule=actionGraph.findBuildRuleByTarget(BuildTarget.builder(parts[0],parts[1]).build());
  Preconditions.checkNotNull(rule,"No rule for %s",id);
  return rule;
}
