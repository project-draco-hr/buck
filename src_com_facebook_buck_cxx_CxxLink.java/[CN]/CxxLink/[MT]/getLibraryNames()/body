{
  return FluentIterable.from(libraries).transform(new Function<Path,String>(){
    @Override public String apply(    Path fileName){
      return MorePaths.stripPathPrefixAndExtension(fileName,"lib");
    }
  }
).filter(MoreStrings.NON_EMPTY).toSet();
}
