{
  return FluentIterable.from(libraries).transform(new Function<Path,Path>(){
    @Nullable @Override public Path apply(    Path input){
      return input.getParent();
    }
  }
).filter(notNull()).toSet();
}
