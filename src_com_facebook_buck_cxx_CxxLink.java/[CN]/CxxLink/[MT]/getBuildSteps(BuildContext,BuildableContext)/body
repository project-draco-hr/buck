{
  buildableContext.recordArtifact(output);
  return ImmutableList.of(new MkdirStep(output.getParent()),new CxxLinkStep(linker.getCommandPrefix(getResolver()),output,args,frameworkRoots,getLibrarySearchDirectories(),getLibraryNames()),new FileScrubberStep(output,linker.getScrubbers(context.getProjectRoot())));
}
