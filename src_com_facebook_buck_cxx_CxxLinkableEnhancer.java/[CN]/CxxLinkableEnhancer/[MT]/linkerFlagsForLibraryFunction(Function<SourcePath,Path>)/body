{
  return new Function<FrameworkPath,String>(){
    @Override public String apply(    FrameworkPath input){
      return "-l" + MorePaths.stripPathPrefixAndExtension(input.getFileName(resolver),"lib");
    }
  }
;
}
