{
  super(params,resolver);
  this.isBuildTimeDownloadingOk=isBuildTimeDownloadingOk;
  this.uri=uri;
  this.sha1=sha1;
  this.downloader=downloader;
  output=BuildTargets.getGenPath(params.getBuildTarget(),String.format("%%s/%s",out));
}
