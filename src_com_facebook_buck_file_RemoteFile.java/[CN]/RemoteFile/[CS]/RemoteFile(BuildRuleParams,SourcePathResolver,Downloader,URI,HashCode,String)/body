{
  super(params,resolver);
  this.uri=uri;
  this.sha1=sha1;
  this.downloader=downloader;
  output=BuildTargets.getGenPath(getProjectFilesystem(),params.getBuildTarget(),String.format("%%s/%s",out));
}
