{
  BuckConfig buckConfig=createMock(BuckConfig.class);
  expect(buckConfig.getMinimumNdkVersion()).andReturn(Optional.of(minNdkVersion));
  expect(buckConfig.getMaximumNdkVersion()).andReturn(Optional.of(maxNdkVersion));
  return buckConfig;
}
