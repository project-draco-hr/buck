{
  BuildTarget dummyOmnibusTarget=BuildTarget.builder(params.getBuildTarget()).addFlavors(DUMMY_OMNIBUS_FLAVOR).build();
  String omnibusSoname=getOmnibusSoname(cxxPlatform);
  ruleResolver.addToIndex(CxxLinkableEnhancer.createCxxLinkableSharedBuildRule(cxxPlatform,params,pathResolver,dummyOmnibusTarget,BuildTargets.getGenPath(dummyOmnibusTarget,"%s").resolve(omnibusSoname),omnibusSoname,extraLdflags));
  return new BuildTargetSourcePath(dummyOmnibusTarget);
}
