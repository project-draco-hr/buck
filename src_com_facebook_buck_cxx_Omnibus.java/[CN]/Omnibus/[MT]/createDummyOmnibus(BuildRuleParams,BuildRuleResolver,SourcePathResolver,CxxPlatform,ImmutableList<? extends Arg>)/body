{
  BuildTarget dummyOmnibusTarget=BuildTarget.builder(params.getBuildTarget()).addFlavors(DUMMY_OMNIBUS_FLAVOR).build();
  String omnibusSoname=getOmnibusSoname(cxxPlatform);
  ruleResolver.addToIndex(CxxLinkableEnhancer.createCxxLinkableSharedBuildRule(cxxPlatform,params,ruleResolver,pathResolver,dummyOmnibusTarget,BuildTargets.getGenPath(dummyOmnibusTarget,"%s").resolve(omnibusSoname),Optional.of(omnibusSoname),extraLdflags));
  return new BuildTargetSourcePath(dummyOmnibusTarget);
}
