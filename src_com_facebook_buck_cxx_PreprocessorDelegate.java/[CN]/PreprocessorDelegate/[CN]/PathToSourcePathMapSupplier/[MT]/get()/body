{
  try {
    return Multimaps.index(CxxHeaders.concat(includes).getFullNameToPathMap().values(),Functions.compose(Functions.toStringFunction(),resolver.getRelativePathFunction()));
  }
 catch (  CxxHeaders.ConflictingHeadersException e) {
    throw new RuntimeException(e);
  }
}
