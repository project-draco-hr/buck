{
  String logger=getRecord().getLoggerName();
  ImmutableMap<String,String> traits=ImmutableMap.of("severity",getRecord().getLevel().toString(),"logger",logger != null ? logger : "None");
  return traits;
}
