{
  ZipSplitter splitter=new ZipSplitter(inFiles,outPrimary,outSecondaryDir,secondaryPattern,zipSizeSoftLimit,zipSizeHardLimit,requiredInPrimaryZip,dexSplitStrategy,canaryStrategy);
  return splitter.execute();
}
