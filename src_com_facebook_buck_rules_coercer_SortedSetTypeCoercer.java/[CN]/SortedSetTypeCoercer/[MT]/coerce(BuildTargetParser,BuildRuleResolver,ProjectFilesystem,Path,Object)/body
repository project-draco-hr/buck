{
  final SortedSet<T> builder=Sets.newTreeSet();
  fillSortedSet(buildTargetParser,buildRuleResolver,filesystem,pathRelativeToProjectRoot,builder,object);
  return ImmutableSortedSet.copyOf(builder);
}
