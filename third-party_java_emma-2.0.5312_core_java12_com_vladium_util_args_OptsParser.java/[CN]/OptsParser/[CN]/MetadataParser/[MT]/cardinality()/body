{
  final Token result=accept(Token.CARD_ID);
  if ("0".equals(result.getValue()))   return OptDef.C_ZERO;
 else   if ("1".equals(result.getValue()))   return OptDef.C_ONE;
 else   return OptDef.C_ZERO_OR_ONE;
}
