{
  Path pathToOutput=getPathToOutput();
  MkdirStep mkOutputDirStep=new MkdirStep(pathToOutput.getParent());
  JarDirectoryStep mergeOutputsStep=new JarDirectoryStep(pathToOutput,toOutputPaths(traversedDeps.builtinDeps),null,null);
  return ImmutableList.of(mkOutputDirStep,mergeOutputsStep);
}
