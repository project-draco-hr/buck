{
  super(params,resolver);
  this.assembly=assembly;
  Path resolvedPath=resolver.getResolvedPath(assembly);
  this.output=BuildTargets.getGenPath(params.getBuildTarget(),"%s").resolve(resolvedPath.getFileName());
}
