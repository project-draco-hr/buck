{
  step=new GenerateCodeCoverageReportStep(SOURCE_DIRECTORIES,CLASSES_DIRECTORIES,Paths.get(OUTPUT_DIRECTORY),CoverageReportFormat.HTML,"TitleFoo");
  context=createMock(ExecutionContext.class);
  expect(context.getProjectFilesystem()).andReturn(new ProjectFilesystem(Paths.get("."))).anyTimes();
  replay(context);
}
