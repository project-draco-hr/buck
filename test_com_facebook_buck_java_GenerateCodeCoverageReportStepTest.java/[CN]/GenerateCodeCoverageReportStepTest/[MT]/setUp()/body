{
  ProjectFilesystem filesystem=new ProjectFilesystem(Paths.get("."));
  step=new GenerateCodeCoverageReportStep(filesystem.getRootPath(),SOURCE_DIRECTORIES,CLASSES_DIRECTORIES,Paths.get(OUTPUT_DIRECTORY),CoverageReportFormat.HTML,"TitleFoo");
  context=createMock(ExecutionContext.class);
  expect(context.getProjectFilesystem()).andReturn(filesystem).anyTimes();
  replay(context);
}
