{
  if (isLibrary) {
    if (!isBytecode) {
      return OCamlUtil.getExtensionVariants(output,OCamlCompilables.OCAML_A,OCamlCompilables.OCAML_CMXA);
    }
 else {
      return ImmutableSet.of(output);
    }
  }
 else {
    return ImmutableSet.of(output);
  }
}
