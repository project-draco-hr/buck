{
  CxxSourceRuleFactory factory=new CxxSourceRuleFactory(params,resolver,new SourcePathResolver(resolver),cxxPlatform,cxxPreprocessorInputs,compilerFlags,prefixHeader);
  return factory.createInferCaptureBuildRules(sources,picType,inferTools,sourceFilter);
}
