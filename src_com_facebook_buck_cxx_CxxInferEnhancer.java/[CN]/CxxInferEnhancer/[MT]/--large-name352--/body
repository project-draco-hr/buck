{
  CxxSourceRuleFactory factory=CxxSourceRuleFactory.of(params,resolver,new SourcePathResolver(resolver),cxxPlatform,cxxPreprocessorInputs,compilerFlags,prefixHeader,picType);
  return factory.createInferCaptureBuildRules(sources,inferTools,sourceFilter);
}
