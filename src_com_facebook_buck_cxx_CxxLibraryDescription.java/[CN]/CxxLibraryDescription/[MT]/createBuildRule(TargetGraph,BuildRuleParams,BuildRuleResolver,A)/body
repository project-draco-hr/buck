{
  TypeAndPlatform typeAndPlatform=getTypeAndPlatform(params.getBuildTarget(),cxxPlatforms);
  return createBuildRule(targetGraph,params,resolver,args,typeAndPlatform,args.linkStyle,Optional.<SourcePath>absent(),ImmutableSet.<BuildRule>of());
}
