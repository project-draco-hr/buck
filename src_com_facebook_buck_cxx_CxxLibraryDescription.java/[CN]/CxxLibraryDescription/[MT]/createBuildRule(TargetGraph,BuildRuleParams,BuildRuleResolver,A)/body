{
  TypeAndPlatform typeAndPlatform=getTypeAndPlatform(params.getBuildTarget(),cxxPlatforms);
  return createBuildRule(targetGraph,params,resolver,args,typeAndPlatform,Optional.<Linker.LinkableDepType>absent(),Optional.<SourcePath>absent());
}
