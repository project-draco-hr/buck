{
  Console console=new TestConsole();
  BuckEventBus eventBus=BuckEventBusFactory.newInstance();
  InstallCommand command=getCommand(args);
  return new AdbHelper(command.adbOptions(),command.targetDeviceOptions(),executionContext,console,eventBus,buckConfig){
    @Override protected boolean isDeviceTempWritable(    IDevice device,    String name){
      return true;
    }
  }
;
}
