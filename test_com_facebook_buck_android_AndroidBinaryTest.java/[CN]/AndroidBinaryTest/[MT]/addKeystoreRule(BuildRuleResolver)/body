{
  BuildTarget keystoreTarget=BuildTargetFactory.newInstance("//keystore:debug");
  return (Keystore)KeystoreBuilder.createBuilder(keystoreTarget).setStore(new TestSourcePath("keystore/debug.keystore")).setProperties(new TestSourcePath("keystore/debug.keystore.properties")).build(ruleResolver);
}
