{
  Preconditions.checkArgument(path.startsWith("/"),"Request URI must only be a path started with / instead of [%s].",path);
  requestBuilder.url(server.resolve(path).toURL());
  return client.newCall(requestBuilder.build()).execute();
}
