{
class Dto implements ConstructorArg {
    public long number;
  }
  Dto dto=new Dto();
  marshaller.populate(ruleResolver,filesystem,buildRuleFactoryParams(ImmutableMap.<String,Object>of("number",42L)),dto);
  assertEquals(42,dto.number);
}
