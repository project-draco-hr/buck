{
class Dto {
    public Optional<Integer> number;
  }
  Dto dto=new Dto();
  marshaller.populate(filesystem,buildRuleFactoryParams(ImmutableMap.<String,Object>of("number",0)),dto);
  assertTrue(dto.number.isPresent());
  assertEquals(Optional.of(0),dto.number);
}
