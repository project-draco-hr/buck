{
class Dto implements ConstructorArg {
    public Set<String> strings;
  }
  Dto dto=new Dto();
  marshaller.populate(ruleResolver,filesystem,buildRuleFactoryParams(ImmutableMap.<String,Object>of("strings",ImmutableSet.of(true,false))),dto);
}
