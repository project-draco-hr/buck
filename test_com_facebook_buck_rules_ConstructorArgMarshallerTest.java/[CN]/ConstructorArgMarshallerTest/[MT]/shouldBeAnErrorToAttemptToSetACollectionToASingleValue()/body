{
class Dto {
    public Set<String> strings;
  }
  Dto dto=new Dto();
  marshaller.populate(createCellRoots(filesystem),filesystem,buildRuleFactoryParams(),dto,ImmutableSet.<BuildTarget>builder(),ImmutableSet.<VisibilityPattern>builder(),ImmutableMap.<String,Object>of("strings","isn't going to happen"));
}
