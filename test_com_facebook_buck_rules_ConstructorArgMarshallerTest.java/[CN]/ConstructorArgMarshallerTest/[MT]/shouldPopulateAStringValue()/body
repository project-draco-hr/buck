{
class Dto {
    public String name;
  }
  Dto dto=new Dto();
  marshaller.populate(ruleResolver,filesystem,buildRuleFactoryParams(ImmutableMap.<String,Object>of("name","cheese")),dto);
  assertEquals("cheese",dto.name);
}
