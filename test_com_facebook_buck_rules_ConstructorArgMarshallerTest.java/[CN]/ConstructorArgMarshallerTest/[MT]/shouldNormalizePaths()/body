{
class Dto implements ConstructorArg {
    public Path path;
  }
  Dto dto=new Dto();
  marshaller.populate(ruleResolver,filesystem,buildRuleFactoryParams(ImmutableMap.<String,Object>of("path","./bar/././fish.txt")),dto);
  assertEquals(basePath.resolve("bar/fish.txt").normalize(),dto.path);
}
