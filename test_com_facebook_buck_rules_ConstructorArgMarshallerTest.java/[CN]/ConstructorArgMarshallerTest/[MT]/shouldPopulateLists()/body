{
class Dto {
    public List<String> list;
  }
  Dto dto=new Dto();
  marshaller.populate(ruleResolver,filesystem,buildRuleFactoryParams(ImmutableMap.<String,Object>of("list",ImmutableList.of("alpha","beta"))),dto);
  assertEquals(ImmutableList.of("alpha","beta"),dto.list);
}
