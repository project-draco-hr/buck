{
  BuildTarget plainTarget=target.getUnflavoredTarget();
  if (isLibrary) {
    return getArchiveOutputPath(plainTarget);
  }
 else {
    return BuildTargets.getBinPath(plainTarget,"%s/" + plainTarget.getShortNameAndFlavorPostfix() + ".opt");
  }
}
