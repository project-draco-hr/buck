{
  TestSelectorList selectomatic=new TestSelectorList.Builder().addRawSelectors("!com.example.clown.Car").build();
  assertFalse(selectomatic.isIncluded(CAR_DOORS));
  assertTrue(selectomatic.defaultIsInclusive);
}
