{
  TestSelectorList selectomatic=TestSelectorList.buildFrom(ImmutableList.of("com.example.clown.Car"));
  assertTrue(selectomatic.isIncluded(CAR_DOORS));
  assertFalse(selectomatic.defaultIsInclusive);
}
