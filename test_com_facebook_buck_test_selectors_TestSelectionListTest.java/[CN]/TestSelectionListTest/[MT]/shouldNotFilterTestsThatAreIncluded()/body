{
  TestSelectorList selectomatic=new TestSelectorList.Builder().addRawSelectors(CAR_CLASS).build();
  assertTrue(selectomatic.isIncluded(CAR_DOORS_TEST));
  assertFalse(selectomatic.defaultIsInclusive);
}
