{
  ImmutableList.Builder<Step> commands=ImmutableList.builder();
  AndroidTransitiveDependencies transitiveDependencies=transitiveDependencyGraph.findDependencies(getAndroidResourceDepsInternal(context.getDependencyGraph()));
  if (skeletonFile.isPresent() && manifestFile.isPresent()) {
    commands.add(new GenerateManifestStep(skeletonFile.get(),manifestFile.get(),transitiveDependencies.manifestFiles));
  }
  return commands.build();
}
