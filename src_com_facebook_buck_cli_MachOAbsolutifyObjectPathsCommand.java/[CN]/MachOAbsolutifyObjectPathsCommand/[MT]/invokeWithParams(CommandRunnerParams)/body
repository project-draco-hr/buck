{
  try (RandomAccessFile file=new RandomAccessFile(getOutput().toFile(),"rw")){
    ObjectPathsAbsolutifier updater=new ObjectPathsAbsolutifier(file,getOldCompDir(),getUpdatedCompDir(),params.getCell().getFilesystem());
    updater.updatePaths();
  }
   return 0;
}
