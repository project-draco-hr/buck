{
  if (isAsset) {
    collector.addNativeLibAssetsDirectory(getBuildTarget(),new PathSourcePath(getProjectFilesystem(),getLibraryPath()));
  }
 else {
    collector.addNativeLibsDirectory(getBuildTarget(),new PathSourcePath(getProjectFilesystem(),getLibraryPath()));
  }
}
