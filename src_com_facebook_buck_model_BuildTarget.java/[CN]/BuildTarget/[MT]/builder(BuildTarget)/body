{
  return ImmutableBuildTarget.builder().setRepository(buildTarget.getRepository()).setBaseName(buildTarget.getBaseName()).setShortName(buildTarget.getShortName()).addAllFlavors(buildTarget.getFlavors());
}
