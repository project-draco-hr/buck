{
  if (!(o instanceof BuildTarget)) {
    return false;
  }
  BuildTarget that=(BuildTarget)o;
  return Objects.equal(this.buildFile,that.buildFile) && Objects.equal(this.baseName,that.baseName) && Objects.equal(this.shortName,that.shortName);
}
