{
  createAndroidBinaryRuleAndTestCopyNativeLibraryCommand(ImmutableSet.of("arm","x86"),"path/to/source/libs.zip","path/to/destination/",ImmutableList.of("unzip -q -d " + nativeOutDir + "path/to/source path/to/source/libs.zip","bash -c [ -d " + nativeOutDir + "path/to/source/armeabi ] && "+ "cp -R "+ nativeOutDir+ "path/to/source/armeabi path/to/destination/ || exit 0","bash -c [ -d " + nativeOutDir + "path/to/source/x86 ] && "+ "cp -R "+ nativeOutDir+ "path/to/source/x86 path/to/destination/ || exit 0"));
}
