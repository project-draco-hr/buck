{
  createAndroidBinaryRuleAndTestCopyNativeLibraryCommand(ImmutableSet.of("armv7"),"path/to/source/libs.zip","path/to/destination/",ImmutableList.of("unzip -q -d " + nativeOutDir + "path/to/source path/to/source/libs.zip","bash -c " + "[ -d " + nativeOutDir + "path/to/source/armeabi-v7a ] && "+ "cp -R "+ nativeOutDir+ "path/to/source/armeabi-v7a path/to/destination/ || "+ "exit 0"));
}
