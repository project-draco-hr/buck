{
  this.enhancer=new ReactNativeLibraryGraphEnhancer(buckConfig);
  this.packager=new Supplier<SourcePath>(){
    @Override public SourcePath get(){
      return buckConfig.getPackager();
    }
  }
;
}
