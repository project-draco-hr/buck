{
  params.getConsole().getStdOut().println("buck audit <cmd>");
  for (  Map.Entry<String,? extends AbstractCommandRunner<?>> entry : getAuditCommands().entrySet()) {
    params.getConsole().getStdOut().println("  " + entry.getKey() + " - "+ entry.getValue().getUsageIntro());
  }
}
