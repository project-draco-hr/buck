{
  try (Fixture fixture=createMultiThreadedFixture("parse_rule_with_bad_dependency")){
    Cell cell=fixture.getCell();
    expectedException.expect(BuildFileParseException.class);
    expectedException.expectMessage(Matchers.stringContainsInOrder("Parse error for build file","No such file or directory"));
    fixture.getParsePipeline().getAllTargetNodes(cell,cell.getFilesystem().resolve("no/such/file/BUCK"));
  }
 }
