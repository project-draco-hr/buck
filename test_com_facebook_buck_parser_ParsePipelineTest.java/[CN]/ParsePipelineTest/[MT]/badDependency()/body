{
  try (Fixture fixture=new Fixture("parse_rule_with_bad_dependency")){
    Cell cell=fixture.getCell();
    fixture.getParsePipeline().getTargetNode(cell,BuildTargetFactory.newInstance(cell.getFilesystem(),"//:base"));
  }
 }
