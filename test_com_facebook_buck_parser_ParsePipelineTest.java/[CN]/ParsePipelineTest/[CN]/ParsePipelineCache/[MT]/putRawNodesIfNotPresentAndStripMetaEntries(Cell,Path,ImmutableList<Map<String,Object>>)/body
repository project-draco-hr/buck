{
  rawNodes=FluentIterable.from(rawNodes).filter(new Predicate<Map<String,Object>>(){
    @Override public boolean apply(    Map<String,Object> input){
      return input.containsKey("name");
    }
  }
).toList();
  if (!rawNodeMap.containsKey(buildFile)) {
    rawNodeMap.put(buildFile,rawNodes);
  }
  return rawNodeMap.get(buildFile);
}
