{
  if (!(o instanceof BuckEvent)) {
    return false;
  }
  BuckEvent that=(BuckEvent)o;
  return eventsArePair(that) && getThreadId() == that.getThreadId() && Objects.equals(getClass(),that.getClass());
}
