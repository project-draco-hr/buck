{
  if (job.isSetDebug() && job.getDebug().getLogBook().size() > 0) {
    return Optional.of(job.getDebug().getLogBook().get(job.getDebug().getLogBook().size() - 1).getName());
  }
  return Optional.absent();
}
