{
  if (combinedProject) {
    return generateCombinedProject(workspaceName,outputDirectory,workspaceGenerator,groupedTests,targetsInRequiredProjects,buildTargetToPbxTargetMapBuilder,targetToProjectPathMapBuilder,targetToBuildWithBuck);
  }
 else {
    return generateProject(projectGenerators,workspaceGenerator,groupedTests,targetsInRequiredProjects,buildTargetToPbxTargetMapBuilder,targetToProjectPathMapBuilder,targetToBuildWithBuck);
  }
}
