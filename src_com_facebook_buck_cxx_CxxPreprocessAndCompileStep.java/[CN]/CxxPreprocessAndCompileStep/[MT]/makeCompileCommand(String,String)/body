{
  return ImmutableList.<String>builder().addAll(compilerCommand.get()).add("-x",inputLanguage).add("-c").addAll(inputType.isPreprocessable() ? getDepFileArgs(getDepTemp()) : ImmutableList.<String>of()).add(inputFileName).add("-o").add(output.toString()).build();
}
