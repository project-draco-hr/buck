{
switch (operation) {
case COMPILE:
case COMPILE_MUNGE_DEBUGINFO:
    return makeCompileCommand(input.toString(),inputType.getLanguage(),inputType.isPreprocessable(),false);
case PREPROCESS:
  return makePreprocessCommand(false);
default :
throw new RuntimeException("invalid operation type");
}
}
