{
switch (operation) {
case COMPILE:
case COMPILE_MUNGE_DEBUGINFO:
    return makeCompileCommand(input.toString(),inputType.getLanguage(),inputType.isPreprocessable());
case PREPROCESS:
  return makePreprocessCommand();
default :
throw new RuntimeException("invalid operation type");
}
}
