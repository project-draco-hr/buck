{
  if (allowColorsInDiagnostics && flagsForColorDiagnostics.isPresent()) {
    return ImmutableList.<String>builder().addAll(arguments).addAll(flagsForColorDiagnostics.get()).build();
  }
 else {
    return arguments;
  }
}
