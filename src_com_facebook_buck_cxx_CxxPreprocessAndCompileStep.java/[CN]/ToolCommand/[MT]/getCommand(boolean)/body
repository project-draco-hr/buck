{
  if (allowColorsInDiagnostics && colorSupport.isPresent()) {
    return ImmutableList.<String>builder().addAll(command).addAll(colorSupport.get().getArgsForColorsInOutput()).build();
  }
 else {
    return command;
  }
}
