{
  if (allowColorsInDiagnostics && flagsForColorDiagnostics.isPresent()) {
    return ImmutableList.<String>builder().addAll(command).addAll(flagsForColorDiagnostics.get()).build();
  }
 else {
    return command;
  }
}
