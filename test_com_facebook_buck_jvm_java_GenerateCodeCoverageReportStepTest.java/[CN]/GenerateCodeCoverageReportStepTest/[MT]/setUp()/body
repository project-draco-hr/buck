{
  filesystem=new ProjectFilesystem(Paths.get(".").toAbsolutePath());
  step=new GenerateCodeCoverageReportStep(filesystem,SOURCE_DIRECTORIES,CLASSES_DIRECTORIES,Paths.get(OUTPUT_DIRECTORY),CoverageReportFormat.HTML,"TitleFoo");
  context=createMock(ExecutionContext.class);
  replay(context);
}
