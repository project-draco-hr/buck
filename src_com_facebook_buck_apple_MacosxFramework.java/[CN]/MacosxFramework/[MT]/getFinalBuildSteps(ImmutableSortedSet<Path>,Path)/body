{
  if (files.isEmpty()) {
    return ImmutableList.of();
  }
 else {
    return ImmutableList.<Step>of(new CompilerStep(getCompiler(),true,files,outputFile,false,ImmutableSortedSet.<Path>of(),ImmutableList.<String>of()));
  }
}
