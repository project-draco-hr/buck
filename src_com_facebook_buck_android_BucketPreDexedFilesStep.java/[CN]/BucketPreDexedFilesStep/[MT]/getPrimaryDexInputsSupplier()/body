{
  return new Supplier<Set<Path>>(){
    @Override public Set<Path> get(){
      return Preconditions.checkNotNull(primaryDexInputs,"Trying to read primary dex inputs before BucketPreDexedFilesStep finished.");
    }
  }
;
}
