{
  assertThat(debugPathSanitizer.sanitize(Optional.of(Paths.get("/project/root")),"a string that mentions the /project/root somewhere",true),equalTo("a string that mentions the ./////////////////////////////////////// somewhere"));
}
