{
  assertThat(debugPathSanitizer.sanitize(Optional.of(Paths.get("/project/root")),"-I/another/path/with/subdirectories/something",true),equalTo("-IOTHER_NAME_WITH_SUFFIX///////////////////something"));
}
