{
  assertEquals(Optional.<Path>absent(),MorePaths.searchPathsForExecutable(Paths.get("bar"),ImmutableList.of(Paths.get("baz","foo")),Functions.<Path>forPredicate(Predicates.<Path>alwaysFalse())));
}
