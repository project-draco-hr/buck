{
  Optional<Path> toolPath=MoreFiles.searchPathsForExecutable(Paths.get(tool),toolSearchPaths,pathIsExecutableChecker);
  if (toolPath.isPresent()) {
    return Optional.<SourcePath>of(new PathSourcePath(toolPath.get()));
  }
 else {
    return Optional.<SourcePath>absent();
  }
}
