{
  BuildTarget target=BuildTargetFactory.newInstance("//:rule");
  BuildRuleResolver resolver=new BuildRuleResolver();
  LocationMacroExpander macroExpander=new LocationMacroExpander();
  assertThat(macroExpander.extractRuleKeyAppendables(target,resolver,"//some/other:rule"),Matchers.<Object>equalTo(new BuildTargetSourcePath(BuildTargetFactory.newInstance("//some/other:rule"))));
}
