{
  return Futures.transformAsync(delegate.close(),new AsyncFunction<Void,Void>(){
    @Override @Nullable public ListenableFuture<Void> apply(    Void input) throws Exception {
      resource.close();
      return null;
    }
  }
);
}
