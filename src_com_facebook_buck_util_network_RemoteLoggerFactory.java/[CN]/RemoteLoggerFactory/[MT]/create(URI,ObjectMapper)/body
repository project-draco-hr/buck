{
  try {
    return new HttpPutLogger(new BlockingHttpEndpoint(uri.toString(),MAX_PARALLEL_REQUESTS,BlockingHttpEndpoint.DEFAULT_COMMON_TIMEOUT_MS),objectMapper);
  }
 catch (  MalformedURLException e) {
    throw new HumanReadableException(e,"Don't know how to upload logs to %s",uri);
  }
}
