{
switch (field) {
case TYPE:
    if (value == null) {
      unsetType();
    }
 else {
      setType((LogRequestType)value);
    }
  break;
case SCRIBE_DATA:
if (value == null) {
  unsetScribeData();
}
 else {
  setScribeData((ScribeData)value);
}
break;
}
}
