{
  return String.format(Locale.US,template,(Object[])FluentIterable.from(ImmutableList.copyOf(time)).transform(new Function<Double,String>(){
    @Override public String apply(    Double input){
      return String.format(Locale.US,"%01.1fs",input);
    }
  }
).toArray(String.class));
}
