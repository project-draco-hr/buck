{
  Process process=createMock(Process.class);
  expect(process.getInputStream()).andReturn(new ByteArrayInputStream(output.getBytes(Charsets.US_ASCII)));
  expect(process.getOutputStream()).andReturn(new ByteArrayOutputStream());
  return process;
}
