{
  List<Object> query=WatchmanWatcher.createQuery("path/to/repo",Optional.of("project"),"uuid",Lists.<Path>newArrayList(),Lists.<String>newArrayList(),ImmutableSet.of(Watchman.Capability.DIRNAME));
  assertThat(query,hasItem(hasEntry("relative_root","project")));
}
