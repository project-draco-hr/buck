{
  CxxTestStep step=new CxxTestStep(filesystem,ImmutableList.of("true"),ImmutableMap.<String,String>of(),exitCode,output,Optional.<Long>absent());
  step.execute(context);
  assertSame(0,readExitCode(exitCode));
  assertContents(output,"");
}
