{
  CxxTestStep step=new CxxTestStep(ImmutableList.of("true"),ImmutableMap.<String,String>of(),exitCode,output);
  step.execute(context);
  assertSame(0,readExitCode(exitCode));
  assertContents(output,"");
}
