{
  CxxTestStep step=new CxxTestStep(ImmutableList.of("false"),ImmutableMap.<String,String>of(),exitCode,output);
  step.execute(context);
  assertSame(1,readExitCode(exitCode));
  assertContents(output,"");
}
