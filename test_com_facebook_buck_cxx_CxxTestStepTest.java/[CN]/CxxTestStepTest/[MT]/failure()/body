{
  CxxTestStep step=new CxxTestStep(filesystem,ImmutableList.of("false"),ImmutableMap.<String,String>of(),exitCode,output,Optional.<Long>absent());
  step.execute(context);
  assertSame(1,readExitCode(exitCode));
  assertContents(output,"");
}
