{
  CxxTestStep step=new CxxTestStep(filesystem,ImmutableList.of("sleep","1"),ImmutableMap.<String,String>of(),exitCode,output,Optional.of(10L));
  expectedException.expectMessage("Timed out running test command");
  step.execute(context);
}
