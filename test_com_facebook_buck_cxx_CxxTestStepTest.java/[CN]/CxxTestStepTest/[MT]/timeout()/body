{
  CxxTestStep step=new CxxTestStep(filesystem,ImmutableList.of("sleep","10"),ImmutableMap.<String,String>of(),exitCode,output,Optional.of(10L));
  expectedException.expectMessage("Timed out after 10 ms running test command");
  step.execute(context);
}
