{
  final int length=in.readInt();
  if (length == NULL_ARRAY_LENGTH)   return null;
 else {
    final boolean[] result=new boolean[length];
    for (int i=length; --i >= 0; ) {
      result[i]=in.readBoolean();
    }
    return result;
  }
}
