{
  BuildTarget buildTarget=BuildTargetFactory.newInstance("//example/base:one#two");
  assertEquals(BuildTarget.builder("//example/base","one").addFlavors(ImmutableFlavor.of("two")).build(),buildTarget);
}
