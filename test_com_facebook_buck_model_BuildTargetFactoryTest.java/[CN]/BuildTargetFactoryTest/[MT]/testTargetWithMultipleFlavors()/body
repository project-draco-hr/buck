{
  BuildTarget buildTarget=BuildTargetFactory.newInstance("//example/base:shortName#one,two,three");
  assertEquals(BuildTarget.builder("//example/base","shortName").addFlavor("one").addFlavor("two").addFlavor("three").build(),buildTarget);
}
