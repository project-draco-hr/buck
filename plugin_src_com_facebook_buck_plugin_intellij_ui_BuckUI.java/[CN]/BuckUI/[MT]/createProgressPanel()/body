{
  buckProgressPanel=new BuckProgressPanel();
  Content content=ContentFactory.SERVICE.getInstance().createContent(buckProgressPanel.getPanel(),buckProgressPanel.DISPLAY_NAME,false);
  messageWindow.getContentManager().addContent(content);
}
