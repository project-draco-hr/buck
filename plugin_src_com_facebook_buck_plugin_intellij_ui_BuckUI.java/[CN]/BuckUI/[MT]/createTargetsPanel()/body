{
  buckTargetsPanel=new BuckTargetsPanel(component);
  Content content=ContentFactory.SERVICE.getInstance().createContent(buckTargetsPanel.getPanel(),buckTargetsPanel.DISPLAY_NAME,false);
  toolWindow.getContentManager().addContent(content);
}
