{
  return Iterables.transform(getRecursiveTargetNodeDependenciesOfTypes(targetGraph,mode,targetNode,type),new Function<TargetNode<?>,TargetNode<T>>(){
    @Override public TargetNode<T> apply(    TargetNode<?> input){
      return (TargetNode<T>)input;
    }
  }
);
}
