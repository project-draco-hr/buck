{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"project_with_no_alias",temporaryFolder);
  workspace.setUp();
  ProcessResult result=workspace.runBuckCommand("build");
  result.assertExitCode("buck build should exit with an error.",1);
  assertEquals("`buck build` should display an error message if no targets are provided.","BUILD FAILED: Must specify at least one build target.\n",result.getStderr());
}
