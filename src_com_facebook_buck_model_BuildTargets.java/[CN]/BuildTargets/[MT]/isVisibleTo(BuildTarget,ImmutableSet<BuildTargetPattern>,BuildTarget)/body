{
  if (target.getCellPath().equals(other.getCellPath()) && target.getBaseName().equals(other.getBaseName())) {
    return true;
  }
  for (  BuildTargetPattern pattern : visibilityPatterns) {
    if (pattern.apply(other)) {
      return true;
    }
  }
  return false;
}
