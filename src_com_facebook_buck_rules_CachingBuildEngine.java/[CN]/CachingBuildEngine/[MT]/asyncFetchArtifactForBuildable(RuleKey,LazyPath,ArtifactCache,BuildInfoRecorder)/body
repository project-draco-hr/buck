{
  return networkExecutor.submit(new Callable<CacheResult>(){
    @Override public CacheResult call() throws Exception {
      return buildInfoRecorder.fetchArtifactForBuildable(ruleKey,lazyZipPath,artifactCache);
    }
  }
);
}
