{
  this.service=service;
  this.fileHashCache=fileHashCache;
  this.buildMode=buildMode;
  this.depFiles=depFiles;
  this.ruleKeyFactories=ImmutableMap.of(filesystem,new RuleKeyFactories(inputBasedRuleKeyBuilderFactory,abiRuleKeyBuilderFactory,depFileRuleKeyBuilderFactory));
}
