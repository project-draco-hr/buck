{
  Thread waiter=new Thread(new Runnable(){
    @Override public void run(){
      try {
        process.waitFor();
      }
 catch (      InterruptedException e) {
      }
    }
  }
);
  waiter.join(millis);
  waiter.interrupt();
  waiter.join();
}
