{
  return Iterators.transform(depsForShortName.keySet().iterator(),new Function<String,DepsForRule>(){
    @Override public DepsForRule apply(    String shortName){
      return new DepsForRule(shortName,depsForShortName.get(shortName));
    }
  }
);
}
