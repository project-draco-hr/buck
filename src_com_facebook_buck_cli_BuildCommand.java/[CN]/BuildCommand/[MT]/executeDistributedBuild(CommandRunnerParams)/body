{
  DistributedBuild build=new DistributedBuild(new DistBuildService(new DistBuildConfig(params.getBuckConfig()),params.getBuckEventBus()),params.getBuckEventBus());
  return build.executeAndPrintFailuresToEventBus();
}
