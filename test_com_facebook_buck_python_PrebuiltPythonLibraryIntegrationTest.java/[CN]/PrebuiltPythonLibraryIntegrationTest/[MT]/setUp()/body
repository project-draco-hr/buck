{
  workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"prebuilt_package",tmp);
  workspace.setUp();
  String version=new PythonBuckConfig(FakeBuckConfig.builder().build(),new ExecutableFinder()).getPythonEnvironment(new ProcessExecutor(new Console(Verbosity.SILENT,new CapturingPrintStream(),new CapturingPrintStream(),Ansi.withoutTty()))).getPythonVersion().getVersionString().substring("Python ".length());
  if (!version.equals("2.6")) {
    workspace.move("dist/package-0.1-py2.6.egg","dist/package-0.1-py" + version + ".egg");
  }
}
