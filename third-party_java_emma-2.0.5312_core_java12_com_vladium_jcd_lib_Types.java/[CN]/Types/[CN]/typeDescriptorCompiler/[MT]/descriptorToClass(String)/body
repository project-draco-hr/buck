{
  char first=typedescriptor.charAt(0);
  if (first == '[')   return arrayOf(typedescriptor.substring(1));
 else   if (first == 'L')   return Class.forName(vmNameToJavaName(typedescriptor.substring(1,typedescriptor.length() - 1)));
 else {
    return primitive(first);
  }
}
