{
  return GoDescriptors.createGoLibraryRule(params,resolver,goBuckConfig,args.packageName.transform(MorePaths.TO_PATH).or(goBuckConfig.getDefaultPackageName(params.getBuildTarget())),args.srcs,args.compilerFlags.or(ImmutableList.<String>of()),args.tests.get());
}
