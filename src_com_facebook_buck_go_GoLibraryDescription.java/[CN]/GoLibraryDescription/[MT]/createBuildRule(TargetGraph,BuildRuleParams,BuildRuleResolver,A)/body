{
  return GoDescriptors.createGoLibraryRule(params,resolver,goBuckConfig,args.packageName.transform(MorePaths.TO_PATH).or(params.getBuildTarget().getBasePath()),args.srcs,args.compilerFlags.or(ImmutableList.<String>of()),args.tests.get());
}
