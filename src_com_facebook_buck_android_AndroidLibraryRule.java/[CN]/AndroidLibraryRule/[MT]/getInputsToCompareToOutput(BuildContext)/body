{
  if (manifestFile.isPresent()) {
    return ImmutableList.<String>builder().addAll(super.getInputsToCompareToOutput(context)).add(manifestFile.get()).build();
  }
 else {
    return super.getInputsToCompareToOutput(context);
  }
}
