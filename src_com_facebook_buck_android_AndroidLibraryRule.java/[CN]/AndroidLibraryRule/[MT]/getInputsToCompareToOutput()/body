{
  if (manifestFile.isPresent()) {
    return ImmutableList.<String>builder().addAll(super.getInputsToCompareToOutput()).add(manifestFile.get().toString()).build();
  }
 else {
    return super.getInputsToCompareToOutput();
  }
}
