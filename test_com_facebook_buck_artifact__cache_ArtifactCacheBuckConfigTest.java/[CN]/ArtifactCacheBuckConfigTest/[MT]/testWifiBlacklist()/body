{
  ArtifactCacheBuckConfig config=createFromText("[cache]","dir = http","blacklisted_wifi_ssids = yolocoaster");
  assertThat(config.isWifiUsableForDistributedCache(Optional.of("yolocoaster")),Matchers.is(false));
  assertThat(config.isWifiUsableForDistributedCache(Optional.of("swagtastic")),Matchers.is(true));
  config=createFromText("[cache]","dir = http");
  assertThat(config.isWifiUsableForDistributedCache(Optional.of("yolocoaster")),Matchers.is(true));
}
