{
  super(params,resolver,srcs,ImmutableList.<String>builder().add("--crate-type","bin").addAll(rustcFlags).build(),features,BuildTargets.getGenPath(params.getProjectFilesystem(),params.getBuildTarget(),"%s/" + params.getBuildTarget().getShortName()),compiler);
}
