{
  UnflavoredBuildTarget plainTarget=getBuildTarget();
  if (isLibrary()) {
    return getArchiveBytecodeOutputPath(plainTarget);
  }
 else {
    return BuildTargets.getScratchPath(BuildTarget.of(plainTarget),"%s/" + plainTarget.getShortName());
  }
}
