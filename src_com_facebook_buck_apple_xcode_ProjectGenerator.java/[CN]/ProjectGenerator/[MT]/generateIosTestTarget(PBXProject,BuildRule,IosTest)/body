{
  PBXNativeTarget target=generateBinaryTarget(project,rule,buildable,testTypeToTargetProductType(buildable.getTestType()),"%s." + buildable.getTestType().toFileExtension(),buildable.getInfoPlist(),true);
  project.getTargets().add(target);
  LOG.debug("Generated iOS test target %s",target);
  return target;
}
