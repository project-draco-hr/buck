{
  PBXNativeTarget target=generateBinaryTarget(project,rule,buildable,testTypeToTargetProductType(buildable.getTestType()),"%s." + buildable.getTestType().toFileExtension(),AppleResourceDescription.TYPE);
  project.getTargets().add(target);
  LOG.debug("Generated iOS test target %s",target);
  return target;
}
