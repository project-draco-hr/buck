{
  PBXNativeTarget target=generateBinaryTarget(project,rule,buildable,testTypeToTargetProductType(buildable.getTestType()),"%s." + buildable.getTestType().toFileExtension(),IosResourceDescription.TYPE);
  project.getTargets().add(target);
  return target;
}
