{
  PBXNativeTarget target=generateBinaryTarget(project,rule,buildable,PBXTarget.ProductType.APP_EXTENSION,"%s.appex",buildable.getInfoPlist(),true,true);
  project.getTargets().add(target);
  return target;
}
