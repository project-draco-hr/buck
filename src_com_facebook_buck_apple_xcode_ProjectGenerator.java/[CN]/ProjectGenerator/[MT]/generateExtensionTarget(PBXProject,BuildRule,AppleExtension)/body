{
  PBXNativeTarget target=generateBinaryTarget(project,rule,buildable,PBXTarget.ProductType.APP_EXTENSION,"%s.appex",AppleResourceDescription.TYPE);
  project.getTargets().add(target);
  return target;
}
