{
  PBXNativeTarget target=generateBinaryTarget(project,rule,buildable,PBXTarget.ProductType.MACOSX_FRAMEWORK,"%s.framework",OsxResourceDescription.TYPE);
  project.getTargets().add(target);
  return target;
}
