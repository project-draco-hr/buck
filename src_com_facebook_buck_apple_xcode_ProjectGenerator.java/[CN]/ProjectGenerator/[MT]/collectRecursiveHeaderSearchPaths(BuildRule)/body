{
  return FluentIterable.from(getRecursiveRuleDependenciesOfType(RecursiveRuleDependenciesMode.BUILDING,rule,IosLibraryDescription.TYPE,XcodeNativeDescription.TYPE)).transform(new Function<BuildRule,String>(){
    @Override public String apply(    BuildRule input){
      return getHeaderSearchPathForRule(input);
    }
  }
).toSet();
}
