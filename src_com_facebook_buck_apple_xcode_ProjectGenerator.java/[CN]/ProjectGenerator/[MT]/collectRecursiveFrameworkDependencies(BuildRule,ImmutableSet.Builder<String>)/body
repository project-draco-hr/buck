{
  for (  BuildRule ruleDependency : getRecursiveRuleDependenciesOfType(RecursiveRuleDependenciesMode.LINKING,rule,IosLibraryDescription.TYPE)) {
    IosLibrary iosLibrary=(IosLibrary)Preconditions.checkNotNull(ruleDependency);
    frameworksBuilder.addAll(iosLibrary.getFrameworks());
  }
}
