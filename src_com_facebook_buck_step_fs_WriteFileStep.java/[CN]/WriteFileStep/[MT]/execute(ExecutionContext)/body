{
  try {
    source.copyTo(Files.asByteSink(context.getProjectFilesystem().resolve(outputPath).toFile()));
    return 0;
  }
 catch (  IOException e) {
    e.printStackTrace(context.getStdErr());
    return 1;
  }
}
