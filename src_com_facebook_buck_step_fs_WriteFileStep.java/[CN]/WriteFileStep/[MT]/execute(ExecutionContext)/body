{
  try {
    Files.write(content.get() + "\n",context.getProjectFilesystem().getFileForRelativePath(outputPath),Charsets.UTF_8);
    return 0;
  }
 catch (  IOException e) {
    e.printStackTrace(context.getStdErr());
    return 1;
  }
}
