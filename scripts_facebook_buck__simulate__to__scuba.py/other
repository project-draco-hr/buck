from __future__ import absolute_import
from __future__ import division
from __future__ import print_function
from __future__ import unicode_literals
from __future__ import with_statement
import argparse
import datetime
import gzip
import json
import logging
import os
import os.path
import re
import shutil
import subprocess
import sys
import tempfile
import time
import urllib2
SCUBA_TABLE = u'buck_simulate_per_diff'
REPORTS_DIR = os.path.join(tempfile.gettempdir(), u'buck_simulate_py')
LOCAL_TIMES_DIR = os.path.join(REPORTS_DIR, u'times')
ALIAS_PATTERN = re.compile(u'.+<(.+)@fb.com>.*')
GFS_TIMES_DIR = u'/mnt/vol/gfsdataswarm-global/namespaces/infrastructure/buck-global/buck-simulate'
NULLID = (u'0' * 40)
SCRIBE_ENDPOINT = u'FILL_IN'
APP_ID = u'FILL_IN'
TOKEN = u'FILL_IN'
CONTENT_TEMPLATE = u'FILL_IN'
gfs_cache = set()
if (__name__ == u'__main__'):
    main()
