{
  resourceDirsBuilder.addAll(FluentIterable.from(resourceDescriptions).transformAndConcat(new Function<AppleResourceDescription.Arg,Set<Path>>(){
    @Override public Set<Path> apply(    AppleResourceDescription.Arg arg){
      return arg.dirs;
    }
  }
));
}
