{
  ShellStep command=createCommand(ImmutableMap.<String,String>of(),ARGS,PATH);
  assertEquals(String.format("(cd '%s' && bash -c 'echo $V1 $V2')",PATH.getAbsolutePath()),command.getDescription(context));
}
