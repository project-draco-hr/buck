{
  ProjectFilesystem projectFilesystem=params.getProjectFilesystem();
  Supplier<ImmutableCollection<Path>> inputPathsSupplier=RuleUtils.subpathsOfPathsSupplier(projectFilesystem,args.dirs);
  return new AppleAssetCatalog(params,new SourcePathResolver(resolver),inputPathsSupplier,args);
}
