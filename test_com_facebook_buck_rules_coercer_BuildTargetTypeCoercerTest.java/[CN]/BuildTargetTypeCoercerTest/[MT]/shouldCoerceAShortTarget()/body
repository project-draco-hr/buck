{
  BuildTarget seen=new BuildTargetTypeCoercer().coerce(targetParser,resolver,filesystem,basePath,":bar");
  assertEquals(BuildTarget.builder("//java/com/facebook/buck/example","bar").build(),seen);
}
