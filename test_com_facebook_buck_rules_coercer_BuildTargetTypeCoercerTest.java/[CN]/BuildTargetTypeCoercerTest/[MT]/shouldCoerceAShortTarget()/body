{
  BuildTarget seen=new BuildTargetTypeCoercer(new BuildTargetParser()).coerce(resolver,filesystem,basePath,":bar");
  assertEquals(BuildTarget.builder("//java/com/facebook/buck/example","bar").build(),seen);
}
