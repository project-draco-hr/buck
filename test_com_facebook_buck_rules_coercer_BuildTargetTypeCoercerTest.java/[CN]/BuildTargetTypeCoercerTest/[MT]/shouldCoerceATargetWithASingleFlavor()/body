{
  BuildTarget seen=new BuildTargetTypeCoercer().coerce(filesystem,basePath,"//foo:bar#baz");
  assertEquals(BuildTarget.builder("//foo","bar").addFlavors(ImmutableFlavor.of("baz")).build(),seen);
}
