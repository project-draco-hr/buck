{
  BuildTarget seen=new BuildTargetTypeCoercer().coerce(targetParser,resolver,filesystem,basePath,"//foo:bar#baz");
  assertEquals(BuildTarget.builder("//foo","bar").addFlavor("baz").build(),seen);
}
