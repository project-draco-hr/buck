{
  BuildTarget seen=new BuildTargetTypeCoercer(new BuildTargetParser()).coerce(resolver,filesystem,basePath,"//foo:bar#baz");
  assertEquals(BuildTarget.builder("//foo","bar").addFlavor("baz").build(),seen);
}
