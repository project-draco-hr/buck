{
  BuildTarget seen=new BuildTargetTypeCoercer().coerce(filesystem,basePath,":bar#baz");
  BuildTarget expected=BuildTarget.builder("//java/com/facebook/buck/example","bar").addFlavors(ImmutableFlavor.of("baz")).build();
  assertEquals(expected,seen);
}
