{
  BuildTarget seen=new BuildTargetTypeCoercer(new BuildTargetParser()).coerce(resolver,filesystem,basePath,":bar#baz");
  BuildTarget expected=BuildTarget.builder("//java/com/facebook/buck/example","bar").addFlavor("baz").build();
  assertEquals(expected,seen);
}
