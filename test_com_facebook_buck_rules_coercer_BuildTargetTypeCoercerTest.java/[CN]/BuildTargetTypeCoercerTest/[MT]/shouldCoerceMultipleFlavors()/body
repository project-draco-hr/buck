{
  BuildTarget seen=new BuildTargetTypeCoercer().coerce(filesystem,basePath,"//foo:bar#baz,qux");
  assertEquals(BuildTarget.builder("//foo","bar").addFlavors(ImmutableFlavor.of("baz")).addFlavors(ImmutableFlavor.of("qux")).build(),seen);
}
