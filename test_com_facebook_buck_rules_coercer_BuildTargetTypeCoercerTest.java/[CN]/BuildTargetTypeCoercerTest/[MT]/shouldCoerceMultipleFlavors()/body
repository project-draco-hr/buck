{
  BuildTarget seen=new BuildTargetTypeCoercer().coerce(targetParser,filesystem,basePath,"//foo:bar#baz,qux");
  assertEquals(BuildTarget.builder("//foo","bar").addFlavor("baz").addFlavor("qux").build(),seen);
}
