{
  return FluentIterable.from(target.getInputs()).transform(new Function<Path,Path>(){
    @Override public Path apply(    Path input){
      Path parent=input.getParent();
      if (parent == null) {
        return Paths.get("");
      }
      return parent;
    }
  }
).toSet();
}
