{
  return FluentIterable.from(input).filter(new Predicate<TargetNode<?>>(){
    @Override public boolean apply(    TargetNode<?> input){
      return MUTUALLY_EXCLUSIVE_TYPES.contains(input.getType());
    }
  }
).toSet();
}
