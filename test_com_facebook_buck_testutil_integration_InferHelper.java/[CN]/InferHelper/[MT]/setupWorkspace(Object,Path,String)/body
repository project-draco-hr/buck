{
  ProjectWorkspace projectWorkspace=TestDataHelper.createProjectWorkspaceForScenario(testCase,scenarioName,new TemporaryRoot(){
    @Override public Path getRootPath(){
      return workspaceRoot;
    }
  }
);
  projectWorkspace.setUp();
  return projectWorkspace;
}
