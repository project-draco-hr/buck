{
  ExecutableFinder ef=new ExecutableFinder();
  Optional<Path> infer=ef.getOptionalExecutable(Paths.get("infer"),ImmutableMap.copyOf(System.getenv()));
  assumeTrue("Infer binaries not found.",infer.isPresent());
  return infer.get();
}
