{
  try {
    Class.forName(classToLoad);
  }
 catch (  ClassNotFoundException e) {
    System.err.println("Unable to locate " + name + " on the classpath. Please add as a test dependency.");
    System.exit(1);
  }
}
