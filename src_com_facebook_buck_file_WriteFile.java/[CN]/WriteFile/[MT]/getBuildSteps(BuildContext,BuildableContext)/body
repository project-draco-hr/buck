{
  buildableContext.recordArtifact(output);
  return ImmutableList.of(new MkdirStep(getProjectFilesystem(),output.getParent()),new WriteFileStep(getProjectFilesystem(),fileContents,output,executable));
}
