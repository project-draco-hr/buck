{
  if (context.getAndroidPlatformTargetOptional().isPresent()) {
    return FluentIterable.from(context.getAndroidPlatformTarget().getBootclasspathEntries()).transform(Functions.toStringFunction()).toSet();
  }
  return ImmutableSet.of();
}
