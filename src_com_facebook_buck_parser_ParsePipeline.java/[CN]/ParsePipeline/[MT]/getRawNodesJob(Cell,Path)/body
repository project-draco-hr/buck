{
  Preconditions.checkArgument(buildFile.isAbsolute());
  return getJobWithCacheLookup(buildFile,cache.lookupRawNodes(cell,buildFile),new Supplier<ListenableFuture<ImmutableList<Map<String,Object>>>>(){
    @Override public ListenableFuture<ImmutableList<Map<String,Object>>> get(){
      return computeRawNodes(cell,buildFile);
    }
  }
,rawNodeJobsCache);
}
