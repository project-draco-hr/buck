{
  super(buildRuleParams,resolver);
  this.nativeLibDirectories=nativeLibDirectories;
  this.cpuFilters=cpuFilters;
  this.filteredNativeLibraries=filteredNativeLibraries;
  Preconditions.checkArgument(!nativeLibDirectories.isEmpty() || !filteredNativeLibraries.isEmpty(),"There should be at least one native library to copy.");
}
