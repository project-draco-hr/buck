{
  super(buildRuleParams,resolver);
  this.nativeLibDirectories=nativeLibDirectories;
  Preconditions.checkArgument(!nativeLibDirectories.isEmpty(),"There should be at least one native library to copy.");
  this.cpuFilters=cpuFilters;
}
