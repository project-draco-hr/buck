{
  Path libPath=outputDir.resolve(getBuildTarget().getShortName() + ".o");
  return NativeLinkableInput.of(SourcePathArg.from(getResolver(),new BuildTargetSourcePath(getBuildTarget(),libPath)),ImmutableSet.<FrameworkPath>of(),ImmutableSet.<FrameworkPath>of());
}
