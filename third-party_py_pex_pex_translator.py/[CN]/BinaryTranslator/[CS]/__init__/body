def __init__(self, package_type, interpreter=PythonInterpreter.get(), platform=Platform.current()):
    self._package_type = package_type
    self._platform = platform
    self._identity = interpreter.identity
