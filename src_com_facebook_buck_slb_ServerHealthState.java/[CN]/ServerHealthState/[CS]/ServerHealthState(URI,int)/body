{
  Preconditions.checkArgument(maxSamplesStored > 0,"The maximum number of samples stored must be positive instead of [%d].",maxSamplesStored);
  this.maxSamplesStored=maxSamplesStored;
  this.server=server;
  this.latencies=Lists.newLinkedList();
  this.errors=Lists.newLinkedList();
}
