{
  Preconditions.checkArgument(maxSamplesStored > 0,"The maximum number of samples stored must be positive instead of [%s].",maxSamplesStored);
  this.maxSamplesStored=maxSamplesStored;
  this.server=server;
  this.pingLatencies=Lists.newLinkedList();
  this.requests=Lists.newLinkedList();
}
