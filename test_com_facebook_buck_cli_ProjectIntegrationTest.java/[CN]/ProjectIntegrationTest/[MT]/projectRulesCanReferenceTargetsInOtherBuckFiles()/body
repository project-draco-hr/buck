{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"project_rules_can_reference_targets_in_other_buck_files",temporaryFolder);
  workspace.setUp();
  ProcessResult result=workspace.runBuckCommand("project","--workspace-and-projects");
  result.assertSuccess();
  workspace.verify();
}
