{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"project_generated_scheme_includes_tests_and_dependencies",temporaryFolder);
  workspace.setUp();
  ProcessResult result=workspace.runBuckCommand("project","--workspace-and-projects","--with-tests","//Apps:workspace");
  result.assertSuccess();
  workspace.verify();
}
