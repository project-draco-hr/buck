{
  BuildTarget buildTarget=BuildTarget.builder("//java/com/example","build_config").build();
  BuildRuleParams params=new FakeBuildRuleParamsBuilder(buildTarget).setType(AndroidBuildConfigDescription.TYPE).build();
  return new AndroidBuildConfig(params,new SourcePathResolver(new BuildRuleResolver()),"com.example",BuildConfigFields.empty(),Optional.<SourcePath>absent(),false);
}
