{
  StringBuilder sb=new StringBuilder("StartBuildRequest(");
  boolean first=true;
  if (isSetStartTimestampMillis()) {
    sb.append("startTimestampMillis:");
    sb.append(this.startTimestampMillis);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
