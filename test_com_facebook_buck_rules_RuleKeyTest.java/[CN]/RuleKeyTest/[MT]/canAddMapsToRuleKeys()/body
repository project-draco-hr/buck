{
  ImmutableMap<String,?> map=ImmutableMap.of("path",Paths.get("some/path"),"boolean",true);
  SourcePathResolver resolver=new SourcePathResolver(new BuildRuleResolver(TargetGraph.EMPTY,new BuildTargetNodeToBuildRuleTransformer()));
  RuleKey key=createEmptyRuleKey(resolver).setReflectively("map",map).build();
  assertNotNull(key);
}
