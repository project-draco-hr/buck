{
  ImmutableList<Label> labels=ImmutableList.<Label>of(ImmutableLabel.of("one"),ImmutableLabel.of("two"));
  RuleKey.Builder.RuleKeyPair reflective=createEmptyRuleKey(new SourcePathResolver(new BuildRuleResolver())).setReflectively("labels",labels).build();
  RuleKey.Builder.RuleKeyPair manual=createEmptyRuleKey(new SourcePathResolver(new BuildRuleResolver())).set("labels",Lists.transform(labels,Functions.toStringFunction())).build();
  assertEquals(manual.getTotalRuleKey(),reflective.getTotalRuleKey());
}
