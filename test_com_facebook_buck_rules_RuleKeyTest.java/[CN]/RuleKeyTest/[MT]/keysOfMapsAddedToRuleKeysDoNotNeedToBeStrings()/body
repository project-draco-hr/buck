{
  ImmutableMap<?,?> map=ImmutableMap.of(Paths.get("some/path"),"woohoo!",42L,"life, the universe and everything");
  RuleKey.Builder.RuleKeyPair key=createEmptyRuleKey(new SourcePathResolver(new BuildRuleResolver())).setReflectively("map",map).build();
  assertNotNull(key);
}
