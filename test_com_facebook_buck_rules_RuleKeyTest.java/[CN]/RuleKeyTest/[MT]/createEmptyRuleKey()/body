{
  return RuleKey.builder(BuildTargetFactory.newInstance("//some:example"),new BuildRuleType("example"),new SourcePathResolver(new BuildRuleResolver()),ImmutableSortedSet.<BuildRule>of(),ImmutableSortedSet.<BuildRule>of(),new FileHashCache(){
    @Override public boolean contains(    Path path){
      return true;
    }
    @Override public HashCode get(    Path path){
      return HashCode.fromString("deadbeef");
    }
  }
);
}
