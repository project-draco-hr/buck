{
  String java=BuildConfigs.generateBuildConfigDotJava(javaPackage,useConstantExpressions,constants);
  ProjectFilesystem filesystem=context.getProjectFilesystem();
  try {
    filesystem.writeContentsToPath(java,outBuildConfigPath);
  }
 catch (  IOException e) {
    context.logError(e,"Error writing BuildConfig.java: %s",outBuildConfigPath);
    return 1;
  }
  return 0;
}
