{
  if (!(obj instanceof GenerateBuildConfigStep)) {
    return false;
  }
  GenerateBuildConfigStep that=(GenerateBuildConfigStep)obj;
  return Objects.equal(this.configPackage,that.configPackage) && Objects.equal(this.debug,that.debug) && Objects.equal(this.outBuildConfigPath,that.outBuildConfigPath);
}
