{
  Preconditions.checkState(allTargetNodes.isEmpty() || !knownCells.isEmpty(),"There are cached target nodes but no known cells. Cache invalidation will not work.");
  for (  Cell cell : knownCells) {
    invalidatePath(cell,path);
  }
}
