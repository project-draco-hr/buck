{
  Preconditions.checkState((sourceTreePath().isPresent() || sourcePath().isPresent()) && !(sourceTreePath().isPresent() && sourcePath().isPresent()),"Exactly one of sourceTreePath or sourcePath should be set");
}
