{
  Preconditions.checkState((getSourceTreePath().isPresent() || getSourcePath().isPresent()) && !(getSourceTreePath().isPresent() && getSourcePath().isPresent()),"Exactly one of sourceTreePath or sourcePath should be set");
}
