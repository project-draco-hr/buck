{
  if (androidAutoGenerateEnabled) {
    return androidAutoGenerateEnabled;
  }
  return getBuckConfig().getBooleanValue("project","enable_android_auto_generate_sources",DEFAULT_ANDROID_AUTO_GENERATE);
}
