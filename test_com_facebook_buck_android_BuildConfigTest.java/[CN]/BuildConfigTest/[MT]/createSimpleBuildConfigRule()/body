{
  BuildTarget buildTarget=new BuildTarget("//java/com/example","build_config");
  BuildRuleParams params=new FakeBuildRuleParamsBuilder(buildTarget).setType(BuildConfigDescription.TYPE).build();
  return new BuildConfig(params,"com.example",true);
}
