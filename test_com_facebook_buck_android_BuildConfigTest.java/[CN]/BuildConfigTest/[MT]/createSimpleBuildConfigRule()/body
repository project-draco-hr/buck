{
  BuildTarget buildTarget=new BuildTarget("//java/com/example","build_config");
  BuildConfig buildConfig=new BuildConfig(buildTarget,"com.example",true);
  return new AbstractBuildable.AnonymousBuildRule(BuildConfigDescription.TYPE,buildConfig,new FakeBuildRuleParams(buildTarget));
}
