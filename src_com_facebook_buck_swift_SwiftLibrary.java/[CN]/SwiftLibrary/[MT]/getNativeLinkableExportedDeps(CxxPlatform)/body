{
  if (!isPlatformSupported(cxxPlatform)) {
    return ImmutableList.of();
  }
  return FluentIterable.from(exportedDeps).filter(NativeLinkable.class).append(new SwiftRuntimeNativeLinkable(appleCxxPlatformFlavorDomain.getValue(cxxPlatform.getFlavor())));
}
