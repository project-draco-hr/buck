{
  return new SerializableIntellijSettings(intellijConfig.getLanguageLevel().orNull(),intellijConfig.getJdkName().orNull(),intellijConfig.getJdkType().orNull(),intellijConfig.getOutputUrl().orNull());
}
