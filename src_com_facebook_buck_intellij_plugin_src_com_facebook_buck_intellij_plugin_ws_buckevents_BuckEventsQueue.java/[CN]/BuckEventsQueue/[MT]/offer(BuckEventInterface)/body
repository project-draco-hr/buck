{
  if (event.getPriority() == BuckEventBase.PRIORITY_HIGH || event.getPriority() == BuckEventBase.PRIORITY_MED) {
    handleHighPriEvent(event);
    return;
  }
  mLowPri.add(event);
  if (mLowPri.size() > lowPriBatchSize) {
    handleLowPriBatch();
  }
}
