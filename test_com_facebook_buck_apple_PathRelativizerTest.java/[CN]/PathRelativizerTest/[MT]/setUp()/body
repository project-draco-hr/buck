{
  root=Paths.get("/my/repo/root/");
  outputPath=root.resolve("output0/output1");
  pathRelativizer=new PathRelativizer(root,outputPath,new SourcePathResolver(new BuildRuleResolver()).getPathFunction());
}
