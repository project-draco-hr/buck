{
  TTupleProtocol iprot=(TTupleProtocol)prot;
  BitSet incoming=iprot.readBitSet(2);
  if (incoming.get(0)) {
    struct.buckConfig=new BuildJobStateBuckConfig();
    struct.buckConfig.read(iprot);
    struct.setBuckConfigIsSet(true);
  }
  if (incoming.get(1)) {
{
      org.apache.thrift.protocol.TList _list49=new org.apache.thrift.protocol.TList(org.apache.thrift.protocol.TType.STRUCT,iprot.readI32());
      struct.fileHashes=new ArrayList<BuildJobStateFileHashes>(_list49.size);
      BuildJobStateFileHashes _elem50;
      for (int _i51=0; _i51 < _list49.size; ++_i51) {
        _elem50=new BuildJobStateFileHashes();
        _elem50.read(iprot);
        struct.fileHashes.add(_elem50);
      }
    }
    struct.setFileHashesIsSet(true);
  }
}
