{
  List<Object> list=new ArrayList<Object>();
  boolean present_buckConfig=true && (isSetBuckConfig());
  list.add(present_buckConfig);
  if (present_buckConfig)   list.add(buckConfig);
  return list.hashCode();
}
