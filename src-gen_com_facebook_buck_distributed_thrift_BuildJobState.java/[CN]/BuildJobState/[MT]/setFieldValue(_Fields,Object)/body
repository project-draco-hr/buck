{
switch (field) {
case BUCK_CONFIG:
    if (value == null) {
      unsetBuckConfig();
    }
 else {
      setBuckConfig((BuildJobStateBuckConfig)value);
    }
  break;
case FILE_HASHES:
if (value == null) {
  unsetFileHashes();
}
 else {
  setFileHashes((List<BuildJobStateFileHashes>)value);
}
break;
case TARGET_GRAPH:
if (value == null) {
unsetTargetGraph();
}
 else {
setTargetGraph((BuildJobStateTargetGraph)value);
}
break;
}
}
