{
  try {
    DataOutputStream out=new DataOutputStream(connection.getOutputStream());
    out.writeBytes(content);
    out.flush();
    out.close();
    return connection.getInputStream();
  }
 catch (  IOException e) {
    throw new HumanReadableException("Error sending and receiving data to %s: %s",url,e.getMessage());
  }
}
