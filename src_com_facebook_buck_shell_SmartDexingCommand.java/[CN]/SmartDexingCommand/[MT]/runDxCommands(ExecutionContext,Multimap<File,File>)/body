{
  DefaultCommandRunner commandRunner=new DefaultCommandRunner(context,getDxExecutor());
  List<Command> dxCommands=generateDxCommands(context,outputToInputs);
  commandRunner.runCommandsInParallelAndWait(dxCommands);
}
