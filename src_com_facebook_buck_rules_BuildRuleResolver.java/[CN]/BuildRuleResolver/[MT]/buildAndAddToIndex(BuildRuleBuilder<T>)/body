{
  T buildRule=builder.build(this);
  BuildRule oldValue=buildRuleIndex.put(buildRule.getBuildTarget(),buildRule);
  if (oldValue != null) {
    throw new IllegalStateException("A build rule for this target has already been created: " + buildRule.getBuildTarget());
  }
  return buildRule;
}
