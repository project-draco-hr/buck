{
  ProjectFilesystem filesystem=new AllExistingProjectFilesystem();
  FakeBuckConfig buckConfig=new FakeBuckConfig(ImmutableMap.of("thrift",ImmutableMap.of("compiler","thrift1")),filesystem);
  ThriftBuckConfig thriftBuckConfig=new ThriftBuckConfig(buckConfig);
  assertEquals(new TestSourcePath("thrift1"),thriftBuckConfig.getCompiler(ThriftLibraryDescription.CompilerType.THRIFT2));
}
