{
  Preconditions.checkArgument(!getName().isEmpty(),"Empty flavor name");
  Preconditions.checkArgument(!INVALID_FLAVOR_CHARACTERS.matcher(getName()).find(),"Invalid characters in flavor name: " + getName());
}
