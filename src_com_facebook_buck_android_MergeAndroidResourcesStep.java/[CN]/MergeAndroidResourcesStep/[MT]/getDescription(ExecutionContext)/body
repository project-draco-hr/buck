{
  ImmutableList<String> sortedSymbolsFiles=FluentIterable.from(symbolsFileToRDotJavaPackage.keySet()).toSortedList(natural());
  return getShortName() + " " + Joiner.on(' ').join(sortedSymbolsFiles)+ " -o "+ pathToGeneratedJavaFiles;
}
