{
  if (!(event instanceof ConsoleEvent)) {
    return false;
  }
  ConsoleEvent that=(ConsoleEvent)event;
  return Objects.equal(getLevel(),that.getLevel()) && Objects.equal(getMessage(),that.getMessage());
}
