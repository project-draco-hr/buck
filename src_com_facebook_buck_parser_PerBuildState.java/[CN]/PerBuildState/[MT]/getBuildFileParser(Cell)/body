{
  ProjectBuildFileParser parser=parsers.get(cell);
  if (parser == null) {
    parser=cell.createBuildFileParser(console,eventBus,permState.isUsingWatchmanGlob());
    parser.setEnableProfiling(enableProfiling);
    parsers.put(cell,parser);
  }
  return parser;
}
