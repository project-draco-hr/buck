{
  ImmutableList.Builder<String> cmdBuilder=ImmutableList.builder();
  cmdBuilder.add(AR).add("-q").add(outputFile.toString());
  for (  Path src : srcs) {
    cmdBuilder.add(src.toString());
  }
  return cmdBuilder.build();
}
