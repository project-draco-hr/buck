@property
def suffix(self):
    "The final component's last suffix, if any."
    name = self.name
    i = name.rfind('.')
    if (0 < i < (len(name) - 1)):
        return name[i:]
    else:
        return ''
