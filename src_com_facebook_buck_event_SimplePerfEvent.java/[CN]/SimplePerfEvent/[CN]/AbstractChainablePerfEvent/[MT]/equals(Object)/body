{
  if (!super.equals(o)) {
    return false;
  }
  AbstractChainablePerfEvent other=(AbstractChainablePerfEvent)o;
  return other.perfEventId.equals(perfEventId) && other.info.equals(info) && other.perfEventType.equals(perfEventType);
}
