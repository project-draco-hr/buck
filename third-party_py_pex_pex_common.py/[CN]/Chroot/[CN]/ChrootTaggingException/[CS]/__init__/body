def __init__(self, filename, orig_tag, new_tag):
    super(Chroot.ChrootTaggingException, self).__init__(('Trying to add %s to fileset(%s) but already in fileset(%s)!' % (filename, new_tag, orig_tag)))
