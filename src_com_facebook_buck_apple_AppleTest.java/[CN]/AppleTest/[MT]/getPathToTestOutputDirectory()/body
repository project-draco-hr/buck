{
  Path path=BuildTargets.getGenPath(getProjectFilesystem(),getBuildTarget(),"__apple_test_%s_output__");
  String subdir=BuckConstant.oneTimeTestSubdirectory;
  if (subdir != null && !subdir.isEmpty()) {
    path=path.resolve(subdir);
  }
  return path;
}
