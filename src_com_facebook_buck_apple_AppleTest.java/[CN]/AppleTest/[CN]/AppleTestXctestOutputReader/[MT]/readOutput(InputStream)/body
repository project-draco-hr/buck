{
  try (InputStreamReader outputReader=new InputStreamReader(output,StandardCharsets.UTF_8);BufferedReader outputBufferedReader=new BufferedReader(outputReader)){
    XctestOutputParsing.streamOutput(outputBufferedReader,xctestEventHandler);
  }
 }
