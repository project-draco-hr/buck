{
  Optional<BuildRule> binaryRule=Optional.of(resolver.getRule(args.binary));
  return new AppleBundle(params,new SourcePathResolver(resolver),args.extension,args.infoPlist,binaryRule,IOS_APP_SUBFOLDER_SPEC_MAP,args.dirs.get(),args.files.get());
}
