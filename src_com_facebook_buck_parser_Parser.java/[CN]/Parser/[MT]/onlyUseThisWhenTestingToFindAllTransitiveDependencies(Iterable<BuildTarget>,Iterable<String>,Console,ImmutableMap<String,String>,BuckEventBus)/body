{
  try (ProjectBuildFileParser parser=buildFileParserFactory.createParser(defaultIncludes,console,environment,eventBus)){
    return findAllTransitiveDependencies(toExplore,defaultIncludes,parser,environment);
  }
 }
