{
  try (PerBuildState state=new PerBuildState(permState,marshaller,eventBus,executor,rootCell,enableProfiling,speculativeParsing)){
    return resolveTargetSpecs(state,eventBus,rootCell,specs);
  }
 }
