{
  try (PerBuildState state=new PerBuildState(permState,marshaller,eventBus,executor,rootCell,enableProfiling,speculativeParsing,false)){
    return resolveTargetSpecs(state,eventBus,rootCell,specs);
  }
 }
