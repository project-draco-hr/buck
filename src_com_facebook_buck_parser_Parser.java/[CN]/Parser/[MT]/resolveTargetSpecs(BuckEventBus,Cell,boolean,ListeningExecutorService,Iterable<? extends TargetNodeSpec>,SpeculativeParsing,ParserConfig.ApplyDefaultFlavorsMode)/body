{
  try (PerBuildState state=new PerBuildState(this,eventBus,executor,rootCell,enableProfiling,speculativeParsing,false)){
    return resolveTargetSpecs(state,eventBus,rootCell,specs,applyDefaultFlavorsMode);
  }
 }
