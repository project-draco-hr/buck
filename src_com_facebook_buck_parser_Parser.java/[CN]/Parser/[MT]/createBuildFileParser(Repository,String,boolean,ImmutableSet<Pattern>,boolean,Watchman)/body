{
  return new Parser(repository,enforceBuckPackageBoundary,tempFilePatterns,new Supplier<BuildFileTree>(){
    @Override public BuildFileTree get(){
      return new FilesystemBackedBuildFileTree(repository.getFilesystem(),repository.getBuildFileName());
    }
  }
,pythonInterpreter,useWatchmanGlob,watchman);
}
