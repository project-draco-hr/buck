{
  try (ProjectBuildFileParser parser=buildFileParserFactory.createParser(defaultIncludes,EnumSet.noneOf(ProjectBuildFileParser.Option.class),console,environment)){
    return findAllTransitiveDependencies(toExplore,defaultIncludes,parser);
  }
 }
