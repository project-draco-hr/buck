{
  try (ProjectBuildFileParser parser=buildFileParserFactory.createParser(defaultIncludes,console,environment)){
    return findAllTransitiveDependencies(toExplore,defaultIncludes,parser,environment);
  }
 }
