{
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(defaultIncludes,console,environment,buckEventBus)){
    return parseBuildFile(buildFile,defaultIncludes,projectBuildFileParser,environment);
  }
 }
