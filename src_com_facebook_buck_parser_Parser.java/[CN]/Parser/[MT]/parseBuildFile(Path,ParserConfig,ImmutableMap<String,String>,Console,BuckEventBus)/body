{
  try (ProjectBuildFileParser projectBuildFileParser=createBuildFileParser(console,environment,buckEventBus)){
    return parseBuildFile(buildFile,parserConfig,projectBuildFileParser,environment);
  }
 }
