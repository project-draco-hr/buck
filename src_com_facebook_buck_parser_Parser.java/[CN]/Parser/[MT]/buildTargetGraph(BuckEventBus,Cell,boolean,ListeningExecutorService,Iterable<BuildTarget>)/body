{
  if (Iterables.isEmpty(toExplore)) {
    return TargetGraph.EMPTY;
  }
  try (final PerBuildState state=new PerBuildState(permState,marshaller,eventBus,executor,rootCell,enableProfiling,SpeculativeParsing.of(true))){
    return buildTargetGraph(state,eventBus,toExplore);
  }
 }
