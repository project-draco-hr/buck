{
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(defaultIncludes,parseOptions)){
    return parseBuildFile(buildFile,defaultIncludes,projectBuildFileParser);
  }
 }
