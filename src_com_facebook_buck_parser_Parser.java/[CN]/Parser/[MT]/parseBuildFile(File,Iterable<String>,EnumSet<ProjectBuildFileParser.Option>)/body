{
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(defaultIncludes,parseOptions,console)){
    return parseBuildFile(buildFile,defaultIncludes,projectBuildFileParser);
  }
 }
