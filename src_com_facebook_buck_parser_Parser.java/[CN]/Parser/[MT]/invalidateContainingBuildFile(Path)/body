{
  String packageBuildFilePath=buildFileTreeCache.get().getBasePathOfAncestorTarget(path).toString();
  state.invalidateDependents(repository.getFilesystem().getFileForRelativePath(packageBuildFilePath + '/' + BuckConstant.BUILD_RULES_FILE_NAME).toPath());
}
