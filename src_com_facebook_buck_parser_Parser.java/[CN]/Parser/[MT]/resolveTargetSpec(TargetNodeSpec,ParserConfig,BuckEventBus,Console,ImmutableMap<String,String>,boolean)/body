{
  try (BuildFileParsers buildFileParsers=new BuildFileParsers(console,eventBus)){
    buildFileParsers.setEnableProfiling(enableProfiling);
    return resolveTargetSpec(spec,parserConfig,buildFileParsers,environment);
  }
 }
