{
  if (projectFilesystem.isPathChangeEvent(event)) {
    final String SRC_EXTENSION=".java";
    Path path=(Path)event.context();
    if (path.toString().endsWith(SRC_EXTENSION) && event.kind() == StandardWatchEventKinds.ENTRY_MODIFY) {
      return;
    }
  }
  invalidateCache();
}
