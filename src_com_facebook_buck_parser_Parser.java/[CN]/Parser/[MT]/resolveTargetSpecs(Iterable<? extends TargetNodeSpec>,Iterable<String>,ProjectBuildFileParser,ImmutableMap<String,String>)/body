{
  ImmutableSet.Builder<BuildTarget> targets=ImmutableSet.builder();
  for (  TargetNodeSpec spec : specs) {
    targets.addAll(resolveTargetSpec(spec,defaultIncludes,buildFileParser,environment));
  }
  return targets.build();
}
