{
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(defaultIncludes,console,environment)){
    return parseBuildFile(buildFile,defaultIncludes,projectBuildFileParser,environment);
  }
 }
