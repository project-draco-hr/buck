{
  try (BuildFileParsers buildFileParsers=new BuildFileParsers(console,buckEventBus,useWatchmanGlob)){
    return parseBuildFile(cellRoot,buildFile,parserConfig,buildFileParsers.create(cell),environment);
  }
 }
