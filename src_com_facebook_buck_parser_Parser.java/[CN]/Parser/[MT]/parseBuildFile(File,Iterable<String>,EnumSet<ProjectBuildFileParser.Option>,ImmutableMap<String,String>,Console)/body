{
  try (ProjectBuildFileParser projectBuildFileParser=buildFileParserFactory.createParser(defaultIncludes,parseOptions,console,environment)){
    return parseBuildFile(buildFile,defaultIncludes,projectBuildFileParser,environment);
  }
 }
