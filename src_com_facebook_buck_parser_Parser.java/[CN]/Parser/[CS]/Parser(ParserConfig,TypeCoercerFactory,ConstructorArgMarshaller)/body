{
  this.permState=parserConfig.getEnableParallelParsing() ? new ParallelDaemonicParserState(typeCoercerFactory,marshaller) : new SerialDaemonicParserState(typeCoercerFactory,marshaller);
  this.marshaller=marshaller;
}
