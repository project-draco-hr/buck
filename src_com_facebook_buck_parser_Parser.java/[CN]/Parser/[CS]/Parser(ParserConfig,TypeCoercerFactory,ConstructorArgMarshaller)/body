{
  this.permState=parserConfig.getEnableParallelParsing() ? new ParallelDaemonicParserState(typeCoercerFactory,marshaller,parserConfig.getNumParsingThreads()) : new SerialDaemonicParserState(typeCoercerFactory,marshaller);
  this.marshaller=marshaller;
}
