{
  try (PrintWriter out=new PrintWriter(buckFilePath)){
    out.write(text);
  }
 catch (  FileNotFoundException e) {
    LOG.error(e.toString());
  }
catch (  IOException e) {
    LOG.error(e.toString());
  }
}
