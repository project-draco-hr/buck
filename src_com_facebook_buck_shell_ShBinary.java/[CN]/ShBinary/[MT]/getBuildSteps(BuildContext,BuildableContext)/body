{
  MakeCleanDirectoryStep mkdir=new MakeCleanDirectoryStep(output.getParent());
  GenerateShellScriptStep generateShellScript=new GenerateShellScriptStep(Paths.get(getBuildTarget().getBasePath()),main.resolve(),SourcePaths.toPaths(resources),output);
  buildableContext.recordArtifact(output);
  return ImmutableList.of(mkdir,generateShellScript);
}
