{
  Function<String,String> pathRelativizer=projectFilesystem.getPathRelativizer();
  String pythonPath=Joiner.on(':').join(Iterables.transform(getPythonPathEntries(),pathRelativizer));
  return String.format("PYTHONPATH=%s python %s",pythonPath,pathRelativizer.apply(main));
}
