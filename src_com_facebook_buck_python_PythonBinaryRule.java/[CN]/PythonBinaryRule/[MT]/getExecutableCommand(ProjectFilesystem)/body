{
  String pythonPath=Joiner.on(':').join(Iterables.transform(getPythonPathEntries(),projectFilesystem.getAbsolutifier()));
  return ImmutableList.of(String.format("PYTHONPATH=%s",pythonPath),"python",projectFilesystem.getPathRelativizer().apply(main).toString());
}
