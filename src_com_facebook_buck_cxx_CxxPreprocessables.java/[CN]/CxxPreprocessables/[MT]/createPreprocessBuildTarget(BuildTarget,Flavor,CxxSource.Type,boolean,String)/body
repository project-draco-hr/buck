{
  String outputName=Flavor.replaceInvalidCharacters(getOutputName(type,name));
  return BuildTarget.builder(target).addFlavors(platform).addFlavors(ImmutableFlavor.of(String.format("preprocess-%s%s",pic ? "pic-" : "",outputName))).build();
}
