{
  assumeTrue(Platform.detect() != Platform.WINDOWS);
  BuildTarget testBuildTarget=BuildTarget.builder().setUnflavoredBuildTarget(UnflavoredBuildTarget.of(Paths.get("/Users/user/src"),Optional.<String>absent(),"//target","short")).addFlavors(CxxInferEnhancer.InferFlavors.INFER.get()).build();
  InferLogLine inferLogLine=new InferLogLine(testBuildTarget,Paths.get("buck-out/a/b/c/"));
  String expectedOutput="//target:short#infer\t[infer]\tbuck-out/a/b/c";
  assertEquals(expectedOutput,inferLogLine.toString());
}
