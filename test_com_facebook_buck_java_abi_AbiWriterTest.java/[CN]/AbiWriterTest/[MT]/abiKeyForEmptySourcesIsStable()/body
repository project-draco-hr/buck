{
  assertEquals("The ABI key used when a java_library() has no srcs should be constant across platforms.",AbiWriterProtocol.EMPTY_ABI_KEY,AbiWriter.computeAbiKey(ImmutableSortedSet.<String>of()));
}
