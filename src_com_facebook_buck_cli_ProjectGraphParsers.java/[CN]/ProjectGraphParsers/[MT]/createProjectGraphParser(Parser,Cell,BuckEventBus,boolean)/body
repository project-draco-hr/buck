{
  return new ProjectGraphParser(){
    @Override public TargetGraph buildTargetGraphForTargetNodeSpecs(    Iterable<? extends TargetNodeSpec> targetNodeSpecs,    Executor executor) throws IOException, InterruptedException {
      try {
        return parser.buildTargetGraphForTargetNodeSpecs(buckEventBus,rootCell,enableProfiling,executor,targetNodeSpecs).getSecond();
      }
 catch (      BuildTargetException|BuildFileParseException e) {
        throw new HumanReadableException(e);
      }
    }
  }
;
}
