{
  TestCommandOptions options=getOptions("--labels","!c","a+b");
  TestRule rule=new FakeTestRule(JavaTestDescription.TYPE,ImmutableSet.<Label>of(Label.of("a"),Label.of("b"),Label.of("c")),BuildTargetFactory.newInstance("//:for"),new SourcePathResolver(new BuildRuleResolver()),ImmutableSortedSet.<BuildRule>of());
  List<TestRule> testRules=ImmutableList.of(rule);
  Iterable<TestRule> result=TestCommand.filterTestRules(new FakeBuckConfig(),options,ImmutableSet.<BuildTarget>of(),testRules);
  assertEquals(ImmutableSet.of(),result);
}
