{
  BuildTarget target=BuildTargetFactory.newInstance("//:example.html");
  params=new BuildRuleParams(target,ImmutableSortedSet.<BuildRule>of(),ImmutableSet.<BuildTargetPattern>of(),Functions.<String>identity());
  root=new File(".");
  context=getBuildContext(root);
}
