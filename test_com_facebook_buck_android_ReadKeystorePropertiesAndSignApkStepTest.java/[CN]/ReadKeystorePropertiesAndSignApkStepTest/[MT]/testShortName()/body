{
  ProjectFilesystem projectFilesystem=createMock(ProjectFilesystem.class);
  ReadKeystorePropertiesAndSignApkStep readKeystorePropertiesAndSignCommand=new ReadKeystorePropertiesAndSignApkStep("src/com/facebook/orca/keystore.properties",BIN_DIR + "/src/com/facebook/orca/orca_unsigned.apk",BIN_DIR + "/src/com/facebook/orca/orca_signed.apk");
  ExecutionContext context=createMock(ExecutionContext.class);
  replay(context,projectFilesystem);
  assertEquals("sign apk",readKeystorePropertiesAndSignCommand.getShortName(context));
  verify(context,projectFilesystem);
}
