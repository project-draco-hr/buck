{
  BuildTargetParser parser=new BuildTargetParser();
  this.macroHandler=new MacroHandler(ImmutableMap.<String,MacroExpander>of("classpath",new ClasspathMacroExpander(parser),"exe",new ExecutableMacroExpander(parser),"location",new LocationMacroExpander(parser)));
}
