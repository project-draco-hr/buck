{
  BuildTargetParser parser=new BuildTargetParser();
  this.macroHandler=new MacroHandler(ImmutableMap.<String,MacroExpander>of("exe",new ExecutableMacroExpander(parser),"location",new LocationMacroExpander(parser)));
}
