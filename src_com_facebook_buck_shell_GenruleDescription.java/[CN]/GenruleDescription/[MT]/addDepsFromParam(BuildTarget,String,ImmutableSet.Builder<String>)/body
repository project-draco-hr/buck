{
  try {
    targets.addAll(Iterables.transform(macroHandler.extractTargets(target,paramValue),Functions.toStringFunction()));
  }
 catch (  MacroException e) {
    throw new HumanReadableException(e,"%s: %s",target,e.getMessage());
  }
}
