{
  if (!args.executable.or(false)) {
    return new Genrule(params,new SourcePathResolver(resolver),srcs,cmd,bash,cmdExe,out);
  }
 else {
    return new GenruleBinary(params,new SourcePathResolver(resolver),srcs,cmd,bash,cmdExe,out);
  }
}
