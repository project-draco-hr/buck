{
  threadsToRunningTestStatusMessageEvent.put(finished.getThreadId(),Optional.<TestStatusMessageEvent>absent());
synchronized (testStatusMessageBuilder) {
    testStatusMessageBuilder.add(finished.getTestStatusMessage());
  }
}
