{
  if (defaultRules == null) {
synchronized (KnownBuildRuleTypes.class) {
      if (defaultRules == null) {
        defaultRules=createBuilder(config,androidDirectoryResolver,javacEnv,pythonEnv).build();
      }
    }
  }
  return defaultRules;
}
