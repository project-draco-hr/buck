{
  final TargetGraph targetGraph=TargetGraphFactory.newInstance(ImmutableSet.copyOf(nodes));
  return createProjectGeneratorForCombinedProject(nodes,projectGeneratorOptions,getSourcePathResolverForNodeFunction(targetGraph));
}
