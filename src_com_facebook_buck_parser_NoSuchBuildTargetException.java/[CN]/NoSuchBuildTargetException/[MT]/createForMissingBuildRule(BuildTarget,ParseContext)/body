{
  String message=String.format("No rule found when resolving target %s",makeTargetDescription(buildTarget.getFullyQualifiedName(),parseContext));
  return new NoSuchBuildTargetException(message);
}
