{
  String message=String.format("No rule found when resolving target %s",parseContext.makeTargetDescription(buildTarget.getFullyQualifiedName()));
  return new NoSuchBuildTargetException(message);
}
