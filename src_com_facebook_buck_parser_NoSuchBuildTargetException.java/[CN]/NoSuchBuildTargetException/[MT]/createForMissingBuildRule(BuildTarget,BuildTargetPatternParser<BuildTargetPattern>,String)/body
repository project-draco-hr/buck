{
  String message=String.format("No rule found when resolving target %s",buildTargetPatternParser.makeTargetDescription(buildTarget.getFullyQualifiedName(),buildFileName));
  return new NoSuchBuildTargetException(message);
}
