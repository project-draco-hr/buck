{
  String message=String.format("No %s file %s when resolving target %s",BUILD_RULES_FILE_NAME,buildFilePath,makeTargetDescription(buildTargetName,parseContext));
  return new NoSuchBuildTargetException(message);
}
