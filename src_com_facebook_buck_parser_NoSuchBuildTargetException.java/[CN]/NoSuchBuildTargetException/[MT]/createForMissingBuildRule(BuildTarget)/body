{
  String message=String.format("No rule '%s' found in %s%s",buildTarget.getShortName(),buildTarget.getBasePathWithSlash(),BUILD_RULES_FILE_NAME);
  return new NoSuchBuildTargetException(message);
}
