def test_glob_excludes_double_star(self):
    search_base = fake_path('foo', glob_results={'**/*.java': ['A.java', 'B.java', 'Test.java'], })
    self.assertEqual(['A.java', 'B.java'], glob_internal(includes=['**/*.java'], excludes=['**/*Test.java'], include_dotfiles=False, allow_empty=False, search_base=search_base))
