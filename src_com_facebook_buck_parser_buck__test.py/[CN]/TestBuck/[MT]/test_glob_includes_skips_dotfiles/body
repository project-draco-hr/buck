def test_glob_includes_skips_dotfiles(self):
    search_base = fake_path('foo', glob_results={'*.java': ['A.java', '.B.java'], })
    self.assertEqual(['A.java'], glob_internal(includes=['*.java'], excludes=[], include_dotfiles=False, allow_empty=False, search_base=search_base))
