def test_glob_includes_does_not_skip_dotfiles_if_include_dotfiles(self):
    search_base = fake_path('foo', glob_results={'*.java': ['A.java', '.B.java'], })
    self.assertEqual(['.B.java', 'A.java'], glob_internal(includes=['*.java'], excludes=[], include_dotfiles=True, search_base=search_base))
