{
  ExportFileRule rule=new ExportFileRule(params,Optional.of("chips"),Optional.of("fish"));
  List<Step> steps=rule.getBuildSteps(context);
  MkdirAndSymlinkFileStep expected=new MkdirAndSymlinkFileStep("chips",BuckConstant.GEN_DIR + "/fish");
  assertEquals(ImmutableList.of(expected),steps);
}
