{
  BuildTarget target=BuildTargetFactory.newInstance("//:example.html");
  params=new CachingBuildRuleParams(target,ImmutableSortedSet.<BuildRule>of(),ImmutableSet.<BuildTargetPattern>of(),artifactCache);
  root=new File(".");
  context=getBuildContext(root);
}
