{
  filesystem=new FakeProjectFilesystem(temp.newFolder());
  BuildRuleParams buildRuleParams=new FakeBuildRuleParamsBuilder(BuildTarget.builder("//lib","junit").build()).setProjectFilesystem(filesystem).build();
  junitJarRule=new PrebuiltJar(buildRuleParams,new SourcePathResolver(new BuildRuleResolver()),new PathSourcePath(PATH_TO_JUNIT_JAR),Optional.<SourcePath>of(new TestSourcePath("lib/junit-4.11-sources.jar")),Optional.<SourcePath>absent(),Optional.of("http://junit-team.github.io/junit/javadoc/latest/"));
}
