{
  if (classLoader == null) {
    return;
  }
  try {
    classLoader.close();
  }
 catch (  IOException e) {
    LOG.warn("Unable to close annotation processor classloader.");
  }
 finally {
    classLoader=null;
  }
}
