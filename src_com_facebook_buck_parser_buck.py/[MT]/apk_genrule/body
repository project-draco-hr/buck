@provide_for_build
def apk_genrule(name, srcs, apk, cmd, out, deps=[], visibility=[], build_env=None):
    deps = (deps + [apk])
    add_rule({'type': 'apk_genrule', 'name': name, 'srcs': srcs, 'apk': apk, 'cmd': cmd, 'deps': deps, 'visibility': visibility, }, build_env)
