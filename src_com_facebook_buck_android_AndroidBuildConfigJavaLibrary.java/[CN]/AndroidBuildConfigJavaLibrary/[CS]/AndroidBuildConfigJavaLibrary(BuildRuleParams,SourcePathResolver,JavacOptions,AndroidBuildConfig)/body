{
  super(params,resolver,ImmutableSortedSet.of(new BuildTargetSourcePath(androidBuildConfig.getBuildTarget())),ImmutableSortedSet.<SourcePath>of(),Optional.<SourcePath>absent(),ImmutableList.<String>of(),ImmutableSortedSet.<BuildRule>of(),ImmutableSortedSet.<BuildRule>of(),ImmutableSet.<Path>of(),javacOptions,Optional.<Path>absent(),Optional.<String>absent());
  this.androidBuildConfig=androidBuildConfig;
  Preconditions.checkState(params.getDeps().contains(androidBuildConfig),"%s must depend on the AndroidBuildConfig whose output is in this rule's srcs.",params.getBuildTarget());
}
