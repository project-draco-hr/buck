{
  super(params,resolver,ImmutableSortedSet.of(new BuildTargetSourcePath(androidBuildConfig.getBuildTarget())),ImmutableSortedSet.<SourcePath>of(),Optional.<Path>absent(),ImmutableList.<String>of(),ImmutableSortedSet.<BuildRule>of(),ImmutableSortedSet.<BuildRule>of(),ImmutableSet.<Path>of(),javacOptions,Optional.<Path>absent());
  this.androidBuildConfig=androidBuildConfig;
  Preconditions.checkState(params.getDeps().contains(androidBuildConfig),"%s must depend on the AndroidBuildConfig whose output is in this rule's srcs.",params.getBuildTarget());
}
