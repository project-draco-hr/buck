{
  Preconditions.checkState(mainClass != null,"Must specify a main class for %s in order to to run it.",buildTarget.getFullyQualifiedName());
  return ImmutableList.of("java","-jar",projectFilesystem.getAbsolutifier().apply(getOutputFile()).toString());
}
