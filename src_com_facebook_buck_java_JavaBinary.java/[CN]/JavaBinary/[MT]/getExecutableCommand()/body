{
  Preconditions.checkState(mainClass != null,"Must specify a main class for %s in order to to run it.",getBuildTarget());
  return new CommandTool.Builder().addArg(javaBinOverride.or("java")).addArg("-jar").addArg(new BuildTargetSourcePath(getBuildTarget())).build();
}
