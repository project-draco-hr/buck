{
  return fluentIterable.filter(new Predicate<TargetNode<?>>(){
    @Override public boolean apply(    TargetNode<?> input){
      return ImmutableSet.of(CxxLibraryDescription.TYPE,CxxBinaryDescription.TYPE).contains(input.getType());
    }
  }
);
}
