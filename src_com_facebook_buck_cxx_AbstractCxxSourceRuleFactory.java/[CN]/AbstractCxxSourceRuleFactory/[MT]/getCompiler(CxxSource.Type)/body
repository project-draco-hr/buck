{
  if (type == CxxSource.Type.ASSEMBLER || type == AbstractCxxSource.Type.ASSEMBLER_WITH_CPP) {
    return getCxxPlatform().getAs();
  }
  return CxxSourceTypes.needsCxxCompiler(type) ? getCxxPlatform().getCxx() : getCxxPlatform().getCc();
}
