{
  assumeThat(packageStyle,Matchers.equalTo(PythonBuckConfig.PackageStyle.INPLACE));
  ProjectWorkspace.ProcessResult result=workspace.runBuckCommand("run",":bin-with-native-libs").assertSuccess();
  assertThat(result.getStdout(),Matchers.containsString("HELLO WORLD"));
}
