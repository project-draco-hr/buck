{
  Config rawConfig=Configs.createDefaultConfig(tmp.getRootPath());
  BuckConfig buckConfig=new BuckConfig(rawConfig,new ProjectFilesystem(tmp.getRootPath()),Architecture.detect(),Platform.detect(),ImmutableMap.copyOf(System.getenv()),new DefaultCellPathResolver(tmp.getRootPath(),rawConfig));
  return new PythonBuckConfig(buckConfig,new ExecutableFinder());
}
