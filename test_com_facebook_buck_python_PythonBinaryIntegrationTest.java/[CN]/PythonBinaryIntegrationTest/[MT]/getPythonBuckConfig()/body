{
  Config rawConfig=Configs.createConfig(ConfigConfig.of().withProjectRoot(tmp.getRootPath()));
  BuckConfig buckConfig=new BuckConfig(rawConfig,new ProjectFilesystem(tmp.getRootPath()),Architecture.detect(),Platform.detect(),ImmutableMap.copyOf(System.getenv()));
  return new PythonBuckConfig(buckConfig,new ExecutableFinder());
}
