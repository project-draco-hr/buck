{
  super.buildFinished(finished);
  ImmutableList.Builder<String> lines=ImmutableList.builder();
  logEventPair("BUILDING",Optional.<String>absent(),clock.currentTimeMillis(),parseTime.get(),buildStarted,buildFinished,getApproximateBuildProgress(),lines);
  Optional<String> httpStatus=renderHttpUploads();
  if (httpStatus.isPresent()) {
    lines.add("WAITING FOR HTTP CACHE UPLOADS " + httpStatus.get());
  }
  printLines(lines);
}
