{
  String line=String.format("BUILT %s",finished.getBuildRule().getFullyQualifiedName());
  if (ruleCount.isPresent()) {
    line+=String.format(" (%d/%d JOBS)",numRulesCompleted.get(),ruleCount.get());
  }
  console.getStdErr().println(line);
}
