{
  super.onHttpArtifactCacheShutdownEvent(event);
  ImmutableList.Builder<String> lines=ImmutableList.builder();
  logEventPair("HTTP CACHE UPLOAD",renderHttpUploads(),clock.currentTimeMillis(),0L,firstHttpCacheUploadScheduled.get(),httpShutdownEvent,Optional.<Double>absent(),lines);
  printLines(lines);
}
