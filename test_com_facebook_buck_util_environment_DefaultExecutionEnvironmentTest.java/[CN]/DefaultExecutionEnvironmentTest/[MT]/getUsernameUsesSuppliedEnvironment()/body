{
  String name="TEST_USER_PLEASE_IGNORE";
  DefaultExecutionEnvironment environment=new DefaultExecutionEnvironment(ImmutableMap.of("USER",name),System.getProperties());
  assertEquals("Username should match test data.",name,environment.getUsername());
}
