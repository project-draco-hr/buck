{
  String name="TEST_USER_PLEASE_IGNORE";
  DefaultExecutionEnvironment environment=new DefaultExecutionEnvironment(new FakeProcessExecutor(),ImmutableMap.of("USER",name));
  assertEquals("Username should match test data.",name,environment.getUsername());
}
