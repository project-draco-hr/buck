{
  for (  HasAndroidResourceDeps dep : androidResourceDepsFinder.getAndroidResourcesUnsorted()) {
    addDep(dep.getBuildTarget());
  }
  return new UberRDotJava(buildTarget,resourceCompressionMode,resourceFilter,androidResourceDepsFinder,rDotJavaNeedsDexing);
}
