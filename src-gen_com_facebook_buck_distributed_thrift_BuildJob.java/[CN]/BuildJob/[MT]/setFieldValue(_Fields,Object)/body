{
switch (field) {
case BUILD_ID:
    if (value == null) {
      unsetBuildId();
    }
 else {
      setBuildId((BuildId)value);
    }
  break;
case DEBUG:
if (value == null) {
  unsetDebug();
}
 else {
  setDebug((DebugInfo)value);
}
break;
}
}
