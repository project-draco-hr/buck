{
  ProjectWorkspace.ProcessResult result=workspace.runBuckCommand("run","//:foreign#default," + linkStyle.toString().toLowerCase());
  result.assertSuccess();
  assertThat(result.getStdout(),Matchers.equalTo("hello world"));
}
