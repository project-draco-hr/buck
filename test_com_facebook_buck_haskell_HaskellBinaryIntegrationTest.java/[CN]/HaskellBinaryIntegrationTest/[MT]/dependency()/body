{
  ProjectWorkspace.ProcessResult result=workspace.runBuckCommand("run","//:dependent#default," + linkStyle.toString().toLowerCase());
  result.assertSuccess();
  assertThat(result.getStdout(),Matchers.equalTo("5"));
}
