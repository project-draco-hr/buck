{
  ProjectWorkspace.ProcessResult result=workspace.runBuckCommand("run","//:foo#default," + linkStyle.toString().toLowerCase());
  result.assertSuccess();
  assertThat(result.getStdout(),Matchers.equalTo("5"));
}
