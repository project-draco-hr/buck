{
  if (this == other) {
    return true;
  }
  if (!(other instanceof Condition)) {
    return false;
  }
  Condition that=(Condition)other;
  return Objects.equals(this.isPrefix,that.isPrefix) && Objects.equals(this.key,that.key) && Objects.equals(this.value,that.value);
}
