{
  ImmutableMap.Builder<String,String> env=ImmutableMap.builder();
  if (sanitizer.isPresent()) {
    env.put("PWD",sanitizer.get().getExpandedPath(context.getProjectDirectoryRoot().toAbsolutePath()));
  }
  return env.build();
}
