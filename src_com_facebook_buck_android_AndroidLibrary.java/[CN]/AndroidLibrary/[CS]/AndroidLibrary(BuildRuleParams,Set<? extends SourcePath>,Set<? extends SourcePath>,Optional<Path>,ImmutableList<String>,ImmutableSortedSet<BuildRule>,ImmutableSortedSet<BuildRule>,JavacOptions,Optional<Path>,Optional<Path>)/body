{
  super(buildRuleParams.getBuildTarget(),srcs,resources,proguardConfig,postprocessClassesCommands,buildRuleParams.getDeps(),exportedDeps,providedDeps,javacOptions,resourcesRoot);
  this.buildRuleParams=Preconditions.checkNotNull(buildRuleParams);
  this.javacOptions=Preconditions.checkNotNull(javacOptions);
  this.manifestFile=Preconditions.checkNotNull(manifestFile);
}
