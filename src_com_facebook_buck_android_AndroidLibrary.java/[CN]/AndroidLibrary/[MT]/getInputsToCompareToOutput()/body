{
  if (manifestFile.isPresent()) {
    return ImmutableList.<Path>builder().addAll(super.getInputsToCompareToOutput()).addAll(getResolver().filterInputsToCompareToOutput(manifestFile.get())).build();
  }
 else {
    return super.getInputsToCompareToOutput();
  }
}
