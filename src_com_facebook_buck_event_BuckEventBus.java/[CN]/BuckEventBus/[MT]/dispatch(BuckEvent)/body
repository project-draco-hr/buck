{
  for (  final Pair<ExecutorService,EventBus> executorAndBus : executorsAndBuses) {
    executorAndBus.getFirst().submit(new Runnable(){
      @Override public void run(){
        executorAndBus.getSecond().post(event);
      }
    }
);
  }
}
