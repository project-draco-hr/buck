{
  Path candidate=Paths.get("drawable-ldpi");
  Predicate<Path> predicate=ResourceFilters.createDensityFilter(ImmutableSet.of(candidate),ImmutableSet.of(Density.MDPI));
  assertThat(predicate.apply(candidate),Matchers.is(true));
}
