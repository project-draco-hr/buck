{
switch (field) {
case BUILD_ID:
    if (value == null) {
      unsetBuildId();
    }
 else {
      setBuildId((BuildId)value);
    }
  break;
case BUILD_GRAPH:
if (value == null) {
  unsetBuildGraph();
}
 else {
  setBuildGraph((ByteBuffer)value);
}
break;
}
}
