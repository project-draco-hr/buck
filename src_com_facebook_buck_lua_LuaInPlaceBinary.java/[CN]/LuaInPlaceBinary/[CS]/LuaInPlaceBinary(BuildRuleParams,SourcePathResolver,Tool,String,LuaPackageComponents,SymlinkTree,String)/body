{
  super(params,resolver,lua,mainModule,components,extension);
  this.lua=lua;
  this.components=components;
  this.linkTree=linkTree;
  this.script=getScript(resolver,lua,mainModule,getProjectFilesystem().resolve(getBinPath()).getParent().relativize(linkTree.getRoot()));
}
