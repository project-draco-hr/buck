def test_glob_allows_empty_results_with_flag(self):
    search_base = self.fake_path('foo', glob_results={'*.java': [], })
    self.assertGlobMatches([], glob_internal(includes=['*.java'], excludes=[], include_dotfiles=False, allow_empty=True, search_base=search_base))
