{
  if (buildRule instanceof WriteFile) {
    return false;
  }
  if (buildRule instanceof ProvidesStaticLibraryDeps || buildRule instanceof AppleDebuggableBinary) {
    return true;
  }
  return false;
}
