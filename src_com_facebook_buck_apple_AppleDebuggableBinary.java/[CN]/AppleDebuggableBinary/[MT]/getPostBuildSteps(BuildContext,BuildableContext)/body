{
  if (appleDebugFormat == AppleDebugFormat.NONE) {
    return ImmutableList.of();
  }
  return ImmutableList.<Step>of(new RegisterDebugSymbolsStep(binaryBuildRule,lldb,getResolver(),dsymOutput));
}
