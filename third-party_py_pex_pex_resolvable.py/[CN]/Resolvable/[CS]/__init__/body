def __init__(self, options):
    if (not isinstance(options, ResolverOptionsInterface)):
        raise TypeError(('Resolvable must be initialized with a ResolverOptionsInterface, got %s' % type(options)))
    self._options = options
