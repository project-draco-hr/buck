{
  return builder.set("moduleDeps",moduleDeps).set("modules",modules).set("vmArgs",vmArgs).set("style",style.name()).set("draftCompile",draftCompile).set("optimize",optimize).set("localWorkers",localWorkers).set("strict",strict).set("experimentalArgs",experimentalArgs);
}
