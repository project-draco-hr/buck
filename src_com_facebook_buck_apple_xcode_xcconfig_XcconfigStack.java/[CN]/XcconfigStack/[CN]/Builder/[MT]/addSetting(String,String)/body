{
  try {
    addSetting(XcconfigParser.parseSetting(String.format("%s = %s",keyCond,value)));
  }
 catch (  ParseException e) {
    throw new HumanReadableException(e,"failed to parse xcconfig setting:\n" + "  key: %s\n" + "  val: %s",keyCond,value);
  }
}
