{
  return new Function<String,Optional<TokenValue>>(){
    @Override public Optional<TokenValue> apply(    String name){
      if (name.equals(INHERITED) || name.equals(key)) {
        return Optional.of(inheritedValue);
      }
      return Optional.absent();
    }
  }
;
}
