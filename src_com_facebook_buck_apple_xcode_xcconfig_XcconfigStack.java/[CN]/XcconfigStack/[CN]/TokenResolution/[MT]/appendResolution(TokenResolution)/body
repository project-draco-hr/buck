{
  ImmutableSortedSet.Builder<Condition> builder=ImmutableSortedSet.naturalOrder();
  return new TokenResolution(builder.addAll(conditions).addAll(resolution.conditions).build(),value + resolution.value);
}
