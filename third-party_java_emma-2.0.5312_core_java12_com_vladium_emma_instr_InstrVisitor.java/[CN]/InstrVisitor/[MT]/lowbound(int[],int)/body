{
  int low=0, high=values.length - 1;
  while (low <= high) {
    final int m=(low + high) >> 1;
    final int v=values[m];
    if (v == x)     return m;
 else     if (v < x)     low=m + 1;
 else     high=m - 1;
  }
  return high;
}
