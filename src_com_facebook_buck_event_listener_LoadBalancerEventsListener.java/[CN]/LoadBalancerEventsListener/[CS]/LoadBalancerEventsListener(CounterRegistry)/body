{
  this.registry=registry;
  this.allServerCounters=Maps.newConcurrentMap();
  noHealthyServersCounter=registry.newIntegerCounter(COUNTER_CATEGORY,"no_healthy_servers_count",ImmutableMap.<String,String>of());
}
