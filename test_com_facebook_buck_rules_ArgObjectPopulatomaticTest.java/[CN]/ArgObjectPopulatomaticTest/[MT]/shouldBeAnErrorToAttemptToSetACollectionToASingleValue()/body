{
class Dto {
    public Set<String> strings;
  }
  Dto dto=new Dto();
  inspector.populate(ruleResolver,buildRuleFactoryParams(ImmutableMap.<String,Object>of("strings","isn't going to happen")),dto);
}
