{
class Dto {
    public Path path;
  }
  Dto dto=new Dto();
  inspector.populate(ruleResolver,buildRuleFactoryParams(ImmutableMap.<String,Object>of("path","./foo/../bar/./fish.txt")),dto);
  assertEquals(basePath.resolve("bar/fish.txt").normalize(),dto.path);
}
