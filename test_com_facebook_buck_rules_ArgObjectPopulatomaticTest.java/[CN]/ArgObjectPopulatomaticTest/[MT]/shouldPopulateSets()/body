{
class Dto {
    public Set<Path> paths;
  }
  Dto dto=new Dto();
  inspector.populate(ruleResolver,buildRuleFactoryParams(ImmutableMap.<String,Object>of("paths",ImmutableList.of("one","two"))),dto);
  assertEquals(ImmutableSet.of(Paths.get("example/path/one"),Paths.get("example/path/two")),dto.paths);
}
