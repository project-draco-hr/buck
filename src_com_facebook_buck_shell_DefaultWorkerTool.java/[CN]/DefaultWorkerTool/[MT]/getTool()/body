{
  Tool baseTool=this.exe.getExecutableCommand();
  return new CommandTool.Builder(baseTool).addInputs(FluentIterable.from(this.getDeps()).transform(SourcePaths.getToBuildTargetSourcePath()).toList()).build();
}
