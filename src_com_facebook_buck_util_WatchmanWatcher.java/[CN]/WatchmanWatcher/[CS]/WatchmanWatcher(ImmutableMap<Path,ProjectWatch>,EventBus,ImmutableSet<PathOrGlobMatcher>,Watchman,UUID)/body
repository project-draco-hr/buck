{
  this(fileChangeEventBus,watchman.getWatchmanClient().get(),DEFAULT_OVERFLOW_THRESHOLD,DEFAULT_TIMEOUT_MILLIS,createQuery(projectWatches,queryUUID.toString(),ignorePaths,watchman.getCapabilities()));
}
