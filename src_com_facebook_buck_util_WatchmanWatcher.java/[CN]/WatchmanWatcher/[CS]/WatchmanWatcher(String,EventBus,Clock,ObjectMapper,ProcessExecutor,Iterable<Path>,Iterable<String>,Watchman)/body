{
  this(fileChangeEventBus,clock,objectMapper,processExecutor,DEFAULT_OVERFLOW_THRESHOLD,DEFAULT_TIMEOUT_MILLIS,createQuery(objectMapper,watchRoot,watchman.getProjectPrefix(),UUID.randomUUID().toString(),ignorePaths,ignoreGlobs,watchman.getCapabilities()));
}
