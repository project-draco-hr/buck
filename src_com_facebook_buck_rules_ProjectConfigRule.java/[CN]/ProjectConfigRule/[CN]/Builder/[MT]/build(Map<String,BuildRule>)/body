{
  BuildRule srcRule=buildRuleIndex.get(srcTargetId);
  BuildRule testRule=buildRuleIndex.get(testTargetId);
  return new ProjectConfigRule(createBuildRuleParams(buildRuleIndex),srcRule,srcRoots,testRule,testRoots,isIntelliJPlugin);
}
