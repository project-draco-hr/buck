{
  BuildRule srcRule=ruleResolver.get(srcTargetId.orNull());
  BuildRule testRule=ruleResolver.get(testTargetId.orNull());
  return new ProjectConfigRule(createBuildRuleParams(ruleResolver),srcRule,srcRoots,testRule,testRoots,isIntelliJPlugin);
}
