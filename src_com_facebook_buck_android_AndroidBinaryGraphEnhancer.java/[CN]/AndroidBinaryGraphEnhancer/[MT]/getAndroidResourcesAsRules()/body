{
  return getTargetsAsRules(FluentIterable.from(androidResourceDepsFinder.getAndroidResources()).transform(new Function<HasAndroidResourceDeps,BuildTarget>(){
    @Override public BuildTarget apply(    HasAndroidResourceDeps input){
      return input.getBuildTarget();
    }
  }
).toList());
}
