{
  Path path=temp.newFolder().toPath().toAbsolutePath();
  ImmutableMap<String,Path> toolchains=AppleToolchainDiscovery.discoverAppleToolchainPaths(path,ImmutableList.<Path>of());
  assertThat(toolchains.entrySet(),empty());
}
