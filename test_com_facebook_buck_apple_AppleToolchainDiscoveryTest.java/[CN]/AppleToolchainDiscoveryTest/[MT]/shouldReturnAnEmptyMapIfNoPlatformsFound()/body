{
  Path path=temp.newFolder().toPath().toAbsolutePath();
  ImmutableMap<String,Path> toolchains=AppleToolchainDiscovery.discoverAppleToolchainPaths(path);
  assertThat(toolchains.entrySet(),empty());
}
