{
  if (cxxPlatform.getCpp().supportsHeaderMaps() && cxxPlatform.getCxxpp().supportsHeaderMaps()) {
    return ImmutableSet.of(CxxDescriptionEnhancer.getHeaderMapPath(target,cxxPlatform.getFlavor(),headerVisibility));
  }
 else {
    return ImmutableSet.of();
  }
}
