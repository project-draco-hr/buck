{
  return ImmutableList.<String>builder().addAll(compiler).addAll(flags).add("-od" + output.getParent().toString()).add("-of" + output.toString()).addAll(FluentIterable.from(inputs).transform(Functions.toStringFunction())).build();
}
