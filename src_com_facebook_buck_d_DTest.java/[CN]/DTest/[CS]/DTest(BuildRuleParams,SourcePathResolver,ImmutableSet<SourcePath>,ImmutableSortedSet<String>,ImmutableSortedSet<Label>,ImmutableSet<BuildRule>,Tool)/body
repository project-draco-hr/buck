{
  super(params,resolver,inputs,ImmutableList.of("-unittest","-main"),BuildTargets.getGenPath(params.getBuildTarget(),"%s/" + params.getBuildTarget().getShortName()),compiler);
  this.contacts=contacts;
  this.labels=labels;
  this.sourceUnderTest=sourceUnderTest;
}
