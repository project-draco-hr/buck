{
  if (isDryRun) {
    return ImmutableList.of();
  }
 else {
    return ImmutableList.of(new MakeCleanDirectoryStep(getPathToTestOutputDirectory()),new DTestStep(getShellCommand(),getPathToTestExitCode(),getPathToTestOutput()));
  }
}
