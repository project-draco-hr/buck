{
  for (  BuildRule dep : getDeps()) {
    if (dep.hasUncachedDescendants(context)) {
      logger.info(String.format("%s not cached because %s has an uncached descendant",this,dep.getFullyQualifiedName()));
      return false;
    }
  }
  return true;
}
