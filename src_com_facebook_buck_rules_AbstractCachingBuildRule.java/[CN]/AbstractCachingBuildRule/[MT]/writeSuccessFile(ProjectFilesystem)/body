{
  String path=getPathToSuccessFile();
  projectFilesystem.createParentDirs(new File(path));
  Iterable<String> lines=getSuccessFileStringsForBuildRules(false);
  projectFilesystem.writeLinesToPath(lines,path);
}
