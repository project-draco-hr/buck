{
  Preconditions.checkArgument(buildRuleResult.isDone(),"rule must be built before this method is invoked");
  if (isRuleBuilt()) {
    try {
      return buildRuleResult.get().isFromBuildCache();
    }
 catch (    InterruptedException|ExecutionException ignored) {
      return false;
    }
  }
 else {
    return false;
  }
}
