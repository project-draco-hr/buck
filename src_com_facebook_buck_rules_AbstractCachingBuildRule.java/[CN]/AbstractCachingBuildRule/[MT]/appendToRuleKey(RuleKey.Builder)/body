{
  Iterable<Path> inputs=getInputs();
  builder=super.appendToRuleKey(builder).setInputs("buck.inputs",inputs.iterator()).setSourcePaths("buck.sourcepaths",SourcePaths.toSourcePathsSortedByNaturalOrder(inputs));
  return buildable.appendDetailsToRuleKey(builder);
}
