{
  List<Path> keyList=Lists.newArrayList(entries.keySet());
  Collections.sort(keyList);
  for (  Path key : keyList) {
    builder.setReflectively("entry(" + key.toString() + ")",getResolver().getPath(entries.get(key)).toString());
  }
  return builder;
}
