{
  BuildRuleResolver ruleResolver=new BuildRuleResolver();
  return (Keystore)KeystoreBuilder.createBuilder(BuildTargetFactory.newInstance("//keystores:debug")).setStore(new TestSourcePath("keystores/debug.keystore")).setProperties(new TestSourcePath("keystores/debug.keystore.properties")).build(ruleResolver);
}
