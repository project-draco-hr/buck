{
  BuildRuleResolver ruleResolver=new BuildRuleResolver();
  return (Keystore)KeystoreBuilder.createBuilder(BuildTargetFactory.newInstance("//keystores:debug")).setStore(Paths.get("keystores/debug.keystore")).setProperties(Paths.get("keystores/debug.keystore.properties")).build(ruleResolver);
}
