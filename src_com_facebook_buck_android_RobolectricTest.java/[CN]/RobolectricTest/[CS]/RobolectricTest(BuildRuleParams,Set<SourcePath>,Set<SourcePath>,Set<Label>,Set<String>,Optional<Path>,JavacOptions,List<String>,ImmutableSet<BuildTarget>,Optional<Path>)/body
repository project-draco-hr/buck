{
  super(buildRuleParams.getBuildTarget(),srcs,resources,labels,contacts,proguardConfig,javacOptions,vmArgs,buildRuleParams.getDeps(),sourceTargetsUnderTest,resourcesRoot);
  this.buildRuleParams=Preconditions.checkNotNull(buildRuleParams);
  this.javacOptions=Preconditions.checkNotNull(javacOptions);
}
