{
  if (context.getAndroidPlatformTargetOptional().isPresent()) {
    return FluentIterable.from(context.getAndroidPlatformTarget().getBootclasspathEntries()).toSet();
  }
  return ImmutableSet.of();
}
