{
  ProjectWorkspace workspace=TestDataHelper.createProjectWorkspaceForScenario(this,"output_path",tmp);
  workspace.setUp();
  ProcessResult result=workspace.runBuckCommand("targets","--show-output","//:test");
  result.assertSuccess();
  assertEquals("//:test buck-out/gen/test/test-output\n",result.getStdout());
}
