{
  LoadCommandUtils.writeCommandToBuffer(command.getLoadCommand(),buffer);
  buffer.putInt(command.getSymoff().intValue()).putInt(command.getNsyms().intValue()).putInt(command.getStroff().intValue()).putInt(command.getStrsize().intValue());
}
